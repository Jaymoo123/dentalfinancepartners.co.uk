Manager pickup prompt — paste verbatim into a fresh Claude Opus 4.7 session in C:/Users/user/Documents/Accounting/

You are taking over as Manager for the Property Net-New Program. Previous manager session closed Wave 6 (2026-05-24) end-to-end and checkpointed before starting Wave 7 prep — HP-lock work is the highest-precision territory in the program and a fresh manager is the right cost/quality trade-off per §14 self-awareness. Wave 7 bucket selection is user-approved + documented; your job is to execute Wave 7 prep from HP-lock work through to a user-gated pre-launch summary. You are fresh.

Working directory: C:/Users/user/Documents/Accounting/ (Windows 11, PowerShell + Bash both available).

Read first:

1. **`docs/property/NETNEW_PROGRAM.md` end-to-end (~15-20 min).** Section 3 is up to date as of 2026-05-24 PM (Wave 6 CLOSED state). Pay close attention to:
   - **Section 0 critical norms** (verify before acting; self-awareness on context; never deploy without user instruction; never edit a session's branch without authorisation; `git status` on main is the primary signal of session activity, see §16.41).
   - **Section 3 Wave 6 close subsection** (8-commit close chain `3808019` → `0805d07`; 181 net-new pages on main; W4 + W5 + W6 held for deploy).
   - **Section 16 lessons**, particularly the recent batch:
     - §16.18 reasoning-first selection; §16.22-§16.27 / §16.30-§16.35 / §16.38-§16.40 Bill-vs-enacted-Act drift verification graduated to per-write mandatory + Stage 2 cross-check gate
     - §16.36 / §16.40 Stage 2 statutory cross-check earns its keep
     - §16.41 Q&A absolute-path discipline; activity-diagnosis discipline; watcher template hygiene (§16.41(d) recommends `## Q-\d+` heading count over STATUS: open line count for Wave 7+)
     - §16.42 EXISTING_PAGE_STALE flag density tracks new-bucket statutory novelty
     - **§16.43 (NEW)** — sub-agent dispatch validated at production scale for site-wide STALE sweeps; recommended pattern for Stage 2 brief generation + post-merge mechanical work
     - **§16.44 (NEW)** — WebFetch summarizer can hide HTML-table content; trust session-time captures when secondary verification path can't see the data
   - **Section 19 Wave 7 prep subsection** (2026-05-24 PM) — full 30-pick bucket list + ~10-11 HP-lock requirements + 10-step prep workflow + cross-bucket sequencing notes + inter-wave items still queued.

2. **`docs/property/house_positions.md`** — current state ~1770+ lines. Wave 6 §22 extension (§22.9-§22.15) + §25 cluster locked at commit `acb9151`. Wave 6 close additions (commit `3808019`) added rate-by-reference framing to §21.1 / §21.4 + NRB 2031 to §9 + §15.1 + s.169E note to §22.14 / §22.15. For Wave 7 you will be adding: NEW §26 Regulatory framework cluster (~6-8 sub-positions), NEW §27 HMRC enquiry mechanics cluster (~4-5 sub-positions), §22 extensions §22.16-§22.18 (TRS + IPDI/QIIP depth + EOT exit), §25.11 (s.198 election depth), §1.X (Sch 15 partnership SDLT relief).

3. **`docs/property/topic_gaps_final.md` lines 1295+** — Track 2B addendum (65 NEW candidates from sitemap-driven discovery; 28 require new HP locks); top 10 prioritised. Wave 7 picks draw heavily from this addendum (mapped per pick in §19 Wave 7 prep subsection).

4. **`docs/property/wave6_discovery_log_session_{A,B,C}.md`** — 74 discovery items from Wave 6. Several feed Wave 7 picks directly (TRS, EOT, IPDI/QIIP, s.198 depth). Read at HP-lock time for adjacent-position context.

5. **Verify state (run these 4 commands):**
   ```
   git log --oneline -10           # main HEAD should be 0805d07 (Wave 6 close, step 6 audit addendum) or newer if Track 2 manager has continued
   git status --short              # working tree may have Track 2 + pre-existing modifications (image.png, Medical, contractors-ir35, package-lock, optimisation_engine/*) — those are not Wave 7 scope
   git worktree list               # four worktrees: main + 3 medical (parked, medical-a locked). No property-wave6 worktrees (cleaned up at Wave 6 step 6).
   git branch | grep wave          # property-wave6-{a,b,c} branches retained for audit; medical-rewrite-{a,b,c} retained too
   ```

6. **Acknowledge with one short status line:** "Picked up. Main at <ref>, Wave 6 CLOSED at 0805d07. Wave 7 prep next: 30 picks (10 A regulatory/compliance + 10 B HMRC-enquiry + 10 C transactional/trust-depth) per NETNEW §19; ~10-11 HP locks to schedule; ~6 hours wall-clock to user-gated pre-launch summary. Ready to start with HP-lock work."

Standing context:

- **Wave 6 is CLOSED.** 181 net-new pages on main. Wave 7 prep is your job. The user has confirmed: hold W4+W5+W6 deploy further, intuition-led bucket picks, 3 buckets × 10 = 30 pages.
- **Wave 7 picks are user-approved.** See NETNEW §19 Wave 7 prep subsection for the explicit 30-pick list. Do NOT re-pick or re-litigate the bucket themes unless user explicitly asks.
- **HP-lock work is the load-bearing first task.** Lock §26 + §27 + §22.16-§22.18 + §25.11 + §1.X per the §19 subsection's "House-position locks required pre-Stage-2" section. Each position verified against legislation.gov.uk / gov.uk / HMRC manual / case law via WebFetch before locking. §16.18 reasoning-first + §16.40 statutory cross-check + §16.42 territory-novelty pattern all firmly load-bearing in this work.
- **Critical drift watchpoints during HP-lock:**
  - BSA 2022 commencement order status (some provisions still pending commencement as of 2026)
  - MEES extension SI status (verify SI 2025/* on legislation.gov.uk; may be SI 2025/something or SI 2026/something)
  - Landlord database commencement (may be pre-commencement; verify)
  - TRS penalty figures (current MLR 2017 reg 45ZA values)
  - EOT 50%+1 controlling-interest test post-FA-2025 reforms (verify enacted state)
  - Sch 24 FA 2007 penalty figures (current behaviour-category percentages)
  - RRA 2025 vs RRA 2026 nomenclature — Wave 1+5 used different naming; verify against legislation.gov.uk for the enacted Act's official short title
- **Track 2 (legacy rewrite) manager is running in parallel.** Their commits land on main directly (different scope from Wave 7 net-new). Recent: `0805d07` Wave 6 close step 6; before that `7083f40` Track 2 manager pickup. Coordinate if your prep commits touch shared files (`docs/property/topic_gaps_final.md`, `briefs/property/*`, or middleware.ts).
- **Q&A watcher is STOPPED.** Re-arm at Wave 7 launch using §16.41(d) `## Q-\d+` heading-count pattern (template-immune; Q heading count is monotonic so any baseline becomes a floor; no false-positive risk like the STATUS: open pattern hit at Wave 6 launch).
- **Sub-agent dispatch is your friend.** §16.43 validates the pattern at production scale. Use for Stage 1a seeds (one sub-agent for all 30) and Stage 2 briefs (one sub-agent per bucket = 3 parallel). For HP-lock work specifically — keep manager-side because precision is paramount and the work doesn't decompose cleanly.

Wave 7 prep workflow (full 10-step plan staged in NETNEW §19):

1. **HP-lock sessions** (~2-3 hours, manager-side, NOT sub-agentable).
2. **Cannibalisation re-check** against main at 181 pages.
3. **Stage 1a brief seeds** (sub-agent dispatch, ~1-2 hours).
4. **Stage 1b cross-check** (manager review of every seed against locked HPs, ~1 hour).
5. **Stage 2 full brief generation** (3 parallel sub-agents per §16.43 pattern, ~40 min each).
6. **Artefacts** (tracker + flags + 3× Q&A + 3× discovery shells).
7. **Worktrees standup** (3 fresh worktrees ff-verified to main HEAD per §16.25).
8. **START_HERE × 3 + WAVE7_LAUNCH_PROMPTS** (adapted from Wave 6 template + §16.41(d) watcher pattern + §16.43 sub-agent dispatch awareness).
9. **Watcher arm** with §16.41(d) heading-count pattern.
10. **User gate** with pre-launch summary.

Most likely first instruction from the user will be one of:

- "Start the HP-lock work" — proceed with §26 first (largest cluster, biggest verification surface), then §27, then §22 extensions, then §25.11, then §1.X. Verify each via WebFetch before locking. Commit each major lock as its own audit-trail commit.
- "Walk me through Wave 7 prep first" — give a tight summary of the 30 picks + HP-lock list + workflow steps; await go-ahead.
- "Re-open the bucket picks" — if user wants to swap any picks, refer to the addendum top-10 (NETNEW §19 references the lines) and the 65-candidate cluster breakdown.
- "Skip HP locks for buckets with stable statute" — Bucket B (HMRC enquiry) has the most stable statute (TMA 1970 + Sch 24 FA 2007); if user wants to start sessions there first and lock §26 + §22/§25 extensions later, that's a defensible reordering but increases drift risk on Bucket B brief seeds. Recommend against unless user is intentional about time pressure.

Section 14 manager self-awareness reminder: HP-lock work is high-precision; if your context fills mid-lock, write a handover update to NETNEW §19 (and a fresh Manager_prompt) and stop. Don't push through silently. The previous manager checkpointed at this exact point for this exact reason.

Operational reminders distilled from §16:

- **Tracker hygiene (§16.14, §16.15, §16.37, §16.41):** sessions write tracker / flags / Q&A / discovery via absolute paths to main per §16.15 + §16.37. Mid-wave, main's working tree is INTENDED to be dirty with these uncommitted edits. `git status` on main is the load-bearing diagnostic for "are sessions running right now". This is for Wave 7 RUN; not relevant during prep.
- **Reasoning-first (§16.18):** HP-lock drafting is reasoning-first work. Do NOT default to running scripts or boilerplating. Each position requires reading statute / manual / case law verbatim, deciding scope, drafting the position, and noting cross-references.
- **Statutory-section attribution drift (§16.40 / §16.42):** Wave 6 surfaced 4 consecutive brief-quality drifts on section numbers (C9 s.1149 vs s.1154; C10 FA 2021 Sch 9 Pt 2 vs ss.12/13; A8 SDLTM33500 vs SDLTM09050+; A10 s.491 abolished). Wave 7's Bucket B HMRC enquiry mechanics has high drift risk (TMA 1970 section numbers are easily confused; Sch 24 vs Sch 55 vs Sch 56 distinctions matter). Bake §16.36 statutory cross-check gate explicitly into Stage 2 dispatch.
- **Bill-vs-enacted-Act drift pattern (§16.22-§16.42):** 12 consecutive catches across the program. ANY locked house position carrying a "verify before relying" hedge must be verified at HP-lock time. New §26 + §27 are largely in stable-statute territory but the BSA 2022 + MEES + EPC C commencement status is moving and must be verified live.
- **Sub-agent dispatch pattern (§16.43):** sub-agents must be told explicitly NOT to commit and NOT to touch newly-merged Wave N pages. The Wave 6 close sub-agent dispatch worked cleanly; mirror the pattern for Wave 7 Stage 1a + Stage 2 dispatch.

Wave 7 HP-lock is the load-bearing first task. Do not start Stage 1a until the HP-locks are committed to main, because Stage 1a brief seeds reference the locked positions by section number.
