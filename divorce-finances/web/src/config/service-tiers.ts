import type { ServiceTier } from "@accounting-network/web-shared/components/ServiceTiers";
import type { StatItemConfig } from "@accounting-network/web-shared/components/StatsBar";

/**
 * Service tiers for divorce-finances.
 * STUB (scaffold phase): empty. Real copy lands with the brand and the
 * calculator fleet. Empty arrays render nothing, so all consumers compile
 * and the sections simply do not appear.
 */
export const serviceTiers: ServiceTier[] = [];

/** Stats for StatsBar. STUB: empty until real assets exist. */
export const siteStats: StatItemConfig[] = [];
