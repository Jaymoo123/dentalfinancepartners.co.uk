# Medical Diagnosis — 2026-07 (living state doc)

Program: full SEO/indexing/lead-paradox diagnosis of www.medicalaccounts.co.uk, then owner-gated fixes.
This file is the WRITE-AHEAD LEDGER and single source of truth for program state. A fresh agent with zero context resumes from HERE.

## ⚡ RESUME HERE
- **AUDIT FIX WAVE also SHIPPED 2026-07-06** (owner asked "is everything truly done" → adversarial completeness audit `wf_b561da06-513` found minor-gaps + 1 material (calculator mojibake) → fix wave `wf_27dcd88b-8af` executed all 14 fix-now items → deploy `dpl_F8Q3qSCwRmeVsFKRATEPsisasTB1`, tag `deploy/medical/2026-07-06-audit-fixes`, commit `4702b8bd`; live markers verified: mojibake 0, favicon 200, homepage FAQPage+WebSite+sameAs live, doubled titles gone). Audit verdict doc = `docs/medical/COMPLETENESS_AUDIT_2026-07-06.md`.
- **Residuals (small, known):** apex redirect still 307 (platform-edge level; owner: Vercel dashboard → Domains → medicalaccounts.co.uk → set redirect 308); /contact title keeps doubled brand suffix (freeze-protected, revisit only with owner); manager-direct factual sweep pending: locum calculator stale "Class 2 still payable" note + homepage visible-FAQ em-dash (frozen) + 2025/26-vs-2026/27 year-label audit across ~45 posts; Speed Insights needs enabling in Vercel dashboard to start collecting.
- **The real 10x levers (owner decision, next program):** flagship citable data asset (medical equivalent of Property's landlord index, e.g. NHS Pension Annual Allowance burden report) + faceless off-site authority. On-site is now at practical max pending those.
- **PROGRAM EXECUTION COMPLETE 2026-07-06.** Phases 0–5 all DONE. Deployed to prod: `dpl_HHomcnfjnDbC9bRB9A3878r7HdST` (tag `deploy/medical/2026-07-06-diag`, commit `6d0c1930`). All new-content markers verified live (llms.txt 94 lines/0 phantom, blog SSR 86 links, CTA blocks live, sitemap 117 URLs, new posts 200). Lead-pipeline battery 4/4 PASS. IndexNow 24 URLs HTTP 200. Weekly `MedicalCoverageWatch` scheduled task live (Mondays 08:00 → `.cache/medical_diag/index_coverage_watch.json`).
- Shipped: MED-F13 lastmod pin · MED-F12 llms.txt repair+enrich · MED-F5 blog SSR (all 78 posts) · MED-F6 orphan cross-links · MED-F8 CTAs on 8 posts (widened per MED-F9 diagnostic: 83 Bing sessions, 0 reached converting surfaces) · MED-F9 diagnostic · MED-F11 wave (5 posts, Opus QA) · GEO slice (feed.xml flat-URL fix, JSON-LD on 4 /for-* + /nhs-pension) · AMAP 45p→55p sweep · MED-F7 freeze held byte-identical.
- **REMAINING — OWNER (both quick):** (1) GSC UI: submit `https://www.medicalaccounts.co.uk/sitemap.xml` in the Sitemaps panel (API submit 403'd: token is webmasters.READONLY; `scripts/medical/gsc_sitemap_submit.py` is ready once scope upgraded — delete `secrets/snapshot_token.json`, change scope to `webmasters` in snapshot auth, re-consent). (2) GSC UI Request Indexing (~10/day): /services /about /contact /nhs-pension /for-gps /for-consultants /blog + /blog/gp-accounting-guide + /blog/locum-doctor-self-assessment-filing-guide. OPTIONAL: MED-F10 Serper top-up; confirm NOTIFY_TO_MEDICAL unset is intentional (medical leads currently operator-routed only).
- **WATCH WINDOWS (next agent's job):** 14d (~2026-07-20): sitemap lastDownloaded refreshes (was 2026-06-04), indexed 6→~15-20 (read `MedicalCoverageWatch` output or run the sweep). 28d (~2026-08-03): indexed ~20-40+, first Google clicks plausible, CTA posts' first lead, AI-channel leads hold post-llms.txt. Failure test: still ≤10 indexed at 28d after a confirmed sitemap re-fetch ⇒ authority wall, pivot to faceless off-site authority (per §3 honest criteria). Note: owner knowingly combined all changes in one deploy, so attribute signals with care (manifest above).
- CRITICAL caveats for battery/synthesis (from P2.r review): (1) `leads.source_url` = form-SUBMIT page (~always /contact), NOT acquisition page — use `entry_page`/`by_entry_page`; true entries = homepage ×4 + GP-partner blog ×1 + 2 unknown. (2) index-absence bucket = `unknown_to_google`. (3) Serper `site:` probes for the typo domain retry ≥2026-07-07 (cost-log idempotency). (4) 7 leads total — absolute counts only.
- Manager: Fable (quota-limited). Fallback: fresh Opus session — read this file top-to-bottom, then the plan file below. NEVER cross an [OWNER] gate autonomously.
- Plan file (the HOW/WHAT, incl. full tool specs + battery specs + guardrails): `C:\Users\user\.claude\plans\medical-site-seems-to-delegated-chipmunk.md`
- Handoff read order: (1) this file, (2) plan file, (3) `docs/medical/STATE.md` + `docs/medical/house_positions.md`, (4) memory LAST — this file overrides memory on any conflict.
- Do-not-redo rule: trust DONE/REVIEWED-PASS rows after running their one-line verify cmd (seconds). Re-run only IN-PROGRESS rows, from their "next action".

## 0. Invariants (never re-derive)
- GSC property (CANONICAL): `sc-domain:medicalaccounts.co.uk`. Site: https://www.medicalaccounts.co.uk. Vercel root `Medical/web`.
- THREE domain strings, keep separate: `medicalaccounts.co.uk` (current/correct) · `medicalaccountantsuk.co.uk` ("old" — legacy post canonicals pointed here until fixed 2026-06-17; owner says NEVER owned it → phantom-canonical hypothesis) · `medicalaccountants.co.uk` (dead typo string, bug in `optimisation_engine/snapshot/diagnostics.py` `_gsc_site_url_for`).
- `optimisation_engine/clients/gsc_page_client.py` is ALREADY CORRECT (fixed 2026-06-03). Do NOT "fix" it. Stale docs (`page_rewrite_tracker.md`, rewrite state "NOT committed" line) will tempt you — they are superseded.
- FLAT blog routing `/blog/<slug>`: NEVER run `slug_resolver --fix` or shared nested link auditors on medical. Use `scripts/medical_flat_link_audit.py`.
- NO DataForSEO spend for this program (medical out of rotation; $1.00 trial ceiling). Serper = fixed short list only; DDG free.
- Denominator warning: medical ≈ 238 sessions/30d. ALWAYS report absolute counts beside any rate; suppress rates with denominator < 20.
- Owner decisions LOCKED 2026-07-06: (1) STOP after Phase 4 synthesis — no fix applies until owner reads it; (2) every deploy is per-deploy owner sign-off; (3) owner never owned the old domain.
- Battery agents fail LOUD on 401/403/empty — never write an empty artifact as DONE.
- Raw artifacts → `.cache/medical_diag/` (uncommitted). Curated findings → this doc + `docs/medical/*.md` (committed).

## 1. Step ledger

Status enum: TODO / IN-PROGRESS / DONE / REVIEWED-PASS / BLOCKED / DROPPED.

| step | phase | status | owner-gated? | artifact | verify cmd (1 line) | next action |
|---|---|---|---|---|---|---|
| P0.a bootstrap state doc + .cache/medical_diag/ | 0 | DONE | no | this file | `git log --oneline -1 -- docs/medical/DIAGNOSIS_2026-07.md` shows a commit | — |
| P1.a staleness census + sites-row verify | 1 | DONE | no | `.cache/medical_diag/staleness.json` + `scripts/medical/00_staleness.sql` | `python scripts/_q.py scripts/medical/00_staleness.sql` re-runs census | — |
| P1.b fix `medicalaccountants` typo in optimisation_engine | 1 | DONE | no | diff in Decision log | `grep -rn "medicalaccountants\.co\.uk" optimisation_engine/` → NO match (md docs elsewhere may match; that's fine) | — |
| P1.c backfill + re-ingest (GSC queries+pages; fresh Bing snapshot; GA4 60d pass; bat log redirect) | 1 | DONE | no | `.cache/medical_diag/ingest_p1c.json` | `python scripts/_q.py scripts/medical/00_staleness.sql`: gsc max(date) ≥ today-3, bing snapshot ~today | — |
| P1.d dead-feed triage (GA4, bing_ai probe) + daily-task health | 1 | DONE | no | `.cache/medical_diag/feed_triage.json` | JSON exists w/ per-feed classification | — |
| P2.t1 build Tool 1 index-coverage sweep (`optimisation_engine/snapshot/index_coverage.py`) | 2 | DONE | no | tool + smoke JSON | `python -m optimisation_engine.snapshot.index_coverage medical --limit 2 --skip-bing` | P2.r review; P3.b3 full run = `... medical --fresh` (~5-7 min, 112 URLs) |
| P2.r1 Opus review Tool 1 | 2 | REVIEWED-PASS (fixed: unknown_to_google bucket added) | no | verdict in Decision log | `python -m optimisation_engine.snapshot.index_coverage medical --limit 2 --skip-bing` | — |
| P2.t2 build Tool 2 lead attribution (`scripts/medical/40_lead_attribution.sql` + `lead_attribution.py`) | 2 | DONE | no | tools + `.cache/medical_diag/lead_attribution.json` | `python scripts/medical/lead_attribution.py` | P2.r review; P3.b5 re-run picks up fresh Bing snapshot for dark-AI lens + add per-site lead-count context query |
| P2.r2 Opus review Tool 2 | 2 | REVIEWED-PASS (fixed: entry vs submit page split; cp1252 exit bug) | no | verdict in Decision log | `python scripts/medical/lead_attribution.py` exits 0 | — |
| P2.t3 build Tool 3 domain forensics (`scripts/medical/domain_forensics.py`) | 2 | DONE | no | tool + `.cache/medical_diag/domain_forensics.json` (smoke = real run) | `python scripts/medical/domain_forensics.py` (idempotent) | P3.b4 = retry Serper site: probe (409'd) + `--coverage-json` residue join after B3 |
| P2.r3 Opus review Tool 3 | 2 | REVIEWED-PASS (fixed: Serper 409 = already_queried_today, not error) | no | verdict in Decision log | `python scripts/medical/domain_forensics.py` exits 0 | — |
| P3.b1 Google read | 3 | DONE | no | `.cache/medical_diag/google_segments.json` + `docs/medical/google_read.md` | doc exists w/ data-window header | — |
| P3.b2 Bing read + SERP diff | 3 | DONE (Serper credits EXHAUSTED — Google SERP probes failed loud; DDG 12/12 OK) | no | `bing_segments.json`, `serp_headqueries.json` + `docs/medical/bing_read.md` | doc exists w/ matched-window note | — |
| P3.b3 index-coverage sweep (112/112, --skip-bing rerun after DDG rate-limit) | 3 | DONE | no | `index_coverage.json` + `docs/medical/index_coverage.md` | `python -m optimisation_engine.snapshot.index_coverage medical --limit 2 --skip-bing` | — |
| P3.b4 phantom-domain forensics (2nd pass w/ coverage join) | 3 | DONE | no | `domain_forensics.json` + `docs/medical/old_domain_forensics.md` | doc exists w/ DNS + residue counts | Serper site: retry ≥07-07 optional (moot for phantom) |
| P3.b5 lead attribution + Opus judgment | 3 | DONE | no | `lead_attribution.json` + `lead_context.json` + `docs/medical/lead_paradox.md` | `python scripts/medical/lead_attribution.py` exits 0 | — |
| P3.b6 opportunity read | 3 | DONE (Google SERP sampling blocked: Serper credits exhausted; DDG substituted) | no | `serp_sample.json` + `docs/medical/opportunity_read.md` | doc exists w/ query list | — |
| P3.b7 inlink depth vs index buckets | 3 | DONE | no | `inlink_correlation.json` + section in `docs/medical/index_coverage.md` | '## Internal linking vs index state' section present | — |
| P4.synth Opus synthesis + 5 adversarial skeptics → diagnosis + ranked fix backlog | 4 | DONE (skeptics: 3 hold, 2 downgrade — llms.txt phantom residue + sitemap lastmod churn caught; corrections applied) | no | §3 + §4 of this doc + `docs/medical/DIAGNOSIS_2026-07_SYNTHESIS.md` | §3 and §4 non-empty; SYNTHESIS doc exists | STOPPED at P4.gate |
| P4.gate OWNER reviews diagnosis before any fix | 4 | TODO | **YES** | — | owner go recorded in Decision log | — |
| P5.* fixes (backlog populated by P4; deploys per-deploy [OWNER]) | 5 | TODO | per-class | — | — | — |

## 2. Findings ledger (headlines only; raw detail in artifacts)

### Battery results (P3, 2026-07-06 — workflow wf_f017a22b-488, all lanes confidence=high)
- [P3.b3 ★HEADLINE] **103/112 URLs (92%) are "unknown to Google" — never discovered.** Indexed = 6 (homepage + 5 blog posts); crawled-not-indexed = 1; zero suppression signals (no noindex/robots/manual). Only 10/112 URLs ever earned a GSC impression. Sitemap live+valid (200, 112 URLs) but **GSC last downloaded it 2026-06-04 (32 days ago)**; GSC "indexed: 0" panel is a domain-property display artifact (URL Inspection is ground truth). Bing (BWT data) knows 38/112 incl. pages Google never crawled. Mechanism = DISCOVERY failure on a young low-trust domain, not quality suppression.
- [P3.b1] Google shape = **YOUTH dominant, injury secondary**: impressions/day accelerating 31→36→55 (pre→mig→post), weighted position improving 43.2→39.3→32.6 continuously. Phantom-canonical trace measurable but recovering: "gp accountants" (1,311 impr, 0 clicks ever) 47→63→59; paired mig→post pages 7/8 improved (median Δ −3.65). 27 clicks all-time, all from blog posts (top: becoming-gp-partner 16); homepage 2,137 impr / 4 clicks at pos 53–64. Post window only 17d; recovery typically 4–12 wks.
- [P3.b2] **Bing works**: impr 142→706, clicks 15→70 across 33 days, pages 12→38, weighted position stable ~5.3–5.6 (breadth growth, not depth). DDG spot checks: pos 8 "gp accountants", pos 6 "medical accountants uk", pos 8 "locum accountant" (Google: 54–82 same terms). Verdict: Bing outperforms because Google never indexed the site — content quality is NOT the blocker. ⚠ Serper credits EXHAUSTED (HTTP 400 all 12 queries) — owner top-up needed for any live Google SERP work.
- [P3.b4] **Phantom-canonical injury RESOLVED**: 0/112 google_canonical residue; the 2 flagged pages are crawl-lag artifacts (last crawled pre-fix; live source clean; Google already overrode). Phantom domain NXDOMAIN/never-registered/0 real results. Cheap follow-up: GSC "Request Indexing" on the 2 stale pages.
- [P3.b7] **Internal linking does NOT explain non-discovery**: 70/103 unknown pages have ≥3 editorial inlinks; median inlinks indexed=27 vs unknown=6; max click depth 2; 0 hard 404s; all 8 category hubs link 100% of their posts. Googlebot crawled homepage 06-29 and followed no outbound links (crawl-budget signature). Real defects found: blog index SSRs only first 12/73 posts (use-client component — 61 posts need JS); 8 structural orphans (5 /locations/*, 3 /calculators/* — footer/nav only).
- [P3.b5] **Lead paradox ANSWERED**: 7 leads total (estate: property 51 · medical 7 · dentists 5 · generalist 4 · solicitors 2 · agency 0 · ir35 0 — "#2" literally true but brittle, 2 leads clear of #3). True channels: **ChatGPT/AI 3 · Google 2 · genuine direct 2 · Bing 0** (lead #6 direct-classified but utm_source=chatgpt.com). Entry surface: homepage ×4, GP-partner blog ×1, 2 pre-tracking unknowns. Monthly: Apr 2, May 0, Jun 1, Jul 4 — accelerating and shifting to AI. **Bing = most search traffic (96 human sessions/90d) but ZERO leads** (may be low-intent long-tail, not a bug — investigate don't assume). Conversion 5/142 GB human sessions. web_sessions for medical only exist since 2026-06-11.
- [P3.b6] **Opportunity is small and gated**: 668-topic pool collapses to 8–12 genuine specialist white-space topics; recommended ceiling ~5 net-new posts (consultant ltd-co advanced cluster + medico-legal income). SERPs (DDG) dominated by NHS/gov + fragmented generalists — thin specialist moat. LOAD-BEARING SEQUENCING: content queues BEHIND Google index repair (73 posts already earn 0 Google clicks / 2,187 impr lifetime; Bing 157 clicks / 1,654 impr).
- [P1.a] Feed census 2026-07-06: gsc_query_data 517 rows (04-01→06-22, STALE 14d) · gsc_page_performance 332 rows (04-01→06-22, STALE 14d; table uses column `niche` not site_key) · bing_query_data 741 rows (06-03→06-24, STALE 12d) · ga4_page_data 28 rows (dead since 05-16) · bing_ai_performance 0 rows. `sites` row VERIFIED correct (sc-domain:medicalaccounts.co.uk, active). GSC history starts 04-01 → pre-May-20 windows currently ABSENT; P1.c backfill from ~2026-02-01 is required for any pre/post claim. → `.cache/medical_diag/staleness.json`
- [P1.c] Feeds repaired 2026-07-06. **DATA-TRUST HEADER (cite in every downstream verdict):** gsc_query_data 610 rows + gsc_page_performance 403 rows, both 2026-04-01→2026-07-04 FRESH; **GSC is API-confirmed EMPTY before April 2026** (zero impressions pre-April — site young, NOT an auth artifact) so windows are: pre = Apr 1–May 19, mig(canonicals-wrong) = May 20–Jun 17, post = Jun 18–Jul 04, and any "pre" claim carries the thin-window caveat. bing_query_data 1,290 rows, snapshots 2026-06-03→2026-07-06 FRESH (rolling aggregates by design — NO pre-May-20 Bing data can ever exist; matched-window comparisons only). ga4_page_data restored: 196 rows 2026-04-18→2026-07-04 (outage was an expired OAuth token; estate-wide restored too). bing_ai_performance DEAD (404) — parked. Ingests all exit 0 → daily-task failure is INTERMITTENT; log redirect added to run_daily_snapshot.bat for next occurrence. → `.cache/medical_diag/ingest_p1c.json`
- [P2.t2 smoke — PRELIMINARY, re-verify in P3.b5] Medical has **7 leads TOTAL** (absolute count). Channels: direct 3 · google 2 · chatgpt 2 · bing 0. Attribution: 5/7 via session, 2/7 source_url-only (early-capture tracking gap), 0 unattributed-fully. **All 7 converted on /contact.** GSC query data covers only 8 blog/content pages, none of them /contact. Dark-AI lens returned empty because smoke ran with <2 Bing snapshots per page (P1.c snapshot may not have been visible yet) — re-run in P3.b5. Paradox direction: AI referrals + direct (possibly dark-AI) + a trickle of Google; NOT ranking-driven. NOTE for synthesis: sanity-check the "#2 lead source" claim with a per-site lead count (7 absolute is tiny). `leads.id` is UUID. → `.cache/medical_diag/lead_attribution.json`
- [P2.t1 smoke — PRELIMINARY, 5-URL sample only, confirm in P3.b3] Sitemap holds **112 URLs** (not ~90). Homepage = "Submitted and indexed", self-canonical, Bing-indexed. But **4/4 other sampled core pages (/services, /nhs-pension, /calculators, /about) = "URL is unknown to Google" — never crawled**. If this generalizes, medical's problem is INDEX ABSENCE (sitemap-processing/discovery failure), not low ranking. Note /calculators only shipped 07-05 (M-2), but /services and /about are old — "unknown" on those is anomalous. → smoke in tool report; full sweep artifact lands at `.cache/medical_diag/index_coverage.json`
- [P2.t3 = de-facto B4 first pass] **PHANTOM DOMAIN CONFIRMED**: medicalaccountantsuk.co.uk was NEVER REGISTERED (Nominet RDAP 404), NXDOMAIN apex+www, no GSC property in our account (11 properties enumerated; medicalaccounts.co.uk present), 0 DDG results. ⇒ all 46 legacy posts canonicalled into a DNS hole until 2026-06-17 (Google behaviour undefined for unreachable canonical targets; consolidation signal lost). The typo string medicalaccountants.co.uk = REAL third-party domain (GoDaddy 2016, AWS IPs) but only ever lived in diagnostics.py config (fixed) — never in page canonicals. OPEN: Serper Google site: probes 409'd on cost-log idempotency — retry in P3.b4. → `.cache/medical_diag/domain_forensics.json`
- [P1.d] Scheduler NOT the problem: task `SiteHealthDailySnapshot` fires daily (last 2026-07-05), but the Python run FAILS WITHIN-RUN (exit 0x800710E0): property reached 07-01, generalist 06-27, other sites stuck ~06-19/22 → per-site GSC credential/quota failure mid-loop; bat writes no log so exact exception unknown (fix: add `>> .cache\daily_run.log 2>&1`). GA4 = TRIVIALLY-REPAIRABLE (token was expired; agent refreshed it, live probe returned data; estate-wide all 4 sites stopped May 9–27; one ingest pass backfills). bing_ai_performance = CONFIRMED-DEAD (GetAiPerformance → HTTP 404, method name was a guess; harmless — swallowed; PARK until real endpoint known). → `.cache/medical_diag/feed_triage.json`

## 3. Synthesis (Phase 4 — Opus writes here)

**Author: P4 integrator (Opus). Date: 2026-07-06. Status: FINAL for owner review (P4.gate). No fix applies until the owner reads this (locked decision #1); every deploy is per-deploy owner-gated (decision #2). Standalone readable version: `docs/medical/DIAGNOSIS_2026-07_SYNTHESIS.md`.**

Data-trust header (inherited): GSC data exists only 2026-04-01 to 2026-07-04 (API-confirmed empty before April, a genuinely young site, not an auth artifact). Windows: pre = Apr 1 to May 19 (49d, thin, indicative), mig = May 20 to Jun 17 (29d, legacy canonicals pointed at the never-registered phantom domain), post = Jun 18 to Jul 4 (17d, clean canonicals). Bing snapshots 2026-06-03 to 2026-07-06 (rolling trailing aggregates, matched-window only). web_sessions exist only from 2026-06-11 (~25d). Absolute counts sit beside every rate; rates with a denominator under 20 are suppressed. Serper (live Google SERP) credits are exhausted (HTTP 400), so Google positions are proxied from GSC weighted average.

### Verdict summary

The site is not being punished; it is barely being seen. 103 of 112 URLs (92%) are "unknown to Google" (never crawled, never queued) with zero suppression signals (0 noindex, 0 robots blocks, 0 manual actions). Google has indexed 6 of 112 (the homepage plus 5 blog posts); 1 is crawled-not-indexed; 2 are canonicalised-away as crawl-lag only. Bing knows at least 38 of 112 through the same live HTML, which proves the pages are accessible and crawlable, so the failure is Google-specific. This is a discovery problem, not a ranking, content-quality, or suppression problem. Evidence: index_coverage.md §1, bing_read.md §5.

Five conditions were tested. (1) Invisible-by-youth is the dominant condition: all commercial head terms sit at positions 47 to 85 across the entire GSC history including the pre window (gp accountants 47 to 63 to 59, medical accountants uk 69 to 61, specialist medical accountants 85 to 65), with 0 head-term clicks ever, and only 10 of 112 URLs have ever earned a single impression. There was no high-ranking period that then collapsed. (2) Invisible-by-injury (the phantom-canonical damage) is real but secondary and largely, not fully, resolved (see below). (3) Invisible-but-converting: leads are decoupled from search traffic (below). (4) Not-yet-discovered is the master mechanism (below). (5) Opportunity white-space is small and gated behind index repair (below).

### Root-cause ranking

| # | Root cause | Strength | What would falsify it |
|---|---|---|---|
| 1 | Young-domain near-zero crawl budget leading to discovery failure (Google fetches the homepage 2026-06-29, follows no links, queues nothing from the sitemap; /contact has 63 editorial inlinks yet is unknown) | Strong | After a confirmed fresh sitemap download plus Request Indexing on core pages, those pages stay "unknown to Google" at 28d AND indexed count stays <= 10. |
| 2 | Sitemap fetch staleness plus churning lastmod (GSC last downloaded 2026-06-04; the 15 new URLs are invisible; `sitemap.ts` emits a fresh `lastModified` on every build so Google sees meaningless change on every path) | Moderate (actionable symptom and lever) | Re-submission plus a pinned lastmod refreshes the fetch but indexed count stays ~6 at 28d. |
| 3 | Phantom-canonical historical injury (46 posts canonicalled to NXDOMAIN; page canonicals fixed Jun 17; one AI-index residue still live in `llms.txt`) | Moderate (secondary; page canonicals resolved, one AI-surface residue remains) | Live page source shows a phantom canonical on either flagged page (it does not, B4 verified), OR the post-window recovery reverses. |
| 4 | YMYL / E-E-A-T authority deficit on head commercial terms | Moderate (ranking ceiling, not discovery) | Head terms climb to page 1 after the indexing fix without any authority work. |
| 5 | SSR-limited blog index (12 of 73 posts in initial HTML; 61 need JS) | Weak-to-moderate (blog-specific aggravator) | SSR-ing all 73 posts alone recovers blog discovery while server-rendered core pages stay dark. |
| 6 | Internal linking / structural orphans | Weak (ruled out as general cause; real only for 8 orphans) | Cross-linking the 8 orphans gets them indexed while comparable well-linked non-orphans stay unknown. |

Mechanism detail for #1: Google demonstrably received the homepage links. A live Googlebot-UA fetch with no JS returns HTTP 200 with real anchor links to /services, /about, /contact, /nhs-pension, /for-gps, /for-consultants, /blog, /medical-guides, /calculators, /locations plus 3 featured posts, so "followed none of them" is crawl budget, not a render gate. robots.txt allows Googlebot (only /api/ and /thank-you disallowed), all sampled unknown core pages return 200 with clean self-canonicals and no noindex, and the sitemap is 200/valid/112 URLs. Part of Bing's lead over Google is that Bing received an explicit IndexNow push on 2026-06-03 that Google never got, which reinforces (not weakens) the discovery thesis. Evidence: index_coverage.md internal-linking §2/§7/§8.

The upstream lever is off-site authority. The reason crawl budget is near zero is that the domain has essentially no external authority signals. This is the slow root and, per the owner constraint, any authority-building must be faceless (data-PR, tools, citations, GEO), not named-expert PR. It sets the ceiling on how fast discovery can be forced by pinging alone.

Note on the GSC "indexed: 0" sitemaps panel: it is a known display artifact for domain (sc-domain:) properties; URL Inspection (6 indexed) is ground truth. Do not read "indexed: 0" as total failure. Evidence: index_coverage.md §6.

### The phantom-canonical injury (narrowed)

Page canonicals are clean and the injury is correctly secondary. B4 fetched both flagged pages' live source (gp-accounting-guide, locum-doctor-self-assessment-filing-guide) and found clean self-canonicals to medicalaccounts.co.uk; 0 of 112 pages carry a phantom google_canonical. The injury did not cause the discovery failure: Bing indexed at least 38 of 112 through the identical phantom-era HTML (a host-wide canonical-trust collapse would have hit Bing too), the discovery failure predates the phantom era, and the unknown core pages never carried a phantom canonical at all. Evidence: old_domain_forensics.md §2/§4.

Two honesty corrections from adversarial review. First, residue is NOT zero. The live file `Medical/web/public/llms.txt` (HTTP 200) still declares the phantom domain medicalaccountantsuk.co.uk for the homepage, services, about, blog, contact, locations, nhs-pension, calculators and the sitemap URL. This is the AI-discovery index (the dynamic llms-full.txt route even points crawlers to it), and it aims AI answer-engines at a dead NXDOMAIN. Because ChatGPT/AI is the largest and accelerating lead channel (3 of 7 leads), this is a live defect, tracked as MED-F12. Second, the recovery evidence is directional, not proof. The 7-of-8 paired mig-to-post improvement (median delta -3.65 positions) is a sign-test p ~ 0.07 two-sided at n=8 (not significant), it is confounded with ongoing maturation (page-level weighted position improved monotonically 43.2 to 39.3 to 32.6, i.e. during the phantom era too), and the headline "gp accountants" 47 to 63 to 59 move is a single-query, homepage-dominated average on a page that never carried a phantom canonical. So the fix is credible but a maturation window to mid/late-July is owed before declaring the recovery complete.

### The lead paradox (resolved)

Leads are decoupled from search traffic. 7 leads total. True channels: ChatGPT/AI 3 (leads #5/#6/#7, all July, all landed on the homepage; #6 was raw-classified "direct" but carries utm_source=chatgpt.com and the ChatGPT-app referrer-strip signature) · Google 2 (one GP-partner blog, one homepage) · unattributable 2 (April, pre-tracking, no session; not confirmed direct, and since April had 1 Google click one of the two could be Google-organic) · Bing 0. Monthly trend is accelerating and shifting to AI: Apr 2, May 0, Jun 1, Jul 4. Conversion: 5 tracked leads / 142 human GB sessions (~25d) = 3.5%; the homepage is by far the densest surface (4 leads / 14 GB homepage sessions, rate suppressed at a denominator under 20). Traffic volume does not predict leads: the two busiest blog entries (gp-partner-vs-salaried 25 sessions, gp-partnership-tax-complete-guide 21 sessions) and the entire 96-session Bing stream produced 0 leads.

Bing's "0 leads from 96 human sessions" is a landing and intent-mix artifact, not a conversion defect: 0 of those 96 sessions ever landed on the homepage (the 4-of-14 converting surface) or /contact; all 96 landed on informational blog and guide posts with high engagement (roughly 1.7 to 6.8 minutes, deep scroll, 20 to 40 events). Bing is delivering engaged readers to the wrong (non-converting) surfaces, which strengthens the decoupling thesis. Evidence: lead_paradox.md §1 to §4/§6.

### Opportunity white-space (small, gated)

Genuine specialist net-new collapses from the 668-topic pool to roughly 8 to 12 topics; recommended ceiling ~5 net-new posts; do not commission content until Google discovery is proven to work. White-space: OPP-1 consultant/private-practice advanced Ltd cluster (~3 to 4 posts) · OPP-2 medico-legal / expert-witness income (1 post) · OPP-3 NHS-pension singletons folded into existing pages. Sequencing is load-bearing: 73 already-published specialist posts that cover live demand earn 0 Google clicks across 2,187 lifetime impressions (positions 40 to 90), so adding posts to a site Google is not indexing pours content into a hole. Evidence: opportunity_read.md §1/§3/§5.

### Adversarial verification record

Five skeptics attacked the six core claims. Two verdicts downgraded, three held; every required correction is applied above and in §4.

- **Index-absence (103/112 unknown is real, not an artifact): HOLDS.** All artifact vectors came back clean and mutually corroborating: the "unknown" count is a 1:1 passthrough of raw GSC coverage_state, the property string matches the impression-bearing tables, 6 pages return genuine index verdicts with real crawl timestamps (a wrong property would fail all 112), and 0 of the impression-bearing URLs from either GSC table land in the unknown bucket. Honest boundary recorded: "not content quality/suppression" is airtight for the 92% never-crawled pages, but the head-term authority ceiling and the 1 crawled-not-indexed page are carried as separate root causes (#4), not denied.
- **Youth-vs-injury / phantom-canonical: DOWNGRADE.** Page canonicals confirmed clean and injury confirmed secondary, but "0 active residue / Jun 17 fix complete" was overclaimed: live llms.txt still points AI engines at the phantom NXDOMAIN. Changes: added MED-F12 to repair llms.txt; softened the recovery evidence to directional-not-significant (p ~ 0.07, confounded with maturation); narrowed "RESOLVED" to page canonicals only.
- **Lead attribution: HOLDS.** Primary SQL re-run independently; the ChatGPT reclassification of lead #6 and the 5 tracked human_confirmed leads are robust; 0 Bing sessions produced a lead. Changes: relabelled the 2 April leads from "genuine-direct" to "unattributable" (one could be Google-organic), and reframed Bing 0-leads as a landing/intent-mix artifact.
- **Bing-accessibility inference + discovery mechanism: HOLDS.** The rendering-block alternative was tested live and failed (Googlebot-UA no-JS fetch returns the links); robots/canonical/sitemap all clean; hreflang self-reference checked and valid. Changes: added the concrete `sitemap.ts` churning-lastmod defect (MED-F13, so MED-F1 does not under-deliver), qualified "Bing 38/112" as ">= 38 by Bing Webmaster impressions proxy" (raw bing_indexed is null for all 112), and noted the Bing-only IndexNow push Google never received.
- **Fix backlog completeness/safety/sequencing: DOWNGRADE.** Spine held (GSC-UI mechanics, content gate, guardrail and IndexNow tool all confirmed) but three defects fixed: added the missing MED-F12 (llms.txt); re-scoped MED-F4 IndexNow to the 15 genuinely-new URLs and dropped the 44 inert re-pings (down to severity low); re-targeted MED-F8 off the already-healthy dynamic llms-full.txt; and stopped MED-F5's blog-SSR change from co-shipping inside the discovery-measurement window.

### Honest success criteria

Baselines to beat: indexed 6/112; GSC ~55 impressions/day and 0.65 clicks/day (post window) with 0 lifetime head-term clicks and 0 Google clicks ever on the blog corpus; Bing ~706 impressions / 70 clicks (trailing) across at least 38 pages; 7 leads total (July run rate 4/month, AI-led).

If the diagnosis is right: at 14d the sitemap lastDownloaded refreshes, the submitted count moves 97 to 112, the 8 to 10 Request-Indexed core pages move unknown to crawled/indexed, indexed count rises 6 to roughly 15-20, and first impressions appear on newly-indexed core pages (leads too small to move, expect 0 to 2). At 28d a coverage re-sweep shows indexed count roughly 20-40+, impressions/day climbs past ~55 toward 80-100+, first non-zero Google clicks are plausible on long-tail blog, and "gp accountants" continues recovering below ~55 (leads 2 to 4, AI still leading). At 56d indexed count is a majority of 112 (over 60) with Google clicks establishing, while head-term positions likely stay deep (authority-gated, expected, not a failure).

What would mean the diagnosis is wrong: after a confirmed fresh sitemap download plus Request Indexing, those pages still sit unknown at 28d and indexed count stays <= 10 (then the binding constraint is a deeper trust/technical block or an authority wall pinging cannot move, not discovery mechanics); or Bing keeps climbing while Google flatlines through 56d despite every discovery lever (a Google-trust problem needing faceless off-site authority); or leads fall to 0 after any homepage/GEO change (an "amplification" breached the one proven converting surface, MED-F7). Conversely, if content added early lifted Google clicks while indexed count stayed flat, that falsifies "content is gated behind indexing."

## 4. Fix backlog (Phase 4/5)
| fix id | root cause | severity | effort | confidence | class (engine/content/deploy/gsc-ui/spend) | owner-gated | signal-watched | status | watch window |
|---|---|---|---|---|---|---|---|---|---|
| MED-F1 GSC sitemap re-submission (force a fresh download) | 1, 2 | blocker | S | H | gsc-ui (sitemaps.submit is scriptable but live-property, so owner-in-browser) | YES | lastDownloaded refresh + submitted count 97 to 112 | TODO | 7-14d. Pair with MED-F13 or it may under-deliver against churning lastmod |
| MED-F2 Request Indexing on core pages (/services, /about, /contact, /nhs-pension, /for-gps, /for-consultants, /blog + 2 crawl-lag posts) | 1 | blocker | S | H | gsc-ui — UI ONLY (requestIndexing not in API); ~10-12 URL/day quota, prioritise 8-10 highest-value | YES | requested URLs unknown to crawled/indexed; indexed 6 to 15-20 | TODO | 14d |
| MED-F3 Fix the 2 crawl-lag pages = Request Indexing only (NO source edit; B4 confirmed live canonicals clean) | 3 | med | S | H | gsc-ui | YES | folded into MED-F2 signal; locum SA page crawled-not-indexed to indexed | TODO | 14-28d |
| MED-F4 IndexNow submission of the 15 genuinely-new URLs ONLY (calculators/* + medical-guides/*); DROP the 44 unchanged-post re-pings (tool etiquette + inert, Bing already declined) | 2 (Bing side) | low | S | M | engine-code | no | Bing-only: pages-with-data > 38, trailing clicks > 70. Does NOTHING for the Google 92% | TODO | 28d |
| MED-F5 Make the blog index SSR all 73 posts (refactor BlogListWithSearch) | 1(D), 5 | high | M | M | site-code + deploy | YES | blog-post unknown count falls faster than core-page count | TODO | 28-56d. Do NOT co-ship inside the discovery-measurement window (one-var); ship AFTER discovery batch moves |
| MED-F6 Cross-link the 8 structural orphans (3 /calculators/*, 5 /locations/*) | 6 | low | S | M | content + site-code + deploy | YES | the 8 orphan URLs move unknown to crawled | TODO | 28-56d |
| MED-F7 Protect the converting surface (freeze homepage trust wall + health-check magnet + LeadForm + /contact CTAs; freeze the GP-partner post) | 3 (protect leads) | high | S | H | guardrail (no deploy; do-not-touch rule) | n/a | homepage stays the 4-lead surface; monthly leads not below the Apr-Jul run rate | STANDING | ongoing guardrail |
| MED-F8 GEO amplification test: add a decision-stage in-body /contact CTA to the 2 highest-traffic non-converting posts (gp-partner-vs-salaried, gp-partnership-tax-complete-guide). llms.txt repair SPLIT OUT to MED-F12 | protect/grow leads | high | M | M | content + site-code + deploy | YES | the 2 CTA-augmented posts produce their first lead; AI leads keep leading. Additive, must not touch homepage | TODO | 28-56d |
| MED-F9 Bing 96-sessions-0-leads DIAGNOSTIC (segment by entry page + intent + engagement) | investigate | med | S | H | engine-code (read-only query) | no | classifies low-intent (expected) vs a Bing-fed conversion-path defect | TODO | days |
| MED-F10 Serper credit top-up (unblocks live Google SERP composition probes) | diagnostic completeness | low | S | H | spend | YES | off critical path; diagnosis stands without it | TODO | optional |
| MED-F11 Deferred content wave (max ~5 posts): OPP-1 consultant Ltd cluster (3-4) + OPP-2 medico-legal income (1); fold OPP-3 into existing pension pages | 5 | low (gated) | L | M | content + deploy | YES | GATE not a signal: DO NOT START until index recovery is proven (73 posts move impressions to clicks) | BLOCKED (gated) | mid/late-July gate after coverage re-sweep |
| MED-F12 Repair public/llms.txt phantom domain (9 URLs + sitemap: medicalaccountantsuk.co.uk to medicalaccounts.co.uk) — the ONLY live file still carrying the phantom; AI-discovery index feeding the #1 lead channel (ChatGPT/AI, 3 of 7) | 3 | high | S | H | content/site-code + deploy | YES | AI-channel leads hold; the phantom NXDOMAIN is cleared from the AI index. Ship ALONE (own signal) | TODO | 28-56d |
| MED-F13 Pin sitemap.ts lastModified to a stable content/build date (stop new Date() churn on every static path/calculator/location/category/guide) | 1, 2 | med | S | H | site-code + deploy | YES | Google stops seeing meaningless per-build change; makes MED-F1 deliver | TODO | ship with MED-F1, 14-28d |

### Sequencing note (batch-safe vs ship-alone)

- **Discovery-repair batch (ship together, ONE shared signal = indexed count from a coverage re-sweep): MED-F1 + MED-F2 + MED-F3 + MED-F13.** Non-competing (all aim to wake Google on the same young domain). MED-F13 pins the sitemap lastmod so MED-F1's re-fetch is meaningful. Keep CONTENT and any homepage/blog-SSR change OUT of this window so the indexed-count signal stays clean.
- **Ship-alone (own signal): MED-F12** (llms.txt phantom repair, an AI-surface fix, not a Google-index fix; own AI-channel signal) and **MED-F11** (content wave, gated).
- **Crawlability-hardening (owner-gated site-code deploys, AFTER the discovery batch starts moving crawl budget): MED-F5 (blog SSR) + MED-F6 (orphans).** MED-F5 must NOT co-ship inside the discovery-measurement window (one-variable discipline) so the blog-post discovery signal is not confounded with the core-page signal.
- **Independent parallel lanes (different signals): MED-F4** (Bing-only), **MED-F7** (guardrail, no deploy), **MED-F8** (leads signal, additive, must not touch homepage), **MED-F9** (read-only diagnostic).
- **Optional / off critical path: MED-F10** (Serper spend).
- **One-variable discipline:** the homepage is the proven converting surface (MED-F7); no discovery or GEO experiment may modify it.

## 5. Decision log
- 2026-07-06: Post-ship COMPLETENESS AUDIT launched as Workflow `wf_b561da06-513` (owner asked "is everything truly done / seamless / at parity / max AI-optimised"): 5 adversarial lanes (live error sweep, UX journey walk, estate-parity check, AI/GEO vs full program spec, Google-readiness) → Opus verdict → `docs/medical/COMPLETENESS_AUDIT_2026-07-06.md`. Fix-now items execute under standing auto-mode go.
- 2026-07-06: P5 build LAUNCHED as Workflow run `wf_b31f0f0b-53f` (lanes: A sitemap-lastmod, B llms.txt, C blog-SSR, D orphan-links, E bing-diag→mileage→CTAs, F GEO/AI slice [Opus], G content wave brief→5 writers→Opus QA; then integration build+audits, then Opus diff review). If resuming cold: `git status` shows the working-tree changes; check integration/review status via lane docs + diff; deploy NOT yet done unless a deploy tag `deploy/medical/2026-07-*-diag` exists.
- 2026-07-06: **OWNER GO — auto mode** for the full backlog + site development + AI optimisation, medical-only, deploys pre-authorized. Owner explicitly overrode the MED-F5/F11 sequencing gates and the ship-alone discipline ("ship everything"); tradeoff recorded: the 14/28d discovery signal will carry a combined-change confound — mitigate by keeping a per-fix deploy manifest in this log. MED-F2/F3 Request Indexing remain OWNER-browser (API impossible). MED-F7 homepage/GP-post freeze REMAINS BINDING.
- 2026-07-06: Phase 5 additions beyond backlog (owner "extremities" mandate): GEO/AI-optimisation slice from `docs/_engines/AI_SEARCH_GEO_PROGRAM.md` §E (medical-scoped, homepage excluded), AMAP 45p→55p factual sweep (house_positions §8 + medical content), Bing-landing CTA extension (MED-F8 scope widened to top Bing entry posts per MED-F9 findings), weekly coverage-watch scheduled task (medical-only).
- 2026-07-06: P4 synthesis LAUNCHED as Workflow run `wf_637812ad-bdc` (draft → 5 skeptics → integrator writes §3+§4 + docs/medical/DIAGNOSIS_2026-07_SYNTHESIS.md). If resuming cold: check §3/§4 non-empty + SYNTHESIS doc exists; if partial, the draft is at `.cache/medical_diag/synthesis_draft.md`.
- 2026-07-06: P3 battery COMPLETE (all 7 lanes, findings in §2, committed 8e5a4d8b). Serper credits exhausted = OPEN operational item for owner.
- 2026-07-06: P3 battery LAUNCHED as Workflow run `wf_f017a22b-488` (script: `~/.claude/projects/C--Users-user-Documents-Accounting/dc4c4f94-5946-4513-b2a9-d1a229e2f592/workflows/scripts/medical-battery-wf_f017a22b-488.js`). B1/B2/B3→(B4∥B7) Sonnet, B5/B6 Opus. Ledger rows P3.b1–b7 = IN-PROGRESS (declared fan-out). If resuming cold: check which docs/medical/{google_read,bing_read,index_coverage,old_domain_forensics,lead_paradox,opportunity_read}.md exist + are fresh; re-run only missing lanes (tools are idempotent; invocation cmds in ledger rows).
- 2026-07-06: Program bootstrapped (Phase 0). Owner decisions locked: pause-at-synthesis; per-deploy gates; old domain never owned (phantom-canonical hypothesis primary).
- 2026-07-06: Declared fan-out: P1.a ∥ P1.b ∥ P1.d run in parallel (all Sonnet); P1.c serial after P1.b.
- 2026-07-06: Declared fan-out: P2.t1 ∥ P2.t2 ∥ P2.t3 (Sonnet builders) overlapped with P1.c (ingest running). Opus review will batch all 3 tools (P2.r1-r3 as one review agent).
- 2026-07-06 P1.b DONE: `optimisation_engine/snapshot/diagnostics.py:99` `_gsc_site_url_for['medical']` changed `sc-domain:medicalaccountants.co.uk` → `sc-domain:medicalaccounts.co.uk` (one line). Dead typo string now appears ONLY in .md docs (DIAGNOSIS/STATE/rewrite_program_state/solicitors STATE — historical notes, left intact). `medicalaccountantsuk.co.uk` has zero hits under optimisation_engine/. Uncommitted; commit with Phase 1 close-out.
