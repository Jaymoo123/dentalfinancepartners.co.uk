# Wave-1 + Wave-2 Independent QA Pass — 2026-07-18

(Original scope: wave-2. Owner extended same-day to wave-1 + blog↔tool wiring; see the
"Wave-1 QA addendum" section at the bottom.)

Fresh-session end-to-end verification of the wave-2 conversion-stack build (medical,
construction-cis, contractors-ir35) against the Property baseline, before any owner-gated
go-live. Method: two independent inventory agents (Property baseline + 3-site claimed-vs-found),
mechanical verification (builds, full test suites, band-math audit), citability cross-check
(llms.txt vs registries, JSON-LD, sitemaps) and a functional prod-server smoke walk per site.

## Verdict

Build is now genuinely at claimed parity, but the QA pass found and fixed **three real defects**
the session-6 build had missed. All fixed, all suites green, all smoke walks pass.

## Defects found and fixed

### 1. Medical public tool fleet was 5/10 (claimed 10) — SEV: high
Session-6 built the 7 roster tools but registered tools 4-7 and 10 (nhs-superannuation-tiered-
contribution, nhs-pension-scheme-pays, gp-partner-drawings-planner, salaried-gp-vs-partner,
consultant-private-vs-nhs) in the PREMIUM registry only — blog-embedded, non-indexable. The
roster (`docs/medical/TOOL_ROSTER.md`) unambiguously specifies them as public.
**Fix:** 5 new public GenericTool configs + pure compute modules (reusing `nhs-super-tiers.ts`
single-source tiers) + golden tests, wired into `medical/web/src/lib/tools/registry.ts`.
Premium siblings retained. Built sitemap now emits all 10 `/calculators/<slug>` URLs (verified).

### 2. construction-cis income-tax band bug NOT fixed (claim said "fixed in 5 libs") — SEV: high
`cis-tax.ts saLiability()` had no PA taper above £100k and no 45% additional rate; three public
tools (cis-vs-paye-comparison, cis-self-assessment-calculator, cis-refund-estimator) inlined the
same flat math. The two NEW wave-2 tools had each inlined a *correct* taper as a workaround
instead of fixing the shared helper. Income tax was understated by £2,000+ for £110k-income cases.
**Fix:** correct `incomeTaxOn()` + `taperedPersonalAllowance()` added to `cis-tax.ts`;
`saLiability` and all three flat tools now route through it; one stale golden (pinned to the
buggy value, £18,289 → correct £20,289) re-derived and updated; new band-edge goldens added
(£60k/£120k/£150k). Stale "basic 20%, higher 40%" note copy updated to mention taper + 45%.
contractors-ir35 `tax2026.ts:109` was verified CORRECT as claimed. Property `landlordTax.ts`
remains FROZEN per owner instruction (not touched).

### 3. Medical `/tools/` links were 404s — SEV: medium
All related-links in medical tool configs (including the pre-existing salaried-doctor-take-home)
pointed to `/tools/<slug>`; the real route is `/calculators/<slug>` and no redirect exists.
Thank-you page linked `/tools` hub twice. **Fix:** all hrefs rewritten to `/calculators/`.

## Citability gaps fixed
- medical `llms.txt`: added the 5 new public tools (was 5/10 listed).
- contractors-ir35 `llms.txt`: added missing umbrella-take-home-calculator (was 9/10).
- Property (16/16) and construction-cis (12/12) llms.txt verified complete.
- WebApplication JSON-LD verified wired on tool pages on ALL 4 sites.
- Sitemaps verified registry-driven on all 4 sites (new tools auto-included).

## Verification results (post-fix)
| Site | Build | Tests | Smoke walk |
|---|---|---|---|
| medical | green (153 pages) | 379+ pass (incl. new goldens) | 9/9 PASS |
| construction-cis | green | 352 pass | 13/13 PASS |
| contractors-ir35 | green | 380 pass | 10/10 PASS |

Smoke walks (prod `next start` + curl): home, calculators hub listing every tool, every new tool
page 200 with WebApplication JSON-LD and no NaN/undefined, llms.txt complete, blog post renders
form/tool island, /resources/[topic], /complete, /api/leads/submit rejects GET (405).

## Verified-OK claims (no action needed)
- A1 shared multistep MiniCapture wrappers on all 3 sites; `NEXT_PUBLIC_MINIFORMS_MULTISTEP` flag staged.
- A2 blog 3-moment (early tool island, mid qualified form, fallback) on all 3.
- A3 nurture ported, dormant/dry-run, per-site site_key scoping; migration 20260718000002 staged (NOT applied — owner-gated).
- Booking + progressive-completion routes on all 3.
- `?qa=1` test-lead flagging present.
- ir35 tool roster 10/10, cis 12/12 as claimed.

## Still open (owner-gated / follow-up)
- Property `landlordTax.ts` band bug — FROZEN, awaiting owner word.
- Opus security-QA verdict on medical/cis booking routes — pending; fold in as one-off commit if it surfaces anything.
- Deploys, migration apply, nurture secrets/arming, MINIFORMS flips, IndexNow drain — owner-gated (unchanged).
- Fresh calculator gap-discovery sweep (beyond the 2026-07 SERP-justified rosters) — optional separate research task.
- Medical premium siblings for tools 4-7/10 currently host the base compute rather than the enhanced variants the roster sketches (multi-year projections etc.) — nice-to-have, not blocking.

---

## Wave-1 QA addendum (same day, second pass)

Same method applied to generalist / Dentists / Solicitors, plus a 6-site blog↔tool wiring
audit (do new calculators actually surface on the blog pillars they belong to?).

### Wave-1 parity verdict: PASS (session-6 claims held up)
- Income-tax band fix VERIFIED genuinely correct in all wave-1 compute libs (PA taper +
  45% additional + correct higher-band width). No wave-1 equivalent of the cis miss.
- Tool fleets: generalist 17/17 public per roster; dentists 13 public + 6 premium (roster
  math reconciles); solicitors 13 public + equity-partner-buy-in special-routed at
  /tools/equity-partner-buy-in (works, JSON-LD present, in llms.txt).
- No /tools/-vs-/calculators/ link rot on any wave-1 site.
- llms.txt complete on all 3; JSON-LD wired; sitemaps registry-driven.
- ExitIntentModal retired + DeepScrollModal + SpecialistWidget mounted on all 3;
  resources de-gated/indexable; blog 3-moment architecture live; multistep staged;
  nurture + booking dormant-complete.

### Blog↔tool wiring findings (the real gaps) — ALL FIXED
The 3-moment blog architecture and personalization layer resolve tools via per-site
intent taxonomies (CALC_SLUG_TO_TOPIC reverse map, topic primaryCalculator, and the
topic→premium-tool resource spine). New tools had NOT been wired in everywhere:

| Site | Gap found | Fix |
|---|---|---|
| Medical | 5 new tools absent from CALC_SLUG_TO_TOPIC; gp-practice topic had primaryCalculator: null AND resource toolId: null, so ~20 gp-practice-management posts rendered NO tool island | Mapped all 5; gp-practice primary → gp-partner-drawings-planner; resource toolId → gp-partner-drawings-planner premium island |
| Dentists | 8 of 13 tools absent from CALC_SLUG_TO_TOPIC; selling topic still pushed practice-valuation instead of the purpose-built practice-sale-cgt; compliance topic had no calculator | Mapped all 8; selling primary → practice-sale-cgt; compliance primary → dental-tax-deductions (CTA copy updated) |
| Generalist | 10 new tools absent from CALC_SLUG_TO_TOPIC; compliance topic had no calculator; the test guard meant to catch this had a stale inlined slug list (only the original 7) so it could never fire | Mapped all 10; compliance primary → mtd-itsa-readiness; test guard list extended to 17 |
| Solicitors | Reverse map complete (13/13). /tools/equity-partner-buy-in missing from sitemap | Added to sitemap staticPaths |
| cis / ir35 | Fully wired at all three levels (registry, topic map, EARLY_TOOL_BY_CATEGORY) | none needed |

Tests pinned to the old "topic has no calculator" behavior (2 dentists, 2 generalist,
3 medical) were updated to assert the new tool offers. Post-fix: all 6 suites green
(generalist 243, dentists 410, solicitors 158, medical 379, cis 352, ir35 380), all
builds green.

### Remaining non-blocking notes
- Generalist orphan tools (mileage-claim, capital-allowances-vehicle,
  cis-subcontractor-deduction) have no matching blog category to host an early island —
  documented as deliberate in taxonomy; now at least reverse-mapped for personalization.
- Dentists compliance topic has no PREMIUM sibling (public dental-tax-deductions only) —
  blog premium island stays empty for that topic by design.
- Solicitors equity-partner-buy-in remains special-routed (not in the calculators
  gallery/registry); functional, cited, now in sitemap. Fold into registry only if the
  gallery should list it.
