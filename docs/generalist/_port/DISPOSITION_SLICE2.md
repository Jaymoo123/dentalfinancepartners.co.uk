# PORT BLUEPRINT — SLICE 2: CALCULATORS, PILLARS, LOCATIONS

Sources read directly 2026-09-09: rollout appendix A-J, DESIGN_SYSTEM §0, Property and generalist source, `packages/web-shared/{design,tools,components}`.

## 0. Findings that change the shape of the work

1. **The standard kit is already extracted** (`packages/web-shared/design/`: primitives, marketing, blog, chrome, globals-standard.css). Generalist has none of it locally. This slice is overwhelmingly consume-from-kit; the only genuinely local builds are the motif backdrop, `CalculatorTabs`, and the ResultGate port.
2. **`employer-ni-calculator` exists TWICE** — registry generic (`lib/tools/configs/employer-ni-calculator.ts`) AND bespoke route + component. The static route shadows the registry copy at `/calculators/...` but the registry copy is LIVE at `/embed/employer-ni-calculator`. §G: "never a second copy". Live standard breach today. GATE G1 decides which survives (delete the config and keep the bespoke multi-employee UI, or delete the route).
3. **Generalist's `CalculatorPageResources` ships two things Property explicitly retired**: the "Go deeper" orange pill strip (:41-48) and a second `GateOrForm` capture (:50-56). Property's file documents both removed — one form per page. Both RETIRE.
4. **Registry thin vs the §G contract**: 0 of 21 tools carry `workedExamples` (GEO field; G2 = 21 content commissions); 8 of 21 carry no `related[]` (G3 — section renders nothing on those; NEVER substitute a calculator link-card, §0.4).
5. **The warning-ramp collision is load-bearing here**: the locked penalty ramp contains orange-700, which is now the brand ramp. Blocks all 22 result tones (`headline.tone`/`verdict.tone`), MTD/VAT in-scope states, R&D non-qualifying rows, until the DESIGN_DELTA ramp lands (G0).
6. **`sectionY` is wrong at the token level** (`py-16 sm:py-20 lg:py-28` vs canonical `py-12 sm:py-16 lg:py-20`); fixed in the tokens commit, pages write the standard values literally.
7. **Every page in this slice violates the body-clamp ban** (`max-w-4xl mx-auto` on both calc routes, near-me, r-and-d x4, locations [slug] x5; `max-w-6xl mx-auto` on /calculators, /locations, r-and-d x2). All removed; `max-w-3xl` survives only on hero copy and standfirsts.
8. **Result capture is the funnel finding**: Property gates every non-embed result (180 result_gate CTA clicks since 08-23); generalist gates nothing on calculator pages (CalcResultCta MiniCapture sits below an already-visible result; bespoke employer-NI has zero capture). Detail in §A.

## 1. /calculators/[slug] (21 pages, 144 lines)

New order: `buildCalculatorJsonLd` + `buildFaqPageJsonLd` (rename from buildWebApplication/buildFaqPage so the FAQ array is provably the FaqSection array) · navy MOTIF hero `py-12 sm:py-16` (motif net-new; flat slab retired) · kit `Breadcrumb onDark` (emits BreadcrumbList, currently omitted) · `Eyebrow onDark` "Free calculator · 2026/27 rates" replacing the orange pill · h1 conforming (no-op, A.3 calculator scale) · standfirst `text-base sm:text-lg text-slate-300` · NET-NEW hero CTA `${btnOnDark}` -> `#get-expert-help` ("Ask an accountant about your figure") · tool on `bg-slate-50` UNCLAMPED, shared web-shared/tools renderer KEPT (§4.6.3; generalist is ahead of Property on shared consumption), wrapped in `ResultGate` (§A) · `CalculatorPageResources` reduced to the premium island only; below `sm` the slot becomes a link to `#get-expert-help`, never a second form · white explainer at FULL container, `h2 text-2xl sm:text-4xl` + Prose replacing the `border-l-4` card (that recipe is reserved for the tool body) · workedExamples with `ExampleFigureNote` (G2) · `RelatedArticles` from `tool.related` on slate-50 (13 of 21 have data) · `#get-expert-help` LeadCTAPanel navy `redirectOnSuccess={false}` (proof points: "Fixed fees, quoted upfront" / "One named accountant" / "24-hour reply") replacing the navy bare-button /contact box (:122-138) · kit FaqSection replacing the hand-rolled `<dl>` (:108-120).

Wiring: `calc_hero_help` (hero, form) · `calc_result_<slug>` MOVED ONTO the gate reveal, id preserved so the vw_cta_performance series does not fork · `calc_confirm_figure` (result_shown) on the revealed state. Zero /contact links in the body. JSON-LD: SoftwareApplication + FAQPage (same array) + BreadcrumbList.
Rules honoured: gate on this route only; inputs stay live; embeds `enabled={false}`; skip always reveals; reveal persisted per campaign=slug; no CalculatorTabs on the tool's own page.

## 2. /calculators/employer-ni-calculator (bespoke, 133 lines)

Same order as §1, deltas: `EmployerNICalculator` KEPT (multi-employee payroll rows are payload the generic model cannot express) but restyled to the shared renderer's shape — host `bg-white border-l-4 border-[var(--brand-primary)] p-6 sm:p-8 lg:p-10`, `grid gap-6 sm:gap-8 lg:grid-cols-[1fr_1.2fr]`, fields left on NumberInput, NAVY result panel right `bg-slate-900 p-6 sm:p-8 text-white` with mono headline one step below the shared scale, rows over `border-t border-slate-700`, fires `calc_view/calc_input_change/calc_computed/calc_result_viewed` — wrapped in `ResultGate campaign="employer-ni-calculator"` (NET-NEW capture on a zero-capture page; sign-off list) · worked example at :97-99 kept + ExampleFigureNote (the model for G2) · related list hand-authored (G4) · closing copy keeps "who first, how the cash flow handles it, when the Employment Allowance gets used up" but STRIKES "We model that with limited company directors monthly" (client-volume claim, G5) · CalculatorPageResources keeps its slug, loses Go-deeper + GateOrForm.

## 3. /calculators index (78 lines)

Two-tier per F.5: navy motif hero `min-h-[300px] sm:min-h-[350px]` · Tier 1 white "Run one now" head block + `CalculatorTabs tabs={INDEX_HEADLINE_TABS}` + LITERAL crawl link naming a specific slug (the guard is a source scan; a link to /calculators alone does NOT satisfy it) · Tier 2 slate-50 categorised directory (`rounded-xl border-2 border-slate-200 bg-white p-3 sm:p-4 hover:border-primary-400` cards, `h-9 w-9 sm:h-11 sm:w-11 rounded-xl` icon badges) replacing the flat 21-card grid with orange-gradient tiles — 15 categories exist across 21 tools with three overlapping pairs (Capital Gains / Exit and Capital Gains; Employment / Employment Taxes; Business Tax / Corporation Tax): merge to ~7 + `CATEGORY_ORDER` (G6) · net-new tail link to `/embed` ("Run a business site? Embed any of these calculators for free") — the embed gallery is orphaned today · net-new `#get-expert-help` LeadCTAPanel CONTAINED (`bg-slate-100 rounded-xl p-6 sm:p-10 lg:p-14 ring-1 ring-slate-200`; no FAQ after it, full-bleed navy would touch the navy footer). The index has zero ask today.
Standfirst keeps "No email gate, no sign-up" — WHICH CONSTRAINS THE GATE (G22, §A note 4). JSON-LD: BreadcrumbList + WebPage; no invented ItemList. `home_calculators_all` is the homepage's id and is NOT reused here.

## 4. /embed + /embed/[slug]

Structurally unchanged: embeds NEVER gated (`enabled={false}` derives from the existing `variant === "embed"` switch — reuse it, no second flag), `robots index:false` kept, out of IndexNow. Gallery restyled to standard scale + `rounded-xl ring-1`; chrome-free via the kit PageShell bypass. G7: 21 live iframes mounted at once on the gallery — lazy-load or snippet-only.

## 5. /services (309 lines, cream idiom)

Cream `TopicHero` `min-h-[360px] sm:min-h-[420px] lg:min-h-[440px]` + cream motif + net-new Breadcrumb, h1 to A.3 pillar scale `text-2xl sm:text-4xl lg:text-6xl font-bold` (7xl `font-semibold tracking-tight` retired), "The full annual cycle." + orange span kept · hero primary -> `#book` (was /contact), secondary `${btnOnCream}` -> /calculators · white StatsCounter strip `py-5 sm:py-7` directly under the hero replacing the shared StatsBar mid-page (StatsBar is a generalist CALL-SITE change; the shared component stays alive for its 14 family-D consumers; G8: audit `siteStats` for client-count claims first) · `ServiceTiers` KEPT on slate-50 (DERIVED, not assumed: live shared kit imported by 15+ family-D services pages; Property never adopting it is not a retirement) with the hard constraint NO PRICES (G9: audit `config/service-tiers.ts`) · the 8 service lines become `CoverageCards columns={2}` on white (numbered mono-rule rows are the pre-redesign template; 8 equivalent things = a grid), all copy verbatim, `dangerouslySetInnerHTML`-for-&rsquo; replaced with real characters · "What's included" moves off the `bg-neutral-900` band onto slate-50 `DrawnTickList` (`tickClassName` at 600; fixes mid-page dark adjacency), all four items verbatim · `ProcessTimeline` net-new (G10: authored copy) · `#book` LeadCTAPanel navy replacing the cream bare-LeadForm section, `redirectOnSuccess` TRUE (false is only for calculator/blog surfaces), "A short call. A clear quote. No follow-up sequence." kept as title/description · FaqSection net-new (G11) — also keeps the navy panel off the navy footer.
RETIRED: neutral-* palette on this page, font-semibold/tracking-tight, mono eyebrows, border-t section dividers, the browse-first paragraph (its two links survive as body-section links; link floor).
JSON-LD: Service (+OfferCatalog from the 8 titles, no pricing) + BreadcrumbList + FAQPage.

## 6. /accountant-near-me (137 lines)

Navy motif hero `min-h-[300px] sm:min-h-[350px]` + kit Breadcrumb + Eyebrow ("UK-wide coverage") replacing the pill · trust chips -> `DrawnTickList` on dark, claims unchanged · NET-NEW hero CTA -> `#book` · the two-paragraph prose section gains a `ProcessTimeline` figure (remote engagement: onboarding, shared ledger, quarterly, year end; §0.2 no prose-only sections), clamp removed, typography pass (raw apostrophes) · city directory unclamped, chips to `rounded-xl border-2 min-h-[44px]` (current ~34px fails the touch floor), ALL ~193 links stay rendered (this page is the link hub for the locations tier) · `#book` LeadCTAPanel replacing the orange-gradient band holding a bare LeadForm (the exact invisible-label failure class — re-verify rendered ground) with the "Don't see your town?" copy kept and the Mail/Contact-us block dropped · FaqSection net-new (G12; strong GEO surface: "do I need a local accountant" / "do you meet in person" / "which software").
Zero schema today -> AccountingService + BreadcrumbList + FAQPage added. G13: near-identical city directory to /locations (cannibalisation) — differentiate on the remote-first argument and link to /locations for the full list, or accept the overlap.

## 7. /r-and-d-credits (304 lines; highest §I copy risk in the slice)

Cream TopicHero + cream motif REPLACING the Unsplash photo hero + brightness/scrim stack (also an LCP win); h1 orange-400 span -> primary-700 (orange-400 on cream ~2:1 fails) · standfirst: "We've processed claims from £15k to over £150k" is a banned client/volume claim -> rewrite to the claim-range-we-prepare pattern (G14) · hero CTAs -> `#book` + the estimator, standard A.5 button box (per-call-site size overrides retired) · stats move off the `bg-orange-700` band (a full-bleed brand band is decoration, not meaning; orange-200-on-orange-700 label contrast risk) onto a white StatsCounter strip with ExampleFigureNote; every claim-range figure must be re-derivable from house_positions.md or it does not publish (G15) · qualifying-activities grid -> `CoverageCards columns={3}` unclamped, gradient icon tiles retired, "Average claim: £40k-£150k+" struck (G14) · the 4 process steps -> `ProcessTimeline` (a grid contradicts a sequence) · calculator promo -> the D.3 mid-page pattern (statement left, `${btnSecondary}` right, literal `<a href="/calculators/rd-tax-credit-estimator">`), RECOMMENDED upgrade to a live `CalculatorTabs tabs={["rdcredit"]}` block (G16: behaviour change on a pillar) · `#assessment` renamed `#book` (hero link moves with it), the orange-50 bare-form box -> LeadCTAPanel navy, "clear yes/no and a directional estimate of claim size" kept as description, message-field guidance kept as footnote · FaqSection + same-array JSON-LD; "Most clients prefer the fixed fee" struck (G17) · closing navy one-button band -> `RelatedArticles` from the R&D category + hub link (fixes navy-on-navy + raises the link floor).

## 8. /locations index (172 lines)

Navy motif hero + NET-NEW `#book` CTA · CityCard photo tiles DELETED (zero photography): major/regional cities become text cards `rounded-xl bg-slate-50 p-6 sm:p-8 ring-1 ring-slate-200/70 hover:border-primary-600` keeping region + `businessHubs.slice(0,3).join(" · ")` (the payload that makes the card worth reading without the photo) · the remaining ~163 as 44px chips, ALL rendered, none hidden · group headings become Eyebrow + h2 section heads with explicit ground oscillation (white / slate-50 / white) replacing the ruled-underline h2 · NET-NEW closing `LeadCTAPanel contained` (Property's own recorded rationale: someone whose town has no page still converts instead of bouncing off a list that excludes them). G18: confirm no Pexels attribution duty survives image removal. JSON-LD added: BreadcrumbList + WebPage.

## 9. /locations/[slug] (~193 pages, 339 lines)

Template: navy motif hero (photo + scrim + Pexels credit deleted — also removes a `priority` LCP image on 193 pages; region -> `Eyebrow onDark`; h1 orange span dropped; `DrawnTickList` trust row; NET-NEW hero CTA "Speak to an accountant in {city}" -> `#book`) · white unclamped whyHere + businessScene + business-hubs `NoticeCard` slate (border-l-4 retired) · slate-50 sector pills (`rounded-full bg-white ring-1 ring-slate-200 text-xs font-semibold text-slate-600`; orange-on-orange retired) + sectorEmphasis + anchor-employers meta row over `border-t` · white `CoverageCards columns={2}` for the six services — the inline array LIFTED to a module constant shared with /services so the two cannot drift · slate-50 anonymised case-study white card (`Eyebrow "{city} case study (anonymised)"`; decorative Quote icon retired; G19: audit all 193 bodies against §I) · white mid-page CTA card -> `#book` net-new ("What it costs depends on the size and structure of the business, so we will talk that through rather than quote blind") · FaqSection + same-array JSON-LD replacing the `<dl>` · adjacent-town cross-link KEPT VERBATIM (deliberate one-per-page link asset; never drop) · `RelatedArticles columns={3}` net-new: city-matched posts then a fallback hub (G20: which hub), slice(0,5) · `#book` `LeadCTAPanel contained`, `redirectOnSuccess` flips false -> TRUE (G21: measurable funnel change x193; false currently costs the booking-picker step).
Schema: the enrichment KEPT wholesale (serviceType[], knowsAbout: localSectors, areaServed + nearbyAreas, geo — better than Property's) but `priceRange: "£££"` STRUCK (pricing signal on a no-pricing site) and no telephone.
Worked example (Manchester) verified: 10 sections, payload dropped = ZERO; payload restyled = chips, employers, case study, FAQs, adjacent link, hubs card; payload added = mid-page CTA, RelatedArticles, hero CTA.

## A. RESULT-GATE PORT (capture-surface change — owner sign-off)

Property behaviour, ported verbatim with the orange ramp: the REAL result behind `blur-[10px] saturate-125 scale-[1.03]` in the navy frame, `inert` + `aria-hidden` held state, glass scrim, `max-w-sm rounded-2xl bg-slate-900/90` prompt card (sanctioned rounded-2xl overlay), reveal fires `cta_click{calc_result_<slug>, result_gate}` and opens the modal; EVERY dismiss path reveals; reveal persisted per campaign; converted visitors bypass; embeds `enabled={false}`; revealed state appends `${btnSecondary} mt-4 w-full` "Get this figure checked by an accountant" (`calc_confirm_figure`).

Per-tool: 21 generic tools gated (CalcResultCta RETIRED — an always-visible capture below a satisfied reader converts at ~4% of the gate's rate: 180 vs 7 clicks); embeds unchanged; bespoke employer-NI gated net-new (`ground="navy"`); the blog premium island's module-global once-per-session flag RE-KEYED to per-campaign `wasRevealed/rememberRevealed` (the module-global IS Property's documented "unlock one, unlock all" bug).

Mechanics: (1) the gate must wrap the RESULT COLUMN, not the whole tool — the shared renderer's `resultCta` slot sits outside the grid; add an ADDITIVE `resultWrapper?: (node) => ReactNode` prop to `web-shared/tools/components/Calculator.tsx` (recommended over forking; shared-package change touching 5+ sites' builds — closure-check + sign-off). (2) The revealed CTA lives inside the same div as the result (Property-documented layout trap). (3) `HeldResult ground="navy"` everywhere in this slice.

**G22 (highest-risk item): `/calculators` promises "No email gate, no sign-up"; a modal collecting an email makes that read false. Either the copy changes ("no sign-up, and you can always skip straight to the number") or the gate does not ship. Owner decides.**

Full capture sign-off list: ResultGate x21 + employer-NI net-new · CalcResultCta removed x21 · GateOrForm removed from the calc island · premium gate re-keyed · shared renderer `resultWrapper` prop · `redirectOnSuccess` flips (193 locations + /services).

## B. CALCULATOR-TABS ADOPTION MAP

Built LOCAL on Property's `CalculatorTabs` shape: WAI-ARIA tablist, roving tabindex, Arrow/Home/End, `#hash` deep link, `next/dynamic` panels, literal `TABLIST_COLUMNS` map (Tailwind scans literals), tab `rounded-xl border-2 p-3 sm:p-4`, selected `border-primary-600 bg-primary-600 shadow-md`, icon badge `h-9 w-9 sm:h-11 sm:w-11 rounded-xl`, panels `mt-6 sm:mt-8`. Almost every tab renders `CalculatorClient` by slug — never a second copy of a tool.

TabKey union (9): salarydividend | takehome | employerni | soletradervsltd | vatthreshold | rdcredit | dividendtax | pension | badr.
HOME_TABS (4): salarydividend, takehome, employerni, soletradervsltd. Homepage literal link: `/calculators/salary-dividend-optimiser`.
INDEX_HEADLINE_TABS (5, ordered by the owner's year; 5 = bundle ceiling, every panel mounts): soletradervsltd, salarydividend, employerni, vatthreshold, badr. Index literal link names a specific slug.
`employerni` renders the bespoke component or the registry slug per G1. /r-and-d-credits renders `tabs={["rdcredit"]}` if G16 approved. Pillar/location pages carry no tabs (not about a calculation) and a plain nav link only.
Full registry (21): salary-dividend-optimiser, take-home-pay-calculator, dividend-tax-2026-27, pension-contribution-optimiser, mileage-claim, employer-ni-calculator, associated-companies-ct, rd-tax-credit-estimator, capital-allowances-vehicle, vat-scheme-comparator, vat-threshold-checker, badr-cgt-calculator, cgt-60-day-reporter, sole-trader-vs-ltd, cis-subcontractor-deduction, p11d-bik-calculator, mtd-itsa-readiness, business-loan-calculator, asset-finance-calculator, eot-tax-saving-calculator, business-valuation-calculator.

## C. KIT-VS-LOCAL (slice 2)

ADOPT KIT: layout-utils (retires divergent local `sectionY` + `btnOnOrange` — its only consumers are the deleted gradient bands), globals-standard.css, page-blocks, FaqSection, Breadcrumb, ExampleFigureNote, NoticeCard, SlimHero, NumberedPagination, accordion, LeadCTAPanel (CTASection retires), all marketing components, RelatedArticles, StickyCTA (chrome slice owns the mount).
KEEP: `ServiceTiers` (live family-D shared kit) · shared tools renderer + ONE additive `resultWrapper` prop (§4.6.3 keeps shared consumption).
BUILD LOCAL: CalculatorTabs, GeneralistBackdrop motif, ResultGate/HeldResult/resultGateStorage (port Property's three files, orange ramp), lib/page-summaries.ts, lib/faq-page-schema.ts.
RETIRE: CalcResultCta, GateOrForm-in-calc-island (file survives for the blog renderer, as Property does). DELETE `ResourceGate.tsx` (dead since 2026-07-17; carve-out-2 class — leaving it on disk is how it gets resurrected).
KEEP-RESTYLE: premium fleet (PremiumCalculator/PremiumUpgrade/MiniGrid/MobileToolSlot: tokens + radius + gate re-key only).

## D. SLICE 2 OWNER GATES (G0-G22)

G0 warning ramp (blocks all result tones) · G1 employer-NI duplicate · G2 21 workedExamples commissions · G3 8 empty related[] · G4 employer-NI related list · G5 "model that monthly" claim · G6 category merge to ~7 · G7 embed gallery 21 live iframes · G8 siteStats claims audit · G9 service-tiers pricing audit · G10 services process-timeline copy · G11 services FAQ · G12 near-me FAQ · G13 near-me vs locations overlap · G14 R&D client/volume claims · G15 R&D figures re-derivable · G16 R&D live tabs · G17 "most clients prefer" · G18 Pexels attribution · G19 193 case-study §I audit · G20 locations fallback hub · G21 redirectOnSuccess flips · G22 "no email gate" promise vs ResultGate.

Standing verifications (not gates): LeadForm rendered-ground check on every capture surface (three current orange-ground instances: accountant-near-me:128, locations/[slug]:330, r-and-d-credits:255); per-page link-floor baseline BEFORE the port (~200 pages; the clamp removals and RelatedArticles additions raise it, the /contact-link removals lower it, and the floor is binding per page).
