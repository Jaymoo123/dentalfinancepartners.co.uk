# Generalist Wave 3 GEO — keyTakeaways backfill (2026-06-30)

**Status: COMPLETE, local, build green, NOT deployed.** Part of the Property-standard parity programme (`PARITY_PROGRAMME_HANDOVER.md` §4a/§4c/§6/§8).

## What

Added `keyTakeaways` answer-box content (and `howToSteps` HowTo schema on procedural posts) to the **audit-clean** generalist blog posts, satisfying the Wave 3 gate ("never amplify an unverified body into the answer-box" — cleared once the full factual-accuracy remediation completed).

## Scope (triple-verified clean list)

- **74 audit-clean posts** = 356 audited − 282 error/fixed (`generalist_audit_files.json` − `.cache/audit_fix/`). Reconstruction cross-checked 3 ways (part-1 42 + part-2 32; `.cache/audit_part2/*.json` clean flags; zero contradictions) and matches an independent agent reconstruction exactly.
- **5 of the 74 already carried pilot `keyTakeaways`** (`change-company-name-companies-house`, `confirmation-statement-late-penalty-companies-house`, `dormant-company-filing-requirements-uk`, `hmrc-reclaim-tax`, `quickbooks-vs-xero-cloud-accounting-comparison`) — clean bodies, left as-is.
- **69 posts authored this run** (list: `.cache/_wave3_eligible.json`). Result: **74/74 audit-clean posts now have answer-boxes** (plus the 5 track-2 rewrites that already had them).

## Method (Workflow `wf_cd61bf16-546`)

2-stage pipeline, 69 items, no barrier (QA on early posts runs while later posts author):
1. **Author (Sonnet):** read each post, derive 4-6 self-contained, body-grounded keyTakeaways (figures/dates/rates only as stated in the body, no outside facts), plus `howToSteps` {name,text} where genuinely procedural.
2. **Verify (Opus, adversarial):** re-read the body, check each takeaway for body-support, exact figure fidelity, no outside facts, and style (no em-dash, UK English, ≤30 words, no firm credential claim); rewrite or drop; return final approved set.

Workers returned structured JSON only. Applied **deterministically** via `.cache/_wave3_apply.py` (HTML-entity-unescaped, double-quoted YAML inserted before the closing `---`, PyYAML-validated) — single code path, avoiding per-agent YAML errors.

## Outcome

- **69/69 approved** (100%), **411 keyTakeaways** (66 posts × 6, 3 × 5), **9 posts** also got `howToSteps`.
- Applied: 69 files, 0 frontmatter/YAML failures; exactly the 69 eligible files changed, additions only.

## Verification

- `npm run build` green; `npm test` 33/33.
- Prerendered HTML confirmed: `id="answer-box"` + "Key takeaways" + takeaway text + `SpeakableSpecification` (answer-box → AI answers); `"@type":"HowTo"` emits from howToSteps.
- Integrity: 450 proper UTF-8 `£` bytes, 0 replacement chars; 0 HTML entities; 0 em-dashes/en-dashes; 0 over-length keyTakeaways.
- Credential-safe: the only ICAEW/ACCA mentions are legitimate reader-education ("check your accountant is registered with ICAEW/ACCA/AAT"), not firm self-claims. No self-claim reintroduced.
- Adversarial date-banding spot-check (dividend 2025/26, CGT, AIA 2024/25, associated-companies, mileage): every figure correctly year-scoped; mileage correctly shows 55p from 6 Apr 2026; no stale rate presented as current.

## Coverage gap / next-step option

The **282 fixed posts** were excluded by the original "clean = never-had-errors" gate, but they are now factually corrected to ground truth. Their bodies are trustworthy, and the Opus QA stage independently re-checks figure fidelity against the body, so a **Wave 3b** could extend keyTakeaways to those 282 (roughly 4× the answer-box coverage). The fixed posts had lighter verification than the clean ones (Sonnet fix + grep/build, no per-post Opus QA), so the QA stage is the safety net. Owner decision.

## Artifacts

- Eligible list: `.cache/_wave3_eligible.json` (69) · clean list reconstruction: `.cache/_wave3_cleanlist.py`
- Approved output (cleaned): `.cache/_wave3_approved.json`
- Applier: `.cache/_wave3_apply.py`
- Workflow script: `…/workflows/scripts/generalist-wave3-geo-keytakeaways-wf_cd61bf16-546.js` (run `wf_cd61bf16-546`)
