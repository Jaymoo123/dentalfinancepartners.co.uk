# Property pickup: agents track + Wave 12 (written 2026-08-21, owner-requested)

Fresh-session handoff for the next two builds. Read `CLAUDE.md`, load the
`standard_terms` skill, then this file, then the referenced sections. The prior
pickup doc (`HANDOFF_2026-08-20.md`) is superseded for sequencing; its writer/QA
method sections remain valid and are referenced below rather than repeated.

## 0. Where the estate stands (evening 2026-08-21)

- TWO owner-triggered deploys today, both clean: (1) wave11+cgt1+tools+rental1
  consolidated (STATE §0.18); (2) rural/landed-estates `landed1` (STATE §0.19).
  Everything committed and pushed through `631d71ef`+, CI green.
- Corpus 767 posts. monitored_pages carries ~47 armed rows from today alone
  (watch until 2026-11-19) plus the SDLT experiment set (28d Bing read ~09-15).
- **THE BINDING CONSTRAINT: measurement windows are armed everywhere.** Every
  page registered today is one-change-per-window frozen (REWRITE_PROGRAM §9.3).
  Any new build MUST run its dossier freeze check against monitored_pages and
  bucket keywords to frozen pages as `already-covered`/`deferred`, never edit them.
- DataForSEO balance ~$4.11 (top-up before any harvest bigger than ~$1).
  Serper OUT OF CREDITS: no live SERP checks, state it as a limitation.
- Reads calendar: SDLT calculator/batch ~09-01 (14d Bing) and ~09-15 (28d);
  today's batches 14d ~09-04, 28d ~09-18, 90d ~11-19. Phase D incorporation
  freeze-check re-runs after the ~09-15 reads (STATE §0.17). Developers track
  is gated behind that re-check (owner sequencing ruling, do not pull forward).

## 1. Track A: estate/letting agents (GO now, sequenced "after Phase C deploy")

Owner rulings in force (EXPANSION_PROPOSAL_2026-08-21, ruled + recorded, do not
re-litigate): pure RESOURCE surface. No service tier, no pricing, no claim to do
agency accounts, no client-money work, NO outreach machinery of any kind (no
email, no contact-form outreach, no partner/affiliate flows, no new monitors or
notifications, nothing that interrupts the owner). Publish-and-be-found only.
Return read: referral traffic + embed attribution, not a pipeline.

Evidence base (proposal Track 3): `renters rights act 2026` = 774.5 median
weekly Bing impressions (86x our core head term, identical GetKeywordStats
method); the related tail is document-shaped ("information sheet" variants
~1,400+1,111+985+603). The demand is in what agents must EXPLAIN, not in
agent-phrased queries (all returned zero history). 12 RRA posts already exist;
house_positions §20 locks the whole regime (commencements §20.12), §26.3 locks
the MEES do-not-write (EPC C 2030 NOT enacted; that constraint is explainer 3's
hook), §19.13 locks MTD managed-portfolio, §26.5/§26.6 database + redress.

Build shape (proposal, approved): `/for-letting-agents` hub (Solicitors'
`/for-<audience>` hubs = estate precedent; /landed-estates = freshest pillar
pattern incl. nav/footer/sitemap wiring) + 4-6 explainers in "what your
landlords will ask you this year" register:
1. RRA 2026 commencements: in force vs not (§20.12) - the highest-signal topic
2. The periodic-tenancy switch in the order it happens
3. MEES: law today vs what landlords think (E-floor + £3,500 cap enacted;
   EPC C 2030 aspiration only - §26.3 constraint AS the hook)
4. MTD for a managed portfolio: who files what (§19.13; extend-vs-new decision
   against `mtd-itsa-letting-agent-managed-portfolio-who-files-quarterly`)
5. Deposit/client-money tax edges, landlord-tax-treatment scope ONLY
6. Landlord database + redress enrolment (§26.5/§26.6)

MANDATORY dossier-first steps (§9 pipeline, rural = the freshest full example,
briefs/property/rural-estates/ = artifact shapes to copy):
- Pull live GSC query sets for the 12 existing RRA pages (proposal limitation 8:
  we may ALREADY earn this demand, which flips work from net-new to EXTEND).
  Client: optimisation_engine/clients/gsc_query_client.py; never stored snapshots.
- Bing page-query stats per RRA URL (GetPageQueryStats per URL, never site-wide
  GetQueryStats - top-N trap).
- Freeze check: which RRA pages sit in armed monitored windows (several wave-era
  pages may; verify in monitored_pages, do not assume).
- Language pass (§9.11): the audience is agents-reading-for-landlords; measure
  the register of whatever actually ranks for `renters rights act 2026` variants.
- /embed is ALREADY indexable (shipped this morning); zero new tools needed.
  The five agent-relevant calculators are listed in the proposal §Track 3.

Batch name suggestion: `agents1`. Everything else per §3 conventions below.

## 2. Track B: Phase E, Wave 12 cost-of-selling (approved, biggest build left)

Standing spec: HANDOFF_2026-08-20 §Phase E (read it - method, writer spec,
QA expectations all there). Headlines: ~45,850/mo family, 13 pages + pillar +
calculator, ON Property, **CGT hook mandatory in every page** (brand-fit gate
resolved on that basis). Owner rulings inherited: gov.uk/MSE-held heads in
scope ("position 3 under gov.uk still wins"); batch size 1 (one Opus writer per
topic); content Opus-only.

Wave 12 cautions specific to now:
- The cost-of-selling family borders the CGT cluster (deployed today, ARMED to
  ~11-19 on Google/28d Bing ~09-18). The dossier freeze check must bucket every
  keyword the cgt1 pages own as already-covered. The CGT calculator is a live
  experiment - do not touch it, do not build a competing seller-CGT calculator;
  the Wave 12 calculator is costs-of-selling (fees/conveyancing/EA commission
  arithmetic), a different tool.
- Estate agent fee content overlaps Track A's audience but NOT its pages; if
  both tracks run in one session, dedupe personas and CTA shapes across both
  batches (today's cross-writer sameness findings = EDITORIAL_batchA/B in
  briefs/property/rural-estates/qa/ - read them, the fingerprints repeat).
- Run /run-wave if using the wave conductor, else the rural session's manual
  pattern (packs -> writers -> two-track QA -> fix round -> floor-8 re-read ->
  gates -> verdict -> predeploy) is fully worked precedent.

## 3. Conventions that bit people this week (all learned/enforced today)

- Deploy: owner-triggered only. Clean worktree at pushed SHA: `git worktree add
  --detach C:/dep <sha>`, env-override deploy from C:/dep (VERCEL_PROJECT_ID
  prj_Di0U5vYZVPlkm7xcA3p9il9gyDzU, ORG team_XF9WAygZX7SGk9Fo4tOAnihH,
  `vercel deploy --prod --yes --archive=tgz`), verify 200s, THEN
  register_monitored_batch.py per batch (--page-urls for non-blog URLs),
  IndexNow exact URLs, remove worktree. deploy-and-index.ps1 handles ONE
  -QaBatch only and never passes --page-urls: multi-batch = manual sequence.
- Registrar dedupes on slug: pre-existing monitored rows for EXTEND pages must
  be re-baselined IN PLACE with a supersession note (script pattern in today's
  session; SDLT + cgt1 + rental1 precedents).
- qa_verdict record needs STRUCTURED evidence JSON (statute_checks with
  exists/content_supports_claim, arithmetic_recomputed, editorial grade);
  anti-vacuous rule blocks empty statute_checks on statute-citing pages.
  Record the batch LAST, after every edit. predeploy_gate --qa-batch <name>.
- Equity gate: scripts/sdlt_equity_gate.py --cluster <name> (add the cluster
  dir + universe to CLUSTERS; parser now tolerates bolded pack labels and
  NET-NEW page lines). Coverage notes live at
  briefs/property/<cluster>/notes/<page>.md_coverage.md for declined keywords.
- Voice scan bands: legacy pages may be pre-existing ROBOTIC - measure the
  BASELINE before claiming a regression (git show <sha>:<file>, swap, scan,
  restore). Meta-commentary weighs heavily; killing "This page sets out..."
  sentences rescued a 34.0 to 8.9 today.
- house_positions is live ground truth and was patched 3x today (§15.4: s.124E
  transferability claim-based, s.162B debt allocation, £1m leftover). Writers
  read it fresh; anything verified at legislation.gov.uk beats memory and beats
  the page. gov.uk announcement summary = F-102 do-not-cite for the cap figure.
- Personas: grep the corpus before naming anyone. Banned list grew today:
  + Malcolm Rees, Ormerod, Helen, Whitfield (+ the wave11 list + house
  Holloway family). CTA sentences: no shared templates across a batch.
- No em-dashes in user-facing copy. UK English. No pricing. A* or don't ship.

## 4. Not in scope for that session

Developers pillar (post-Phase-D gate), Phase D itself (~09-15 re-check), rural
delta list (DOSSIER §8 additions: s.125 woodlands, environmental-scheme APR,
s.162B full treatment, mixed-estate voice sweep), T5 benchmarks EXTEND
(~09-15), September reads. Off-site authority is owned elsewhere. Never GBP,
never new notifications without asking, never DeepSeek.
