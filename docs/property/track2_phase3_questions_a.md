# Track 2 Phase 3 — Worktree A Q&A log

Append-only. Worktree A sub-agent (`track2-phase3-a` branch, CGT reliefs cluster, 6 REWRITE) appends questions to manager here. Manager polls and responds via direct edit in this same file.

**Critical:** edits via ABSOLUTE PATH `C:/Users/user/Documents/Accounting/docs/property/track2_phase3_questions_a.md` only (per §16.15 + §16.37 + §16.14). NEVER edit a relative-path copy from the worktree subdir.

**Format:**
- Sub-agent appends: `Q-A1 | YYYY-MM-DD HH:MMZ | <slug> | <question text>` plus optional context bullets
- Manager replies: `A-A1 | YYYY-MM-DD HH:MMZ | <answer text>` immediately below the Q line, with the same `A1` identifier

**Expected volume:** 1-3 questions across the 6 REWRITE briefs (Phase 3 execution can surface in-the-moment statute-drift / build-edge-case / internal-link-survey questions that need manager arbitration before commit).

**No questions yet.** Worktree A sub-agent appends Q-A1, Q-A2, ... as needed.
