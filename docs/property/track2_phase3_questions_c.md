# Track 2 Phase 3 — Worktree C Q&A log

Append-only. Worktree C sub-agent (`track2-phase3-c` branch, Trial cohort, 4 REWRITE including T4 gold-reference canonical-rewrite) appends questions to manager here. Manager polls and responds via direct edit in this same file.

**Critical:** edits via ABSOLUTE PATH `C:/Users/user/Documents/Accounting/docs/property/track2_phase3_questions_c.md` only (per §16.15 + §16.37 + §16.14). NEVER edit a relative-path copy from the worktree subdir.

**Format:**
- Sub-agent appends: `Q-C1 | YYYY-MM-DD HH:MMZ | <slug> | <question text>` plus optional context bullets
- Manager replies: `A-C1 | YYYY-MM-DD HH:MMZ | <answer text>` immediately below the Q line, with the same `C1` identifier

**Expected volume:** 2-4 questions across the 4 REWRITE briefs (higher than Worktrees A/B because T1/T2/T3 are STRUCTURE-ONLY briefs requiring fresh data fetch + because T4 is a special-case canonical-rewrite requiring preservation decisions). Highest likelihood = T3 Bill-vs-enacted status check (has FA 2026 received Royal Assent? — verify against legislation.gov.uk at write time per F-2 + F-5 + F-22 pattern) + T4 canonical depth-up preservation boundary (which existing canonical sections to preserve verbatim vs uplift vs replace).

**No questions yet.** Worktree C sub-agent appends Q-C1, Q-C2, ... as needed.
