# TRADE PORT BLUEPRINT — SLICE 2: CALCULATORS, PILLARS, TRADE PAGES, LOCATIONS, GLOSSARY

Source of truth: `docs/_engines/PROPERTY_STANDARD_ROLLOUT.md` §A, §D.1-D.3, §E, §F.1/F.5, §G, §H, §I; `docs/property/DESIGN_SYSTEM.md` §0.1/0.4/0.5/0.6, §4/4a/4b, §5, §6/6a, §7, §9; `docs/_engines/DESIGN_PORT_PLAYBOOK.md` §6 (T1-T21) + §14 (T22-T27), §10.2. Binding Phase 0: `BRAND_LAYER.md` (T-W1 §4.3, contrast §5.2/5.3), `LIVE_DEFECTS.md` (TD-01..TD-30, TD-K1/K2), `FUNNEL_BASELINE.md`, `link_baseline.json`, `sweep_baseline.json` (both captured 2026-09-11T09:41Z, sha `18b4f25f`).
Target: `construction-cis/web` (site key `construction-cis`, Vercel `trade-tax-specialists`). Date: 2026-09-11. Format precedent: `docs/generalist/_port/DISPOSITION_SLICE2.md`.
Method: every file named below was opened on BOTH sides (Trade source + Property reference). Every count in this document was derived by command, not read off a prior doc; where a prior doc disagrees, §0.9 says so. Rendered DOM read by `curl`/`urllib` against the already-running prod server at `http://localhost:3177` (not started, not stopped by me). No file was edited. No build, no test, no git write.

## 0. Reality check: findings that change the shape of the work

**0.1 Trade consumes ZERO of the design kit.** `grep -rho '@accounting-network/web-shared/[a-zA-Z/_-]*' src | sort -u` returns 50 paths and **not one** is under `web-shared/design/`. Trade imports only `components/{ServiceTiers,StatsBar}`, `tools/*`, `content/{TableOfContents,ReadingProgress}`, plus analytics/leads/nurture/console. So unlike Generalist, nothing in this slice is "already half-ported": every `TopicSection` / `FaqSection` / `LeadCTAPanel` / `CoverageCards` / `ProcessTimeline` / `RelatedArticles` / `Breadcrumb`(kit) / `Eyebrow` / `ExampleFigureNote` / `NoticeCard` / `StatsCounter` adoption in this slice is a first import. Mitigating fact for T24: they all live in the SAME workspace package Trade already depends on (`package.json:14 "@accounting-network/web-shared": "*"`), and the kit's only third-party needs are `@radix-ui/react-accordion` (declared at `packages/web-shared/package.json:160`) and `lucide-react` (declared at `construction-cis/web/package.json:17`). So no new top-level dependency is required — but that is a DERIVED claim and `python scripts/check_dependency_closure.py` is still an acceptance test on every package, because Trade's own `package.json` does not list radix and a bundler change would expose it.

**0.2 The capture gate does not exist on any calculator route, and `resultWrapper` is already waiting for it.** Trade has NO `ResultGate.tsx`, NO `HeldResult.tsx`, NO `resultGateStorage.ts` (`ls src/components/calculators/` = `CalcResultCta.tsx`, `CalculatorClient.tsx`, `premium/`). What Trade has is `CalcResultCta` → `MiniCapture formId="calc_result"`, an always-visible capture below an already-satisfied result. Meanwhile a sibling port has ALREADY parameterised the shared renderer: `packages/web-shared/tools/components/Calculator.tsx:42,69` accepts `resultWrapper?: (node) => ReactNode` and applies it at `:122` around the navy result panel only. Generalist's slice 2 proposed adding that prop; on Trade it is a consume, not a build. The gate port is therefore 3 local files + 1 prop already in place.

**0.3 `PremiumCalculator` and `ResultGateModal` are REACHABLE, but not from any route in this slice.** Import chain, grep-derived: `ResultGateModal` ← `PremiumCalculator` (only importer) ← `PremiumUpgrade` (`next/dynamic`, `ssr:false`, `hidden sm:block`) ← `src/components/blog/BlogPostRenderer.tsx` (only consumer). `CalculatorPageResources` (the only island on `/calculators/[slug]`) renders `ResourceGate`, NOT `PremiumUpgrade`. Rendered proof: `/blog/cis-refunds/cis-tax-refund-how-to-claim` contains `Free interactive tool` ×1 and `aria-busy` ×4 (the sized placeholder); `/calculators/cis-refund-estimator` contains `Free interactive tool` ×0 and `Go deeper` ×2. So the premium fleet is live blog-only, desktop-only, client-only — **not dead code, and not the Solicitors five-dead-routes trap.** Consequence for this slice: the gate-skip instrumentation the brief describes (`cta_click{cta_id:"result_gate_skip"}`, `ResultGateModal.tsx:51`) has never fired on a calculator page, because the gate is not there.

**0.4 The registry is thinner than §G in three fields, and one of them silently disables the kit's whole warning path.** `grep -c` across `src/lib/calculators/tools/*.ts`: `related:` **0 of 12**, `workedExamples` **2 of 12** (`cis-penalty-calculator`, `cis-sole-trader-vs-limited`), `faqs:` **12 of 12**, `tone:` **0 of 12**, `verdict` **0 of 12**. Because no tool sets `headline.tone` or `verdict`, the kit's `bg-[var(--calc-warn-bg,#fbbf24)]` / `text-[var(--calc-warn-accent,#fbbf24)]` branches at `Calculator.tsx:131,141` are UNREACHABLE on Trade today. Rendered proof: `/calculators/cis-refund-estimator` contains `fbbf24` ×0 and `calc-warn` ×0, against `brand-primary` ×31.

**0.5 The calculator fleet emits ZERO unique internal links, and so do all three pillar pages.** Measured: chrome = 14 internal page links (`/about` has 16 `href="/…"` of which 2 are `_next` assets; the baseline instrument counts the 14). `/calculators/<any of 12>` = 14, set difference against chrome = **{}**. `/services`, `/cis-refund`, `/gross-payment-status` = 14, set difference = **{}** each: their body links (`/for`, `/gross-payment-status`, `/cis-refund`, `/contact`) are all chrome destinations, so they add nothing — T14 in its purest form. Every link this slice adds is pure upside; there is almost nothing to protect.

**0.6 `/for/[slug]` has a LeadForm. The funnel doc's "no form at all" is imprecise and the imprecision changes the fix.** `src/app/for/[slug]/page.tsx:237` renders `<LeadForm submitLabel="Request a callback" />` inside a white card on a navy band (D.1 ground invariant satisfied). `FUNNEL_BASELINE.md:171,244` reports 24 sessions / 2 CTA clicks / **0 form views** / 0 starts and concludes "the template presents no form". The template presents a form; nobody is delivered to it. Both hero CTAs leave the page (`:64` → `/contact`, `:68` → `/cis-refund`), there is no `#book` anchor anywhere on the page, and the form sits below the FAQ at the page foot. **So the fix is not "add capture to 45 pages"; it is "point the hero primary at the form that is already there, instrument it, and move it above the FAQ".** That is a one-template edit, not 45.

**0.7 The brief's "45 trade pages are the biggest under-monetised surface in your slice" is CORRECT and is restated here as the slice's priority, with the three pillars deliberately deprioritised.** `/for/[slug]` = 24 sessions / 19 distinct slugs / 2 CTA clicks / 0 form views / 0 starts / 0 leads. `/for` hub = 9 sessions. `/services` + `/cis-refund` + `/gross-payment-status` = **1 session between them in 19 days** (`FUNNEL_BASELINE.md:242,419`): nothing can be concluded about those templates in either direction, so they get the standard F.1 treatment and no heroics, no new figures commissioned, no copy re-authoring beyond the TD fixes. `/locations/[slug]` = 1 session, `/glossary` = 9, `/calculators` = 7 sessions with 11 `form_step_view` and 0 starts. Calculators are where the site's capture mechanism lives by design (`calc_result` produced the only calculator-sourced lead in site history, `5ade5168`, 2026-08-20) but they are not where the traffic is; the trade family is.

**0.8 Em-dashes: four different numbers, and the floor is 2, not 0.** The metrics are not interchangeable:
| metric | how | value in this slice |
|---|---|---|
| raw `—` occurrences in the 5 tool files | `grep -o '—' \| wc -l` | **39** (back-years 21, vs-paye 8, deduction 5, splitter 4, take-home 1) |
| user-facing source em-dashes (comments excluded) | minus `// YEAR N —` at back-years `:20,58,93,128` | **35** |
| rendered body em-dashes, default inputs, scripts stripped | `urllib` + strip `<script>`/`<style>` + unescape | **34** (back-years 18, vs-paye 8, splitter 4, deduction 3, take-home 1, self-assessment 0) |
| `sweep_baseline.json.totalDashes` | `docs/_engines/instruments/sweep.mjs:72` `DASH = /[—–]\|&mdash;\|&ndash;/g` | **36** |
The 36 is **34 em + 2 en**: `cis-self-assessment-calculator.ts:125` and `cis-vs-paye-comparison.ts:113` each carry `£12,570–£50,270`, a numeric range, which `LIVE_DEFECTS.md` TD-28 explicitly protects ("Do **not** touch en-dashes in numeric ranges"). **The acceptance test is `totalDashes == 2`, with those two lines named.** A test asserting 0 forces a correct en-dash to be broken; a test asserting "no regression" lets 34 ride. Also note TD-28's headline "53 lines across 6 user-facing files" does not reconcile with its own per-file figures (17+8+5+4+1 = 35, +8 in `llms-full.txt` = 43); the per-file figures are right, the headline is not. `llms-full.txt` is a route, not a page in this slice: flagged, not owned.

**0.9 Corrections to prior artefacts, recorded so nobody re-derives them.** (a) `sweep_baseline.json` `/calculators/cis-self-assessment-calculator: 1` is an **en**-dash, not an em-dash (see §0.8). (b) `/calculators/cis-deduction-calculator` renders 3 of its 5 user-facing em-dashes at default inputs (`:84` DRC note and `:89` unregistered verdict need non-default input) — all 5 still get fixed. (c) Property's `/section-24` figure sibling is `components/property/Section24Wedge.tsx`, not a `section-24-figures.tsx`. (d) `/embed` (26 links) and `/embed/[slug]` (14) are absent from `link_baseline.json`; they are `robots index:false` and correctly excluded, so they carry **no** link floor.

---

## Standing conversions applied in EVERY row below (stated once, never repeated)

1. `neutral-*` → `slate-*` on every ground, text and border in this slice; `bg-[#fafaf9]` / `bg-[#fafaf7]` / `bg-stone-50` → `bg-slate-50`; `bg-[#1e293b]` / `bg-neutral-900` → `bg-slate-900`. 2. `border border-slate-200` → `ring-1 ring-slate-200/70`; `rounded-2xl` → `rounded-xl`; square (no-radius) cards → `rounded-xl`. 3. Section padding → `py-12 sm:py-16 lg:py-20` literal; hero → `py-12 sm:py-16`. 4. `font-bold tracking-tight` heading pairs → §A.3 scale (`h1 text-3xl sm:text-4xl lg:text-5xl`, `h2 text-2xl sm:text-4xl`), `tracking-tight` dropped. 5. Hand-rolled `.section-label` / orange pill eyebrows → kit `Eyebrow` (`onDark` on navy). 6. `border-t`/`border-b border-neutral-200` section dividers → deleted; grounds do the separating (§9). 7. Every body clamp (`max-w-3xl mx-auto`, `max-w-4xl mx-auto`, `max-w-5xl`, `max-w-6xl mx-auto`) → removed; `siteContainerLg` IS the measure. `max-w-3xl` survives ONLY on hero copy and a section standfirst above a full-width grid (§6a). 8. Grounds set explicitly per section, oscillating white/slate-50; a card's ground opposes its section's; navy never touches navy; canonical tail = panel, FAQ, footer. 9. Every figure-bearing visual gains `ExampleFigureNote` (sole exemption `StatsCounter`). 10. Hand-rolled `<details>`/`<dl>`/h3+p FAQ → kit `FaqSection` + `buildFaqPageJsonLd` on the SAME array (T17). 11. On-page primary CTA → `#book` (`#get-expert-help` on calculator routes), anchor div carries `scroll-mt-24`; only header + StickyCTA leave for `/contact`. 12. Bare `LeadForm` in a coloured card → `LeadCTAPanel`; bare button → the D.3 mid-page card. 13. Every CTA carries `data-cta` + `data-cta-placement` + `data-cta-goal`; existing ids preserved byte-exact (T22); the full attribute set is diffed rendered-vs-rendered, not id-only. 14. `text-orange-500`/`text-orange-600` as TEXT → `text-orange-700` on light (5.18) / `text-orange-300` on dark (8.67); orange-600 stays legal for rings, ticks and motif strokes (3.56 clears the 3:1 graphics floor). 15. `text-red-600` validation → `var(--form-error)` red-700 + `role="alert"` + `aria-invalid`. 16. Warning semantics → T-W1 `--warn-1..4` / `--warn-n-on-dark` (never `--warn-red`). 17. No em-dashes in anything rendered; en-dashes in numeric ranges untouched. 18. No client/volume/aggregate claims, no turnaround promises, no fee or price signal, no "guarantee".

---

## 1. `/calculators/[slug]` — 12 routes, one template, `src/app/calculators/[slug]/page.tsx` (169 lines)

| # | New (standard classes) | Replaces (file:line) | Verdict | Why | Adaptation |
|---|---|---|---|---|---|
| 1.1 | `buildCalculatorJsonLd` + `buildFaqPageJsonLd` on the SAME `tool.faqs` array, + kit `Breadcrumb` BreadcrumbList | `:46-63` (two `<script>` blocks, correct already); `:66` local `Breadcrumb` | KEEP | Already the §F.5 schema pair, same array both ways — T17 satisfied. Local `Breadcrumb` emits no BreadcrumbList | Swap to kit `Breadcrumb` for the JSON-LD only; visual is a no-op |
| 1.2 | navy hero `bg-slate-900 py-12 sm:py-16` + `Eyebrow onDark` "Free calculator · 2026/27 rates" + h1 + standfirst `max-w-3xl text-base sm:text-lg text-slate-300` + NET-NEW `${btnOnDark}` → `#get-expert-help` | `:65-77` (no eyebrow, no CTA, standfirst `max-w-2xl text-lg`) | ADOPT-STANDARD | §0.5 checklist: a page a reader can scroll past without meeting an ask is not finished. 11 `form_step_view` / 0 starts on this family says the ask is not being reached | `data-cta="calc_hero_help"`, `placement="hero"`, `goal="form"` — net-new id, does not collide with any of the 620 baseline CTA attributes |
| 1.3 | `bg-slate-50` section, UNCLAMPED, `CalculatorClient` wrapped in NET-NEW `ResultGate campaign={tool.slug}` via the existing `resultWrapper` prop | `:80-86` (`max-w-5xl` clamp; `resultCta={<CalcResultCta/>}`) | ADOPT-STANDARD (id = GATE) | §6a body-clamp ban; §D.2 ResultGate. `resultWrapper` already exists at `Calculator.tsx:42,69,122` (§0.2) so no kit edit and no T23 multi-consumer risk | The gate wraps the RESULT COLUMN only, which is exactly what `resultWrapper` encloses. `calc_result_<slug>` is NOT an existing id on this site, so the reveal takes a NEW id `calc_result_gate_<slug>`; `result_gate_skip` is preserved byte-exact from `ResultGateModal.tsx:51` |
| 1.4 | `CalcResultCta` / `MiniCapture formId="calc_result"` | `:83`; `src/components/calculators/CalcResultCta.tsx` (26 lines) | GATE | Property DELETED `calc_result` estate-wide 2026-08-22 (`Property/web/src/config/deploy-watch.ts:149`) and the gate out-converts an always-visible capture. BUT `FUNNEL_BASELINE.md:132` is explicit: it produced a real lead on 2026-08-20 (`5ade5168`) and must NOT be deleted as dead. 9 step views, 0 starts in-window | T-G3. If retained, it moves BELOW the revealed gate, never beside it (two asks on one result is the Generalist `GateOrForm` defect) |
| 1.5 | `CalculatorPageResources` reduced to the island; "Go deeper" orange chip → `Eyebrow` | `:84`; `src/components/resources/CalculatorPageResources.tsx:39-48` | KEEP-PAYLOAD-RESTYLE | Live, rendered (`Go deeper` ×2 on every calc page), and `ResourceGate` is already the post-2026-07-18 no-email-unlock MiniCapture (`ResourceGate.tsx:4-10`) — carve-out 2 is NOT breached here. `formId="resource_block"`, 0 leads in-window | Inline `style={{background:"#1e293b"}}` → `bg-slate-900`. Second-ask question is T-G4, not a unilateral retirement |
| 1.6 | white explainer at FULL container, `h2 text-2xl sm:text-4xl` + kit `Prose` | `:88-97` (`max-w-3xl` clamp, `h2 sm:text-3xl`) | ADOPT-STANDARD | §6a names calculator routes as the family that kept missing this; `sm:text-3xl` is a size that exists nowhere in §A.3 | One clamp removal covers the explainer, worked examples, FAQ and CTA — they share the wrapper at `:90` |
| 1.7 | `workedExamples` rendered with `ExampleFigureNote` | `:99-130` (renders, no figure note) | KEEP-PAYLOAD-RESTYLE | §5 rule 6: mandatory on every visual carrying figures. Only 2 of 12 tools have the data (§0.4) | The other 10 render nothing here and stay rendering nothing; commissioning 10 is T-G5, NOT this package |
| 1.8 | kit `FaqSection faqs={tool.faqs}` | `:132-147` (hand-rolled h3+p pairs) | ADOPT-STANDARD | §4b bans h3-and-paragraph FAQ pairs; the schema at `:52` already reads the same array, so this is a render swap with zero schema risk | 12 of 12 tools have `faqs` (§0.4), so it renders on all 12 |
| 1.9 | `<div id="get-expert-help" class="scroll-mt-24">` + `LeadCTAPanel` navy, `redirectOnSuccess={false}` | `:149-168` (bare `LeadForm` in an orange-gradient `rounded-2xl` card, `border-orange-500/20`, `h2 text-orange-700`) | ADOPT-STANDARD | §D.3: "Never a bare LeadForm in a coloured card." `scroll-mt-24` is already present at `:151` (TD-22 confirms this page is one of the compliant ones) so the anchor behaviour is preserved | `redirectOnSuccess={false}` is already the live value at `:165` — keep it. Ground flips orange-gradient → navy panel, so the FAQ at 1.8 must sit AFTER it or the panel touches the navy footer (§9) |
| 1.10 | NET-NEW `RelatedArticles columns={3}` on `bg-slate-50` from `tool.related` | — | GATE | §F.5 requires it; `related:` is absent from **all 12** tools (§0.4). §0.4 of DESIGN_SYSTEM forbids substituting a calculator link-card | T-G6: 12 `related[]` arrays are a content commission. Until then this section renders nothing and the route's unique-link count stays 0 |
| 1.11 | `--calc-warn-bg` / `--calc-warn-fg` / `--calc-warn-accent` defined on `:root` from T-W1 | `src/app/globals.css` (tokens absent); consumed at `Calculator.tsx:131,141` | NET-NEW | The kit seam exists and Trade defines none of the three, so the amber fallbacks would apply — but they are UNREACHABLE today (§0.4, `fbbf24` ×0 rendered). Define them anyway, before any tool gains a `tone`, or the first `tone:"warn"` ships amber | `--calc-warn-accent: var(--warn-2-on-dark)` (pink-400, 6.74 on slate-900 — the result panel is navy); `--calc-warn-bg: var(--warn-2)` pink-700 with `--calc-warn-fg: #ffffff` (6.04 band). Set in the tokens commit, never by editing the kit (T12) |
| 1.12 | `dynamicParams = false` + `generateStaticParams` from `genericTools()` | `:18-22` | KEEP | Verified: all 12 registry slugs return **HTTP 200** against the running server; `BESPOKE` is `[]` at `registry.ts:31`; no registry entry lacks a route and no route lacks a registry entry. The Solicitors five-dead-premium-routes trap does NOT reproduce here | Nav/tab lists may quote all 12 safely. Evidence in §4 |

## 2. `/calculators` index — `src/app/calculators/page.tsx` (131 lines)

| # | New (standard classes) | Replaces (file:line) | Verdict | Why | Adaptation |
|---|---|---|---|---|---|
| 2.1 | navy motif hero `min-h-[300px] sm:min-h-[350px]` + kit `Breadcrumb` + `Eyebrow onDark` | `:48-67` (`py-14 sm:py-20`, local Breadcrumb, no eyebrow) | ADOPT-STANDARD | §F.5 index anatomy; standing conversion 3 | Standfirst `max-w-2xl` → `max-w-3xl`, hero copy is the sanctioned narrow measure |
| 2.2 | Tier 1 white "Run one now" block + `CalculatorTabs tabs={INDEX_HEADLINE_TABS}` + a LITERAL `<a href="/calculators/cis-refund-estimator">` | — (no tabs, no tier split) | NET-NEW | §F.5 two-tier index; §6 "tabs, not link cards" for on-page tools. The crawl guard is a SOURCE scan and cannot see through a constant, so the href is spelled out | `CalculatorTabs` is absent from the kit (`ls packages/web-shared/design/marketing` has no such file) → BUILD LOCAL from `Property/web/src/components/calculators/CalculatorTabs.tsx`. Tab list in §5 |
| 2.3 | Tier 2 `bg-slate-50` categorised directory, cards `rounded-xl border-2 border-slate-200 bg-white p-3 sm:p-4 hover:border-orange-500`, icon badge `h-9 w-9 sm:h-11 sm:w-11 rounded-xl` | `:70-103` (flat `rounded-2xl border-2` cards, `text-orange-600` "Open calculator →") | ADOPT-STANDARD | §F.5 directory recipe; standing conversions 2 and 14 (`text-orange-600` on white = 3.56, FAIL as text, `BRAND_LAYER.md:211-212`) | All 12 cards stay rendered — they are 12 of the index's 13 unique links (§7) |
| 2.4 | `CATEGORY_ORDER` corrected to the categories that exist | `:30-38` (7 names listed) | ADOPT-STANDARD | Grep-verified live categories are **5**: CIS Basics (4), CIS Compliance (4), CIS Refunds (3), CIS Comparisons (1), and nothing else. Three listed names (`VAT and MTD`, `Expenses`, `Limited Company`) match no tool; `CIS Comparisons` is NOT listed and falls to the `-1 → 99` tail | Either list the 5 real categories in order, or merge `CIS Comparisons` into `CIS Basics` (1 tool does not earn a heading). T-G7 |
| 2.5 | `/embed` tail link kept | `:105-114` | KEEP | It is 1 of the index's 13 unique links and `/embed` is otherwise orphaned; deleting it costs a unique link on the one page in this slice that has any (T14) | `text-slate-500 hover:text-orange-600` → `hover:text-orange-700` |
| 2.6 | `#get-expert-help` `LeadCTAPanel contained` (`bg-slate-100 rounded-xl p-6 sm:p-10 lg:p-14 ring-1 ring-slate-200`) | `:116-129` (navy `bg-slate-900` block, bare `btnPrimary` → `/contact`) | ADOPT-STANDARD | §D.3 "a bare button is not a CTA block"; §9 `contained` is for where a navy band would collide — there is no FAQ after it, and a full-bleed navy band here would touch the navy footer. Also standing conversion 11: this is the index's only ask and it currently leaves the site section entirely | `/contact` → on-page form. The `/contact` link is chrome anyway, so removing it costs **0** unique links (T14) |
| 2.7 | `FaqSection` + `buildFaqPageJsonLd` | — | GATE | §F.1 tail, and it keeps a navy panel off the footer if 2.6 ships full-bleed instead of contained | T-G8: 4-6 questions to author. Not required if 2.6 stays `contained` |

## 3. `/embed` + `/embed/[slug]`

| # | New (standard classes) | Replaces (file:line) | Verdict | Why | Adaptation |
|---|---|---|---|---|---|
| 3.1 | gallery restyled to standard scale + `rounded-xl ring-1` | `src/app/embed/page.tsx:53-147` | KEEP-PAYLOAD-RESTYLE | Structurally correct already | `bg-slate-*` is already in use here (this file is the one page in the slice that is not on the `neutral-*` palette) |
| 3.2 | snippet-only gallery | `:40-47,120` (`EmbedSnippet code={iframeSnippet(...)}`) | KEEP | Generalist's G7 was "21 live iframes mounted at once". Trade mounts **zero**: the gallery prints the snippet as text. Verified by reading `:40-47` and `:120` and by `/embed` rendering 12 `/calculators/*` links and no `<iframe src>` | No work. Recorded so nobody re-files G7 here |
| 3.3 | embeds NEVER gated | `src/app/embed/[slug]/page.tsx:37` `variant="embed"` | KEEP | The existing `variant === "embed"` switch already suppresses `resultCta` (`Calculator.tsx:181`). Derive `enabled={false}` for `ResultGate` from the SAME switch; do not add a second flag | `robots index:false` at `:25` kept; out of IndexNow; **no link floor** (§0.9d) |

## 4. Calculator fleet — all 12, verified

HTTP status from `curl -s -o /dev/null -w '%{http_code}'` against `localhost:3177`, 2026-09-11. "Gate today" = a capture surface on the result. "em (src)" = user-facing source em-dashes, comments excluded. "em (rendered)" = SSR body text, default inputs, scripts stripped.

| # | Route | HTTP | Category | Gate today | em (src) | em (rendered) | `related[]` | `workedExamples` | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| 4.1 | `/calculators/cis-deduction-calculator` | 200 | CIS Compliance | no (MiniCapture `calc_result`) | 5 (`:42,43,44,84,89`) | 3 | — | — | ADOPT-STANDARD (id = GATE) |
| 4.2 | `/calculators/cis-gps-eligibility-checker` | 200 | CIS Compliance | no (MiniCapture) | 0 | 0 | — | — | ADOPT-STANDARD + TD-05/TD-06 fix at `:157,169` |
| 4.3 | `/calculators/cis-refund-estimator` | 200 | CIS Refunds | no (MiniCapture) | 0 | 0 | — | — | ADOPT-STANDARD (id = GATE) |
| 4.4 | `/calculators/cis-self-assessment-calculator` | 200 | CIS Refunds | no (MiniCapture) | 0 | 0 (+1 **en**, `:125`, protected) | — | — | ADOPT-STANDARD |
| 4.5 | `/calculators/cis-take-home-calculator` | 200 | CIS Basics | no (MiniCapture) | 1 (`:124`) | 1 | — | — | ADOPT-STANDARD |
| 4.6 | `/calculators/cis-invoice-splitter` | 200 | CIS Basics | no (MiniCapture) | 4 (`:42,43,44,96`) | 4 | — | — | ADOPT-STANDARD |
| 4.7 | `/calculators/cis-vs-paye-comparison` | 200 | CIS Basics | no (MiniCapture) | 8 (`:89,90,92,96,105,106,108,113`) | 8 (+1 **en**, `:113`, protected) | — | — | ADOPT-STANDARD |
| 4.8 | `/calculators/cis-back-years-calculator` | 200 | CIS Refunds | no (MiniCapture) | 17 (16 labels `:23,31,39,46,61,68,75,82,96,103,110,117,131,138,145,152` + template `:219`) | 18 | — | — | ADOPT-STANDARD — the worst single file |
| 4.9 | `/calculators/cis-penalty-calculator` | 200 | CIS Compliance | no (MiniCapture) | 0 | 0 | — | yes | ADOPT-STANDARD + T-W1 is the natural home for its ladder |
| 4.10 | `/calculators/cis-reverse-charge-checker` | 200 | CIS Compliance | no (MiniCapture) | 0 | 0 | — | — | ADOPT-STANDARD |
| 4.11 | `/calculators/trade-take-home-calculator` | 200 | CIS Basics | no (MiniCapture) | 0 | 0 | — | — | ADOPT-STANDARD |
| 4.12 | `/calculators/cis-sole-trader-vs-limited` | 200 | CIS Comparisons | no (MiniCapture) | 0 | 0 | — | yes | ADOPT-STANDARD — produced the site's only calculator lead (`5ade5168`) |
| | **totals** | **12/12 = 200** | 4 categories | 0 gates, 12 MiniCaptures | **35** | **34** (+2 en = the 36 baseline) | **0/12** | **2/12** | |

Em-dash replacement, per line, no bulk-sed. Field labels and select options (`"2025/26 — Gross CIS income"`, `"Gross Payment Status — deduct 0%"`, `"0% — Gross Payment Status"`, `"CIS path — income tax"`): **colon**, e.g. `"2025/26: Gross CIS income"`, `"Gross Payment Status: deduct 0%"`, `"CIS path: income tax"`. Prose mid-sentence em-dashes (`deduction-calculator:89` "…before making the first payment — failure to do so…", `invoice-splitter:96` "…your materials — only to the labour element…", `vs-paye:113` "…through Self Assessment — annual take-home figures…", `deduction-calculator:84` "DRC applies — you account for VAT separately"): **full stop** where both halves are sentences, **comma** where the second half is a clause, never a middle dot in a money sentence. Question text (`take-home:124` "I am deducted at 30% — how do I get to 20%?"): **comma**. Template expression `back-years:219` `` `${y.label} — estimated ${...}` ``: `` `${y.label}: estimated ${...}` `` (one edit fixes 4 rendered rows). The 4 `// YEAR N —` comments at `back-years:20,58,93,128` are code comments and are **exempt** — do not touch them, or the next sweep's source count and rendered count diverge again.

## 5. The §G calculator-on-a-page contract, mapped onto Trade

**File layout (§G):** `registry.ts` ✓ (BESPOKE[] + GENERIC[] → TOOLS + shared `makeRegistryHelpers`); `tools/*.ts` one per tool ✓ (12 files); `lib/calculators/cis-tax.ts` is the ground-truth math module ✓; `components/calculators/` ✗ no tabs, no gate; `app/calculators/[slug]` ✓ SSG; `app/calculators/<bespoke>/` n/a (BESPOKE empty, and that is the right answer — 12 generic tools through one route is AHEAD of Property's 5-bespoke shape, §4.6.3); `app/embed/[slug]` ✓ noindex. `types.ts`/`format.ts`/`nav.ts` live in the shared package, not locally — also ahead, keep.

**`GenericTool` field audit against §G:** `slug, name, category, oneLiner, metaTitle, metaDescription, intro, fields[], compute→{headline,rows,note}, explainer{heading,paragraphs}, faqs[], embedHeight` all present ×12. Missing: `related[]` ×12 (T-G6), `workedExamples` ×10 (T-G5), `verdict` ×12, `headline.tone` ×12 (§0.4 — the kit's warning path is unreachable, token defaults land anyway per 1.11).

**Capture-gate decision (the slice's one real conversion change).** Port Property's three files verbatim, re-hued: `Property/web/src/components/calculators/{ResultGate,HeldResult,ResultGateModal}.tsx` + `resultGateStorage.ts` → `construction-cis/web/src/components/calculators/`. Behaviour, unchanged: the REAL result behind `blur-[10px] saturate-125 scale-[1.03]` inside the navy frame, `inert` + `aria-hidden` on the held node, glass scrim, `max-w-sm rounded-2xl bg-slate-900/90` prompt card (the sanctioned rounded-2xl overlay), every dismiss path reveals, reveal persisted per campaign, converted visitors bypass, embeds `enabled={false}` derived from the existing `variant==="embed"` switch, revealed state appends `${btnSecondary} mt-4 w-full` "Get this figure checked by a CIS specialist" inside the SAME div as the result. Mounted through `resultWrapper` (§0.2), so it wraps the result column and not the whole tool, with zero kit edits and zero T23 exposure. Trade's existing `ResultGateModal` at `src/components/calculators/premium/ResultGateModal.tsx` is the BLOG premium gate (§0.3): it stays where it is, keeps its `result_gate_skip` id, and the calculator gate reuses that id value so the skip diagnostic does not fork. **Three things bind this decision and two of them are owner calls: `calc_result` retention (T-G3), `resource_block` retention (T-G4), and the fact that unlike `/calculators` on Generalist, Trade's index standfirst makes NO "no email gate" promise** — `src/app/calculators/page.tsx:63-66` says only "Free tools for UK CIS subcontractors and contractors", so Generalist's G22 blocker does **not** exist here. Verified by reading the rendered hero text.

**Tabs / index structure.** `CalculatorTabs` BUILD LOCAL from Property's file: WAI-ARIA tablist, roving tabindex, Arrow/Home/End, `#hash` deep-link, `next/dynamic` panels, literal `TABLIST_COLUMNS` map (Tailwind scans literals only), tab `rounded-xl border-2 p-3 sm:p-4`, selected `border-orange-600 bg-orange-600 shadow-md` (white on orange-600 = 3.56, large-text/graphics only — the tab label is `font-bold` at `text-sm`, so the selected label must be `text-white` on `bg-orange-700` 5.18, NOT orange-600; this is the one place Property's recipe cannot be copied hue-for-hue), icon badge `h-9 w-9 sm:h-11 sm:w-11 rounded-xl`, panels `mt-6 sm:mt-8`. Every panel renders `CalculatorClient` by slug — never a second copy of a tool. TabKey union (4, one per live category, keyed to the funnel's own evidence): `refund` → `cis-refund-estimator`, `deduction` → `cis-deduction-calculator`, `takehome` → `cis-take-home-calculator`, `soletradervsltd` → `cis-sole-trader-vs-limited` (the only slug with a recorded lead). `INDEX_HEADLINE_TABS` = all 4; every listed panel mounts and downloads, so 4 is the ceiling, not a starting point. Pillar pages (`/services`, `/cis-refund`, `/gross-payment-status`) carry `tabs={["refund"]}` / `tabs={["deduction"]}` where the page is about that calculation, and each such page then owes ONE literal `<a href="/calculators/...">`. `/for/[slug]`, `/locations/[slug]` and `/glossary/[slug]` carry **no** tabs (not about a calculation) and a plain nav link only. Trade has no `calculator-tabs-crawl-path.test.ts`; the package that introduces tabs introduces the guard.

## 6. Service pillars

### 6a. `/services` — `src/app/services/page.tsx` (214 lines)

| # | New (standard classes) | Replaces (file:line) | Verdict | Why | Adaptation |
|---|---|---|---|---|---|
| 6a.1 | `TopicHero` cream `min-h-[360px] sm:min-h-[420px]` + kit `Breadcrumb` + `Eyebrow` + h1 `text-3xl sm:text-4xl lg:text-5xl` | `:111-121` (`bg-[#1e293b]`, `border-b`, `py-16 sm:py-20`, `.section-label`, h1 `text-4xl sm:text-5xl font-bold tracking-tight`, no breadcrumb) | ADOPT-STANDARD | §F.1 hero contract; standing conversions 1,3,4,5,6. No breadcrumb on a pillar page is a crawl and BreadcrumbList gap | Hero CTA row is NET-NEW: primary `#book`, secondary → `/calculators` |
| 6a.2 | white `StatsCounter` strip `py-5 sm:py-7` | `:124-128` shared `StatsBar stats={siteStats}` | KEEP-PAYLOAD-RESTYLE | `StatsBar` is a 27-line pure function with no counter (LIVE_DEFECTS §5.2 corrects the brief that expected an SSR-truncation bug: "structurally impossible here"). Swapping to `StatsCounter` buys animation and costs an SSR-truth risk (T15) | **Recommendation: KEEP `StatsBar`.** Nothing is broken. But `siteStats` content is defective: TD-29 `"57+"` guides against `ls content/blog \| wc -l` = **82**, and TD-13 `"24h Response guarantee"`. Fix the data, not the component |
| 6a.3 | 7 service lines → `CoverageCards columns={2}` on white, copy verbatim | `:131-165` (`bg-[#fafaf9]`, `border border-neutral-200` cards, `h-14 w-14` square orange icon tile, `.section-label`) | ADOPT-STANDARD | §E `CoverageCards` is the 2/3-col grid with icon badge; 7 equivalent things IS a grid (§5) | Card ground must oppose the section's (§4a rule 3). `id={service.id}` + `scroll-mt-24` at `:139-140` are load-bearing anchors for inbound links — preserve all 7 |
| 6a.4 | the fabricated director-penalty sentence | `:46` "Director liability under **Finance Bill 2026** ss.62A/62B reaches up to **30% of the tax lost**" | DELETE | **TD-05** (banned BY NAME in `house_positions.md` §3 as a CORRECTED fabrication) **and TD-06** (FA 2026 received Royal Assent 18 Mar 2026; "Bill" understates settled law). Two separate locked rules on one line; fixing one does not fix the other | Keep director exposure general per §3, or cite FA 2004 s.72B accurately. Same sentence recurs at `gross-payment-status:49` and `cis-gps-eligibility-checker.ts:157,169` — **fix all four in one commit** (T23 shape) |
| 6a.5 | `ServiceTiers tiers={serviceTiers} featuredBadge=""` | `:179` `featuredBadge="Most Popular"` | KEEP-PAYLOAD-RESTYLE | The component is LIVE shared kit (`grep -rl ServiceTiers src` → `app/page.tsx`, `app/services/page.tsx`, `config/service-tiers.ts`). The rollout doc's §4.2 "dead component" line refers to Property's unused LOCAL copy and the doc contradicts itself at :302 — **not** grounds for retirement. `"Most Popular"` is an aggregate claim about client behaviour, banned by §I | `featuredBadge=""` suppresses the badge while `featured: true` at `service-tiers.ts:39` keeps the emphasis. T13 applies: verify the kit's DEFAULT is not itself `"Most Popular"` before assuming `""` is needed rather than omission — read `ServiceTiers.tsx` defaults first. The homepage instance at `app/page.tsx` belongs to slice 1 |
| 6a.6 | `service-tiers.ts` data fixes | `:70` `"57+"`; `:38` "Fixed fee, quoted before we start"; `:71` `"24h / Response guarantee"` | ADOPT-STANDARD | TD-29 (82 posts, derive from `getAllPosts().length`), TD-18 (under the pool model the partner firm sets the fee, not us), TD-13 (no response-time promise; the privacy policy discloses up to six firms and a possible 48 hours) | All three are `siteStats`/tier data shared with the homepage — **slice 1 renders the same array.** Co-ordinate or the two slices fight over one file |
| 6a.7 | `<div id="book" class="scroll-mt-24">` + `LeadCTAPanel` navy, `redirectOnSuccess` TRUE | `:184-211` (two bare `Link`s both → `/contact`, no form on the page at all) | ADOPT-STANDARD | `/services` is the only pillar in this slice with **zero** capture (`/cis-refund:249` and `/gross-payment-status:266` both render a `LeadForm`). §0.5: a pillar that informs and does not convert is half-built | `redirectOnSuccess` TRUE (false is only for calculator and blog surfaces). The two `/contact` links removed cost **0** unique links (both chrome, T14) |
| 6a.8 | `FaqSection` + `buildFaqPageJsonLd`; JSON-LD `Service` + `OfferCatalog` (no pricing) + `BreadcrumbList` + `FAQPage` | — (this page emits **no** JSON-LD) | GATE | §F.1 tail and §H. The page's 7 service titles are an `OfferCatalog` for free | T-G9: 5-6 questions to author. The trailing `/for` link at `:205` survives as a body link |

### 6b. `/cis-refund` (256 lines) and `/gross-payment-status` (273 lines)

| # | New (standard classes) | Replaces (file:line) | Verdict | Why | Adaptation |
|---|---|---|---|---|---|
| 6b.1 | cream `TopicHero` + `Breadcrumb` + `Eyebrow`, hero primary → `#book` | `cis-refund:84-99`; `gps:85-100` (`bg-[#1e293b]`, `border-b`, `btnPrimary` → `/contact`) | ADOPT-STANDARD | §F.1; standing conversion 11. Both pages already hold a form at the foot, so the hero CTA currently sends the reader away from it | 1 session across both pages in 19 days (§0.7): standard treatment, no new figures, no re-authoring |
| 6b.2 | `TopicSection` bands, grounds oscillating white/slate-50, `border-b border-neutral-200` dividers deleted, clamps removed | `cis-refund:102,128,149,172`; `gps:103,144,191` | ADOPT-STANDARD | §4/§4b/§9; standing conversions 1,6,7 | Both files alternate `bg-white` / `bg-[#fafaf9]` already — the oscillation is right, only the tokens and the dividers are wrong |
| 6b.3 | rate tables kept, `text-orange-600` cells re-hued | `cis-refund:198`; `gps:130` | KEEP-PAYLOAD-RESTYLE | The table is what makes a reference page citable (§4a) — never replaced by a figure. `text-orange-600` on white = **3.56, FAIL** (`BRAND_LAYER.md:211-212` names these two lines) | → `text-orange-700` (5.18). Header rows `bg-[#1e293b]` → `bg-slate-900` |
| 6b.4 | the 30% director-penalty sentence + "Finance Bill 2026" ×2 | `gross-payment-status/page.tsx:49` and `:198` | DELETE | TD-05 + TD-06, as 6a.4. `:198` is a second "Finance Bill 2026" on the GPS pillar, i.e. on the page about that very statute | Note TD-07: the guard test at `src/tests/assistant-journey-opener.test.ts:706-711` asserts against the resources-registry FAQ set, NOT this page's `faqs` array, so it is green while the page is wrong. The package that fixes the copy repoints the guard |
| 6b.5 | `<div id="book" class="scroll-mt-24">` + `LeadCTAPanel` navy | `cis-refund:216-252`; `gps:226-269` (bare `LeadForm` in a white card inside a `bg-[#1e293b]` grid) | ADOPT-STANDARD | §D.3 no bare LeadForm in a coloured card. The D.1 ground invariant is already satisfied (white card, slate-900 labels) — re-verify on the rendered page, do not assume either way | The two body links (`gps:251` → `/cis-refund`, `:258` → `/for`; `cis-refund:207` → `/gross-payment-status`, `:241` → `/for`) are all chrome destinations and contribute **0** unique links — but keep them, they are free |
| 6b.6 | `FaqSection` + `buildFaqPageJsonLd`; keep `buildHowToJsonLd` | `cis-refund:62`; `gps:63` (HowTo present, no FAQPage) | GATE | §F.1 tail | T-G9 covers all three pillars' FAQ commissions together |
| 6b.7 | NET-NEW `CalculatorTabs tabs={["refund"]}` / `tabs={["deduction"]}` + one literal `<a href="/calculators/cis-refund-estimator">` | — | ADOPT-STANDARD | §6: where a page would carry a card pointing at a calculator, render the tool. Both pillars are about a calculation the reader can run on the spot, and both currently emit **zero** unique links (§0.5) | Raises each pillar's floor from 14 to 15+. Depends on §5's tabs component |
| 6b.8 | `/cis-refund` metadata + H-section £2,000 claim | `cis-refund/page.tsx:10` (metadata) and `:88` | GATE | **TD-11**: "the average CIS subcontractor is owed around £2,000 back" re-scopes a third-party marketing figure (Dearne £1,840, RIFT range) to a whole population, uncaveated, and `:10` is `metadata.description`, so it is in the SERP | Owner's word needed (LIVE_DEFECTS §6 item 3). Do not fix unilaterally; T-G10 |

## 7. Trade pages — the slice's priority family

### 7a. `/for` index — `src/app/for/page.tsx` (118 lines)

| # | New (standard classes) | Replaces (file:line) | Verdict | Why | Adaptation |
|---|---|---|---|---|---|
| 7a.1 | navy motif hero + `Breadcrumb` + `Eyebrow onDark` | `:20-30` (`bg-neutral-900`, `border-b`, `.section-label`, no breadcrumb) | ADOPT-STANDARD | §F.1; standing conversions 1,3,5,6 | NET-NEW hero CTA → `#book` |
| 7a.2 | ground oscillation: navy hero → white trades grid → slate-50 businesses grid | `:33` `bg-neutral-900` + `:63` `bg-neutral-800` | ADOPT-STANDARD | §9: the hero is navy and the next section is navy, so **two touching sections share a ground** and a navy band sits mid-page on a topic grid, which §9 reserves for sections addressed AT the reader. `bg-neutral-800` exists nowhere in the standard | Cards move from `bg-white/5 border-white/10` to light cards `rounded-xl bg-white ring-1 ring-slate-200/70 hover:border-orange-600`. All **45** links stay rendered |
| 7a.3 | `text-orange-400` section headings on dark → `Eyebrow onDark` + real `h2` | `:35-37`, `:65-67` (`h2 text-xs font-semibold uppercase` — an `h2` styled as an eyebrow) | ADOPT-STANDARD | §A.3: a 12px uppercase `h2` is an eyebrow wearing a heading tag; it breaks the document outline on the hub page for 45 children | Eyebrow carries the label, the `h2` carries the real heading |
| 7a.4 | the em-dash in the source comment at `:61` | `:61` `{/* Construction businesses grid — hidden when empty */}` | KEEP | Code comment, exempt per §I and TD-28. Recorded because `grep -rl '—' src/app/for` hits this file and a careless sweep "fixes" it | No change |
| 7a.5 | `#book` `LeadCTAPanel contained` | `:108-112` (bare `btnPrimary` → `/contact`) | ADOPT-STANDARD | 9 sessions, 0 CTA clicks, 0 form views. §D.3 bare button | `contained` — the section above is cream/light and the footer is navy |

### 7b. `/for/[slug]` — 45 routes, one template, `src/app/for/[slug]/page.tsx` (279 lines), data `src/data/trade-types.ts` (3,305 lines)

45 slugs confirmed: `grep -oE 'slug: *"[^"]+"' src/data/trade-types.ts | sed 's/.*"\(.*\)"/\1/' | sort -u | wc -l` = **45** (the brief is right; `grep -c 'slug:'` = 47 because `:16`-style `slug: string` type annotations are counted). Cross-checked against `sweep_baseline.json`, which carries exactly **45** `/for/*` routes. Note the slugs are PLURAL (`/for/electricians`; `/for/electrician` → **404**).

| # | New (standard classes) | Replaces (file:line) | Verdict | Why | Adaptation |
|---|---|---|---|---|---|
| 7b.1 | kit `Breadcrumb` (Home → Trades → {trade}) | `:49-55` ("All trades" back-link with a rotated `ArrowRight`) | ADOPT-STANDARD | No BreadcrumbList on 45 pages; a rotated arrow is a back button, not a breadcrumb | The `/for` link survives inside the breadcrumb, so no link is lost |
| 7b.2 | hero primary → `#book`, secondary → `#calculator` or `/cis-refund` | `:64` → `/contact`; `:68` → `/cis-refund` | ADOPT-STANDARD | **The single highest-value edit in this slice.** §0.6: 24 sessions, 2 CTA clicks, **0 form views**, with a working `LeadForm` at `:237` that nothing points at. Both hero CTAs currently leave the page. One line × 45 pages | `data-cta="for_hero_book"`, `placement="hero"`, `goal="form"`. `/contact` is chrome, so the swap costs **0** unique links (T14) |
| 7b.3 | `type.stats` → white `StatsCounter` strip `py-5 sm:py-7` with `ExampleFigureNote` | `:78-89` (`bg-orange-600` full-bleed band, `text-white font-mono` values, `text-orange-100` labels) | ADOPT-STANDARD | §9: a full-bleed brand band is decoration, not meaning. White on orange-600 = **3.56** — passes the 3:1 large-text floor for the `text-2xl font-bold` value but the `text-xs uppercase` orange-100 label is normal text and orange-100-on-orange-600 is **not in `BRAND_LAYER.md`'s measured table**, so it must be hand-computed before it ships | §5 rule 6: every figure-bearing visual takes `ExampleFigureNote`. T15: if `StatsCounter` is used, the SSR frame must be the true value |
| 7b.4 | "24-hour response guarantee" | `:225` | DELETE | **TD-13**, × **45 pages**: a turnaround promise escalated to "guarantee", and false under the pool model — `privacy-policy/page.tsx:127-136` discloses up to six independent firms and that a firm may take 48 hours | Replace with what happens: "a specialist firm will be in touch". Owner's word (LIVE_DEFECTS §6 item 4) = T-G11 |
| 7b.5 | "Fixed fees, quoted before we start" | `:226` | GATE | TD-18 shape: under the pool model the partner firm sets the fee. Not listed in TD-18's three cited lines, so this is a **fourth instance** found by reading the template | Bundle into T-G11's rewording |
| 7b.6 | testimonial block: `Quote` icon retired, `Eyebrow "{trade} snapshot (anonymised)"`, card ground opposes section | `:140-159` (`bg-neutral-50` section, white card, decorative `Quote` at `:145`, `max-w-3xl mx-auto` clamp) | KEEP-PAYLOAD-RESTYLE | §I allows anonymised testimonials (role + detail + initials). Clamp removal per §6a | **`:154` "Composite snapshot based on client patterns"** asserts a client base — the §I ban. Rewrite to describe the mechanics ("an illustrative CIS position, figures anonymised; the tax mechanics are real"). T-G12: the owner may want all 45 `testimonial` payloads audited, not just the footnote |
| 7b.7 | kit `FaqSection` + `buildFaqPageJsonLd` on the same array | `:162-201` (hand-rolled `<details>` + `buildFaqJsonLd` at `:165`, `max-w-3xl mx-auto`) | ADOPT-STANDARD | §4b bans the hand-rolled accordion; TD-27 records **three** FAQPage builders on this site and this page uses the `schema.ts` one. T8: verify the kit accordion sets `hidden` on collapsed content and does NOT `forceMount` | One binding → both render and schema (T17). Section moves ABOVE `#book` so the canonical tail is panel, FAQ, footer (§9) |
| 7b.8 | `NextStepOffer` kept | `:206`; `src/components/intent/NextStepOffer.tsx` | KEEP | It SSRs (verified in the rendered DOM of `/for/electricians`: an `<aside>` with `href="/calculators/cis-refund-estimator"`) and is **1 of the 6 unique links** that make this family's floor of 20. Its docstring says "control arm sees nothing", so the 20th link is emitted by a conditional surface | Add `data-cta-placement` (absent) and a real `data-cta-goal` for the tool arm (`undefined` today) — T22. **Acceptance test must assert the calculator link is present in the static HTML of all 45 pages**, or an intent-logic change silently drops 45 links |
| 7b.9 | `<div id="book" class="scroll-mt-24">` + `LeadCTAPanel` navy | `:211-241` (navy grid, white card, bare `LeadForm` at `:237`; `✓` glyph in a `div` at `:229`) | ADOPT-STANDARD | §D.3. The `✓` in a bare `div` is a text tick doing semantic work → kit `DrawnTickList` with `tickClassName` (REQUIRED on light) | `redirectOnSuccess` TRUE. The proof-point list is where TD-13/TD-18 live (7b.4, 7b.5) |
| 7b.10 | "Other trades" `slice(0, 5)` | `:250-265` | KEEP | 5 of the 6 unique links on every page of the family; `slice()` here is a static render, not a client pager, so §4e does not apply | Cards → `rounded-xl ring-1`. Never reduce below 5 (T14) |
| 7b.11 | NET-NEW `ProcessTimeline` in "How we help" | `:119-129` (3-card grid) | GATE | §4a: every section carries a visual, and §5 "a grid says N equivalent things" — "what we do for you" is a sequence, not a set | T-G13: the 3 `howWeHelp` titles exist ×45 but the sequence copy does not. A figure must re-present the section's own copy and never invent a fact (§5 rule 1) |

## 8. Locations

### 8a. `/locations` index — `src/app/locations/page.tsx` (198 lines)

| # | New (standard classes) | Replaces (file:line) | Verdict | Why | Adaptation |
|---|---|---|---|---|---|
| 8a.1 | navy motif hero, `Eyebrow onDark` replacing the orange pill | `:110-135` (`bg-neutral-900 py-16 sm:py-20`, `bg-orange-500` pill) | ADOPT-STANDARD | §F.1; standing conversions 1,3,5 | NET-NEW hero CTA → `#book` |
| 8a.2 | `CityCard` photo tiles → text cards `rounded-xl bg-slate-50 p-6 sm:p-8 ring-1 ring-slate-200/70` | `:38-84` (`next/image` hero photo, or an `orange-500→orange-700` gradient + `HardHat` fallback) | ADOPT-STANDARD | A brand-gradient tile with a decorative icon is decoration standing in for content; the standard has no photography. Removing it also drops 25 images off the index | Keep `region` + a real payload line so the card is worth reading without the photo. **T-G14: confirm no Pexels attribution duty survives image removal** — the per-city credit lives at `[slug]/page.tsx:107-127`, not here |
| 8a.3 | chips `rounded-xl border-2 min-h-[44px]` | `:86-95` (`px-4 py-3 text-sm`, ~42px) | ADOPT-STANDARD | §A.8 touch floor | All **25** city links stay rendered; this page is the locations tier's only hub |
| 8a.4 | `max-w-6xl mx-auto` body clamp | `:139` | DELETE | §6a body-clamp ban; `siteContainerLg` is already the outer measure, so this clamp narrows the page inside its own container | — |
| 8a.5 | `#book` `LeadCTAPanel contained` | `:141-157` (empty-state `/contact` link only; no ask in the populated state) | NET-NEW | Property's recorded rationale: someone whose town has no page still converts rather than bouncing off a list that excludes them | `contained`; 1 session on this family in 19 days, so standard treatment only |

### 8b. `/locations/[slug]` — 25 routes, one template (338 lines), data `src/app/locations/[slug]/data.ts` (1,943 lines)

| # | New (standard classes) | Replaces (file:line) | Verdict | Why | Adaptation |
|---|---|---|---|---|---|
| 8b.1 | navy motif hero `py-12 sm:py-16`; photo + scrim + Pexels credit DELETED | `:95-140` (`h-[420px] sm:h-[500px]` `priority` image, 3-stop gradient scrim, two `rel="noopener nofollow"` outbound credit links) | ADOPT-STANDARD | The standard has no photography; this also removes a `priority` LCP image from 25 pages and two followed-pattern outbound links the site never decided to publish (T27 family) | `city.region` → `Eyebrow onDark` (retires the `bg-orange-500` pill at `:319`); h1 orange-400 span at `:325` dropped; NET-NEW hero CTA "Speak to a CIS specialist in {city}" → `#book` |
| 8b.2 | body sections at FULL container | `:145`, `:161`, `:189`, `:221`, `:262` (five `max-w-4xl mx-auto` clamps) | DELETE | §6a, five times on 25 pages | One of the five wraps the FAQ, one the nearby-areas strip |
| 8b.3 | `majorProjects` cards: `border-l-4 border-l-orange-500` → `rounded-xl ring-1`, ground opposing | `:166-178` | KEEP-PAYLOAD-RESTYLE | `border-l-4 border-[var(--brand-primary)]` is reserved for the calculator tool body (§E); a project card is not a tool | Payload verbatim |
| 8b.4 | `tradeMix` chips → `rounded-full bg-white ring-1 ring-slate-200 text-xs font-semibold text-slate-600` | `:197-211` (`bg-stone-50` chips with `hover:bg-orange-50 hover:text-orange-700`) | KEEP-PAYLOAD-RESTYLE | orange-on-orange hover retired; the 10 links are the page's entire unique-link budget | **Verified, against the Solicitors dead-link trap: all 25 × 10 `tradeMix` entries resolve.** `tradeMix` values are already lowercase hyphenated slugs, and `t.toLowerCase().replace(/\s+/g,"-")` (`:78`) maps every one of the 250 onto a real `trade-types.ts` slug — **0 bad** (script-derived). No dead links here. Keep the derivation but add a build-time guard, because the mapping is string surgery over two unlinked files |
| 8b.5 | kit `FaqSection` + `buildFaqPageJsonLd` | `:218-256` (hand-rolled `<details>`, `buildFaqJsonLd` at `:75`) | ADOPT-STANDARD | §4b; TD-27 builder collapse | Same array → render + schema |
| 8b.6 | the £2,000 client-average claims | `data.ts:429,472,503,577,616,651,726,773,879,1187` | GATE | **TD-10**, 10 city pages: "averaged around £2,000 across Trade Tax Specialists' client base" / "Our clients typically recover…" ×4 / "Our clients' average refund…" ×2 / "The average CIS refund we see…" / "across our client base" / "typical for first-year clients". Ten client-outcome claims, and they **misattribute a third-party marketing figure** (§13 sources it to Dearne £1,840 and RIFT) as our own results | Owner's word (LIVE_DEFECTS §6 item 2) = T-G10. The compliant template already exists on site at `content/blog/how-long-does-cis-refund-take.md:38,109`. Do **not** fix unilaterally, and do **not** bulk-sed: the ten wordings differ |
| 8b.7 | `priceRange` struck from the LocalBusiness JSON-LD | `src/lib/schema.ts:71` and `:200` (consumed at `[slug]/page.tsx:56`) | DELETE | **TD-01**: `priceRange: "££"` is a published advisory price signal in structured data on all 25 city pages plus the site-wide Organization block. `schema.ts` is per-site; the shared copy at `packages/web-shared/schema/local-business.ts:127` is **TD-K1 and NOT this slice's to touch** | T-G15 + an orchestrator handoff. Fixing only Trade's two forks leaves the kit line live for every other consumer, and fixing only the kit leaves Trade's forks live (T23) |
| 8b.8 | schema enrichment otherwise KEPT | `:56-72` (`serviceTypes[]`, `areaServed` + `nearbyAreas`, `geo`, region) | KEEP | Richer than Property's; no telephone, correctly | Keep wholesale minus 8b.7 |
| 8b.9 | `<div id="book" class="scroll-mt-24">` + `LeadCTAPanel contained`; `redirectOnSuccess` TRUE | `:275-302` (navy grid, bare `LeadForm` at `:298`, `btnPrimary` → `/contact` at `:290`) | ADOPT-STANDARD | §D.3. Re-verify the rendered form ground (D.1 invariant) rather than assuming the white card is enough | `contained` keeps a navy band off the navy footer now that there is no FAQ below it — or keep navy and move the FAQ (8b.5) after it. Pick one explicitly |
| 8b.10 | NET-NEW `RelatedArticles columns={3}` + adjacent-city cross-link | — (`nearbyAreas` render as plain text at `:266-268`) | GATE | §F.1 and the link floor: this family's 10 unique links are all `/for/*`, so it contributes nothing to the blog or to sibling cities | T-G16: which hub is the fallback. `nearbyAreas` are towns without pages, so they cannot become links — the cross-link must be to sibling CITY slugs |

## 9. Glossary

### 9a. `/glossary` index — `src/app/glossary/page.tsx` (132 lines)

| # | New (standard classes) | Replaces (file:line) | Verdict | Why | Adaptation |
|---|---|---|---|---|---|
| 9a.1 | navy motif hero, `Eyebrow onDark` replacing the orange pill | `:53-77` (`py-16 sm:py-20`, `bg-orange-500` pill) | ADOPT-STANDARD | §F.1; standing conversions 3,5 | NET-NEW hero CTA → `#book` |
| 9a.2 | "All figures verified for **2025/26**" | `:72` | ADOPT-STANDARD | The site's ground truth is **2026/27** throughout (55p AMAP from 6 Apr 2026, dividend rates from 6 Apr 2026, FA 2026 enacted). A "verified for 2025/26" stamp on 50 definitions dates the asset by a year on the page that exists to be cited | Not in LIVE_DEFECTS — found by reading the rendered hero. Bundle with the glossary content fixes; state the year the data is actually on |
| 9a.3 | `max-w-6xl mx-auto` clamp removed; `bg-stone-50` → `bg-slate-50`; `h2` with `pb-3 border-b` → `Eyebrow` + h2 + explicit ground oscillation | `:80-82`, `:104-106` | ADOPT-STANDARD | §6a; §9. A ruled-underline h2 is the pre-redesign recipe | All **50** links stay rendered; this page is the glossary tier's only hub (64 links, the highest in the site) |
| 9a.4 | `hover:bg-white` on a `bg-white` card | `:112` | DELETE | A no-op hover state: the card is already white. Cosmetic, but it is the tell that the card ground was never checked against its section (§4a rule 3) | Card ground becomes white on a slate-50 section, hover on the ring |
| 9a.5 | `#book` `LeadCTAPanel contained` | — (ask exists only in the `entries.length === 0` empty state at `:92-98`, which is unreachable with 50 entries) | NET-NEW | 9 sessions, 0 CTA clicks. The populated state has no ask at all | `contained` |

### 9b. `/glossary/[slug]` — 50 routes, one template (148 lines), data `src/app/glossary/[slug]/data.ts` (959 lines)

| # | New (standard classes) | Replaces (file:line) | Verdict | Why | Adaptation |
|---|---|---|---|---|---|
| 9b.1 | navy hero `py-12 sm:py-16`, `Eyebrow onDark` = category | `:71-91` (`bg-orange-500` category pill) | ADOPT-STANDARD | Standing conversion 5 | `Breadcrumb` already present at `:73` and already emits via the local component — swap to kit for the BreadcrumbList |
| 9b.2 | `max-w-3xl mx-auto` body clamp removed | `:96` | DELETE | §6a | `entry.body` is `dangerouslySetInnerHTML` on `.article-body.prose-blog`; the prose class governs the measure, so removing the wrapper clamp is safe |
| 9b.3 | mid-page navy CTA card → the D.3 mid-page pattern (`rounded-xl bg-white p-6 ring-1 ring-slate-200 sm:flex …`) pointing at `#book` | `:103-115` (`bg-neutral-900 p-8` card with a bare `btnPrimary` → `/contact`) | ADOPT-STANDARD | §D.3 "a bare button is not a CTA block"; the statement must be specific to its section. A navy card dropped into a white article is the decoration case §9 warns about | `/contact` is chrome → **0** unique links lost |
| 9b.4 | related terms `slice(0, 3)` kept, cards restyled | `:118-142` (`bg-stone-50` cards on a white section) | KEEP | 3 of the 4-5 unique links on every page of the family (T14) | Ground is already opposing — correct. `text-orange-600` → `text-orange-700` |
| 9b.5 | `DefinedTerm` JSON-LD kept; `BreadcrumbList` added | `:59-68` | KEEP | `DefinedTerm` + `inDefinedTermSet` is the right schema and better than nothing elsewhere in the slice | `Breadcrumb` (9b.1) brings the BreadcrumbList for free |
| 9b.6 | s.62B penalty quantum | `data.ts:518` "20% of the sums treated as paid on a return (FA 2004 s.62B)" | DELETE | **TD-08**: `house_positions.md` §3 has a dedicated sentence — "s.62A = 20% of the payment; s.62B = the full sum returned. **Never write '20%' for s.62B**". The page understates exposure **fivefold**, and the neighbouring entry at `:540-552` is CORRECT, so one data file contradicts itself | Replace with "an amount equal to the sum the return treats as paid on account of the person's liabilities (100% of the sum returned)". Fix the s.62A half in the same edit so the two quanta read as different |
| 9b.7 | CIS300 12-month penalty tier | `data.ts:359`, `:377`, `:432` "£300 or 100% of the CIS liability at twelve months" | DELETE | **TD-09**: §4's ladder is £100 / £200 / £300-or-5% / £300-or-5%, with the 100% layer an **additional** charge for deliberate withholding only (capped "up to £3,000 or 100%"). Overstates a statutory penalty twentyfold, three times, to the contractor audience. The blog cluster received the 2026-06-12 correction; the glossary did not | Reproduce §4's ladder verbatim in all three places, with deliberate withholding as a separate tier. `cis-penalty-calculator`'s own copy is clean (0 em-dashes, correct ladder) — use it as the reference so page and tool cannot disagree |
| 9b.8 | NET-NEW `#book` `LeadCTAPanel contained` + `FaqSection` | — (no form anywhere on 50 pages) | ADOPT-STANDARD (id = GATE) | 50 reference pages with no ask. `FaqSection` is the GATE half (questions to author per entry is not viable × 50) | Panel: ADOPT now. FAQ: T-G17, and the honest answer may be "a glossary entry IS the answer, no FAQ" — recommend **no FAQ on this family** |

## 10. Kit-vs-local sourcing

**ADOPT KIT** (all from `@accounting-network/web-shared`, already a declared dependency, §0.1): `design/layout-utils` (retires the divergent local `src/components/ui/layout-utils.ts` tokens — **NOT the file**: it has **41 importers** per LIVE_DEFECTS §3, which explicitly records that the Medical port nearly deleted its equivalent on a bad grep; convert the token VALUES, keep the module) · `design/globals-standard.css` · `design/primitives/{page-blocks,FaqSection,accordion,Breadcrumb,Eyebrow+EyebrowRule,ExampleFigureNote,NoticeCard,SlimHero,NumberedPagination}` · `design/marketing/{TopicSection,LeadCTAPanel,CoverageCards,ProcessTimeline,DrawnTickList,StatsCounter,ComparisonTable,WhatToExpectCard,ScrollGlowGroup}` · `design/blog/RelatedArticles` (the only article-card grid) · `tools/components/Calculator` **including the existing `resultWrapper` prop** (§0.2).

**KEEP** (live, correct, not Trade's to retire): `components/ServiceTiers` — 3 importers (`app/page.tsx`, `app/services/page.tsx`, `config/service-tiers.ts`), live shared kit; the rollout doc's §4.2 "dead component" refers to Property's unused LOCAL copy and the doc contradicts itself at :302 · `components/StatsBar` — 3 importers, a 27-line pure function with no counter and therefore no T15 exposure (LIVE_DEFECTS §5.2 corrects the brief here) · `tools/{types,registry-helpers,format,embed/EmbedSnippet,embed/EmbedAutoResize}` · `leads/MiniCapture` · `src/components/resources/{CalculatorPageResources,ResourceGate}` — live and rendered, post-2026-07-18 no-email-unlock, so carve-out 2 is intact · `src/components/intent/NextStepOffer` — SSRs, and is 1 of the 6 unique links holding the `/for/[slug]` floor · `src/components/calculators/premium/*` — reachable via blog only (§0.3), restyle tokens/radius only, do not retire.

**BUILD LOCAL** (Property is the source, orange ramp + T-W1): `src/components/calculators/CalculatorTabs.tsx` ← `Property/web/src/components/calculators/CalculatorTabs.tsx` (absent from the kit — verified by `ls packages/web-shared/design/marketing`) · `src/components/calculators/{ResultGate,HeldResult,ResultGateModal}.tsx` + `resultGateStorage.ts` ← the four Property files of the same names (Trade's existing `premium/ResultGateModal.tsx` is a DIFFERENT component for the blog gate; name the new one `ResultGate`-scoped to avoid a collision) · `src/components/trade/TradeBackdrop.tsx` (the motif; `aria-hidden` inline SVG, `absolute inset-y-0 right-0 w-[55%] hidden sm:block`, navy stroke-opacity .18 mask `to left, black 35% → transparent 92%`) · `src/lib/page-summaries.ts` (§I: one sentence per non-article route, read by the owning page's hero AND every card referencing it, with a first-sentence guard test) · `src/tests/calculator-tabs-crawl-path.test.ts`.

**RETIRE** (call-site removal, file fate stated): `src/components/calculators/CalcResultCta.tsx` — **importer grep: `src/app/calculators/[slug]/page.tsx:83` (1 render site) + `src/components/calculators/premium/PremiumCalculator.tsx` + 2 test files (`src/tests/{extras-qualifiers,lead-payload}.test.ts`)**. Because `PremiumCalculator` imports it and the tests pin its payload, this is a **GATE, not a retirement** (T-G3); the file survives for the premium path regardless of the calculator-route decision · the local `src/components/ui/Breadcrumb.tsx` call sites in this slice (`calculators/[slug]:66`, `calculators/page.tsx:52`, `locations/page.tsx`, `locations/[slug]:311`, `glossary/page.tsx:55`, `glossary/[slug]:73`) move to the kit component; **the file itself has call sites outside this slice — grep before deleting** · `.section-label` as an eyebrow (call sites in this slice: `services:113,147,171`, `for/page:22,96`, `for/[slug]:56,94,115,215`, `cis-refund`, `gross-payment-status`) → kit `Eyebrow`; the CSS class survives while other slices still use it (T26: map it, do not leave a visible regression across phases).

**DELETE:** nothing in this slice. `src/components/experiments/useExperiment.ts` (TD-26, zero importers) and the dormant `packages` CTA pricing (TD-02) are real deletions but belong to chrome/config, not here.

## 11. Per-route link-floor arithmetic (from `link_baseline.json`, sha `18b4f25f`)

Chrome baseline = **14** internal page links, derived: `/about` renders 16 `href="/…"` of which `/_next/static/chunks/webpack-*.js` and `/_next/static/css/*.css` are assets; `link_baseline.json` gives `/about: 14`, `/contact: 14`, `/terms: 14`, `/privacy-policy: 14`, `/cookie-policy: 14`. The 14 destinations are `/`, `/about`, `/blog`, `/calculators`, `/cis-refund`, `/contact`, `/cookie-policy`, `/for`, `/glossary`, `/gross-payment-status`, `/locations`, `/privacy-policy`, `/services`, `/terms`. **Every one of those is already in the chrome, so any body link to any of them adds ZERO unique links (T14).** "Δ" below = measured set-difference against that chrome set.

| Family | Routes | Floor (each) | Arithmetic | Δ unique body links | Binding consequence |
|---|---|---|---|---|---|
| `/calculators/[slug]` | 12 | **14** | 14 + 0 | **0** | Nothing to protect. The 1.10 `RelatedArticles` and the 6b.7 tabs crawl link are pure gains. A body link to `/contact` or `/calculators` would still score 0 |
| `/calculators` | 1 | **27** | 14 + 12 tools + `/embed` = 27 ✓ | 13 | All 12 tool cards AND the `/embed` tail link are load-bearing. Tier-2 recategorisation must not drop a card; `CalculatorTabs` emits `<button role="tab">` and contributes **0**, hence the mandatory literal `<a>` |
| `/embed`, `/embed/[slug]` | 1 + 12 | **none** | absent from the baseline; `robots index:false` | 12 / 0 | No floor. Measured 26 and 14 for the record |
| `/services` | 1 | **14** | 14 + 0 | **0** | `/for` and both `/contact` links are chrome. Removing them costs nothing; 6b.7-style tabs + a literal calculator `<a>` is the only way this page's floor rises |
| `/cis-refund` | 1 | **14** | 14 + 0 | **0** | as above (`/gross-payment-status`, `/for` are chrome) |
| `/gross-payment-status` | 1 | **14** | 14 + 0 | **0** | as above (`/cis-refund`, `/for` are chrome) |
| `/for` | 1 | **59** | 14 + 45 trade pages = 59 ✓ | 45 | All 45 stay rendered. The `businessSegment` block at `:62` is conditional on a non-empty filter — if the segment split changes, the hub silently sheds links |
| `/for/[slug]` | 45 | **20** | 14 + 5 "other trades" + 1 `NextStepOffer` calculator link = 20 ✓ | 6 | **The most fragile floor in the slice.** 5 come from `slice(0,5)` at `:252`; the 6th comes from a conditional personalisation component (7b.8). Guard both. `/for` (breadcrumb), `/cis-refund` (hero secondary), `/services`, `/contact` all score 0 |
| `/locations` | 1 | **39** | 14 + 25 cities = 39 ✓ | 25 | Deleting the photo tiles (8a.2) must keep all 25 anchors |
| `/locations/[slug]` | 25 | **24** | 14 + 10 `tradeMix` = 24 ✓ | 10 | The entire budget is the 10 trade chips, and all 250 resolve (8b.4). `nearbyAreas` are plain text and must NOT be converted to links — those towns have no pages |
| `/glossary` | 1 | **64** | 14 + 50 terms = 64 ✓ | 50 | Highest in the site. Category regrouping must not drop a term |
| `/glossary/[slug]` | 50 | **18** or **19** | 14 + 3 related + 1-2 inline `/blog/*` links in `entry.body` = 18/19 ✓ | 4-5 | `related` is `slice(0,3)` on same-category siblings (`:54-56`); a category with <4 entries yields fewer. The 18-vs-19 split is inline body links, which vary per entry — **the floor is per-page, so it must be re-measured per page, not per family** |

Slice totals for the verification gate: 12 + 1 + 1 + 1 + 1 + 1 + 45 + 1 + 25 + 1 + 50 = **139 of the baseline's 246 routes**, carrying 14×12 + 27 + 14×3 + 59 + 20×45 + 39 + 24×25 + 64 + 18.x×50 ≈ **3,020 of 5,301** internal links.

## 12. SLICE 2 OWNER GATES

1. **T-G1 — T-W1 warning ladder sign-off.** Blocks: `--calc-warn-*` token definitions (1.11), the penalty-calculator ladder, every future `tone:"warn"`. Already PROPOSED at `BRAND_LAYER.md:913`; nothing here re-derives it.
2. **T-G2 — ResultGate on all 12 calculator routes (a new capture surface).** Blocks: WP-B entirely. Note Generalist's G22 blocker does NOT apply: Trade's index makes no "no email gate" promise (§5).
3. **T-G3 — keep or retire `calc_result` / `CalcResultCta`.** Property deleted it estate-wide 2026-08-22; it produced Trade's only calculator lead on 2026-08-20 and `FUNNEL_BASELINE.md:132` forbids deleting it as dead. Blocks: the result-column layout and 2 test files.
4. **T-G4 — keep or retire the `resource_block` island on calculator pages.** Renders fine, 0 leads in-window. Blocks: whether a calculator page carries one ask or two.
5. **T-G5 — commission `workedExamples` for the 10 tools without them.** Blocks: 1.7 on 10 of 12 routes.
6. **T-G6 — commission `related[]` for all 12 tools.** Blocks: 1.10, and the only route to lifting the calculator family off a 0 unique-link floor.
7. **T-G7 — `/calculators` categories: list the 5 real ones, or merge `CIS Comparisons` (1 tool) into `CIS Basics`.** Blocks: 2.3/2.4.
8. **T-G8 — `/calculators` index FAQ: author, or ship `LeadCTAPanel contained` and no FAQ.** Blocks: 2.6/2.7 ordering.
9. **T-G9 — FAQ commissions for `/services`, `/cis-refund`, `/gross-payment-status`.** Blocks: 6a.8, 6b.6, and the §9 navy-panel ordering on all three.
10. **T-G10 — the £2,000 refund figure: reattribute as a third-party illustrative average, or publish a real derived number.** Covers TD-10 (10 city pages) and TD-11 (`/cis-refund` metadata + every blog sidebar). Blocks: 6b.8, 8b.6. Owner's word already requested at LIVE_DEFECTS §6 items 2-3.
11. **T-G11 — remove all 18 response-time promises, 4 of them "guarantee".** In this slice: `for/[slug]:225` ×45, `service-tiers.ts:71`, `cis-refund:230`, `gross-payment-status:240`, plus TD-18's fixed-fee trio and the fourth instance at `for/[slug]:226`. Blocks: 7b.4, 7b.5, 6a.6.
12. **T-G12 — audit all 45 `testimonial` payloads in `trade-types.ts` against §I, or fix only the "client patterns" footnote.** Blocks: 7b.6.
13. **T-G13 — `ProcessTimeline` copy for `/for/[slug]` "How we help".** A figure may not invent a fact (§5 rule 1), so this is authored copy, not a restyle. Blocks: 7b.11.
14. **T-G14 — confirm no Pexels attribution duty survives deleting the city photography.** Blocks: 8a.2, 8b.1.
15. **T-G15 — strip `priceRange: "££"` (TD-01, 26 surfaces) and hand TD-K1 to the orchestrator.** Both ends or neither (T23). Blocks: 8b.7.
16. **T-G16 — which hub is the `/locations/[slug]` `RelatedArticles` fallback, and do sibling-city cross-links ship.** Blocks: 8b.10.
17. **T-G17 — FAQ on `/glossary/[slug]` × 50: recommend NO (the entry is the answer).** Blocks: 9b.8's second half only.
18. **T-G18 — `redirectOnSuccess` flips to TRUE on 45 `/for/[slug]` + 25 `/locations/[slug]` + `/services`.** A measurable funnel change across 71 routes; `false` currently skips the booking-picker step. Blocks: 6a.7, 7b.9, 8b.9.
19. **T-G19 — `/glossary` "All figures verified for 2025/26" → the year the data is actually on (2026/27).** Not a LIVE_DEFECTS entry; found in the rendered hero. Blocks: 9a.2.

Standing verifications, not gates: the D.1 rendered-form-ground check on every capture surface in this slice (`for/[slug]:235-237`, `locations/[slug]:294-298`, `cis-refund:247-249`, `gross-payment-status:264-266` — all four are white cards inside navy bands, which LOOKS right and must still be measured, per §D.1 "RE-VERIFY on every sibling rather than assuming either way"); the full `data-cta` attribute-set diff, rendered-vs-rendered, id AND placement AND goal (T22); per-page link floors re-measured per page, not per family (§11).

## 13. Work packages

**WP-A — tokens, palette and the em-dash register (no layout).** Files: `src/app/globals.css` (add `--warn-1..4`, `--warn-n-on-dark`, `--form-error`, `--calc-warn-bg/fg/accent`); `src/lib/calculators/tools/{cis-back-years-calculator,cis-vs-paye-comparison,cis-deduction-calculator,cis-invoice-splitter,cis-take-home-calculator}.ts` (35 user-facing em-dashes, per §4's per-line table); `src/config/service-tiers.ts:70`. Property source: `Property/web/src/app/globals.css`. Spec: `BRAND_LAYER.md` §4.3/§5.2/§5.3; rollout §A.1; §I. Acceptance: `node docs/_engines/instruments/sweep.mjs` → `totalDashes == 2` with `cis-self-assessment-calculator.ts:125` and `cis-vs-paye-comparison.ts:113` named as the two protected en-dashes; `grep -o '—' src/lib/calculators/tools/*.ts | wc -l` == 4 and all 4 on `// YEAR N —` comment lines; `grep -rn 'text-red-600' src` == 0; `python scripts/check_dependency_closure.py`. Depends on: T-G1. Blocks: every other package (the ramp is a prerequisite for any tone decision).

**WP-B — the calculator fleet: `[slug]` template + index + embeds + the ResultGate port.** Files: `src/app/calculators/[slug]/page.tsx`, `src/app/calculators/page.tsx`, `src/app/embed/page.tsx`, `src/app/embed/[slug]/page.tsx`, NEW `src/components/calculators/{ResultGate,HeldResult,ResultGateModal,CalculatorTabs}.tsx` + `resultGateStorage.ts`, NEW `src/tests/calculator-tabs-crawl-path.test.ts`, `src/components/resources/CalculatorPageResources.tsx` (chip token only). Property source: `Property/web/src/components/calculators/{ResultGate,HeldResult,ResultGateModal,CalculatorTabs}.tsx` + `resultGateStorage.ts`; `Property/web/src/app/calculators/[slug]/page.tsx` and `page.tsx`. Spec: rollout §F.5, §G, §D.2, §D.3; DESIGN_SYSTEM §0.4, §6, §6a. Acceptance: all 12 slugs still HTTP 200 and each appears in `generateStaticParams`; `/calculators` renders ≥27 internal links including a literal `<a href="/calculators/cis-refund-estimator">`; `/calculators/[slug]` renders ≥14; every `/embed/[slug]` renders an UNGATED result and 0 `resultCta`; the gate's held node carries `inert` + `aria-hidden` and every dismiss path reveals; `result_gate_skip` fires with the byte-identical id; **T4: no function prop crosses the server/client boundary — `resultWrapper` is applied INSIDE `Calculator` (a client module) and the server page passes only `slug` and React nodes, so a real build is the only proof and the manager runs it**; `python scripts/check_dependency_closure.py`. Depends on: WP-A, T-G2, T-G3, T-G4, T-G7. **Heaviest client-side package in the slice — 12 routes, a new tablist, a new gate, all client components.**

**WP-C — the trade family: `/for` + `/for/[slug]` × 45.** Files: `src/app/for/page.tsx`, `src/app/for/[slug]/page.tsx`, `src/data/trade-types.ts` (`:225`-equivalent payload strings only), `src/components/intent/NextStepOffer.tsx` (add `data-cta-placement`/`goal`). Property source: `Property/web/src/app/section-24/page.tsx` + `components/property/Section24Wedge.tsx` (the pillar + its figure sibling; note the sibling is `Section24Wedge.tsx`, there is no `section-24-figures.tsx`). Spec: rollout §F.1, §D.1/D.3; DESIGN_SYSTEM §4a, §5, §9. Acceptance: all 45 routes render ≥20 internal links, asserted **per slug**, including `/calculators/cis-refund-estimator` from `NextStepOffer` in the STATIC html; hero primary resolves to `#book` and the `#book` div carries `scroll-mt-24`; `grep -rn '24-hour response guarantee' src` == 0; the FAQ accordion sets `hidden` on collapsed items (T8, not a text-presence proxy); the FAQ render array and `buildFaqPageJsonLd` array are one binding (T17); `python scripts/check_dependency_closure.py`. Depends on: WP-A, T-G11, T-G12, T-G13, T-G18. **One template edit covers 45 pages — do not propose touching 45 files.**

**WP-D — the three pillars: `/services`, `/cis-refund`, `/gross-payment-status`.** Files: those three `page.tsx`, `src/config/service-tiers.ts`, `src/lib/calculators/tools/cis-gps-eligibility-checker.ts` (`:157,169`), `src/tests/assistant-journey-opener.test.ts:706-711` (repoint the TD-07 guard). Property source: `Property/web/src/app/services/page.tsx` + `Property/web/src/app/services/property-accountant/page.tsx`. Spec: rollout §F.1, §H, §I; DESIGN_SYSTEM §4b, §9. Acceptance: `grep -rn '30% of the tax' src` == 0 and `grep -rn 'Finance Bill 2026' src` == 0 (**all four TD-05 sites and all three TD-06 sites in ONE commit**, T23); the repointed guard FAILS when the string is reintroduced (T9 — prove the guard bites); `featuredBadge=""` renders no badge, verified against `ServiceTiers.tsx`'s own default (T13); all three routes render ≥14 links and ≥15 once the literal calculator `<a>` lands; `/services` renders a `#book` form; `python scripts/check_dependency_closure.py`. Depends on: WP-A, WP-B (for the tabs component), T-G9, T-G10, T-G11. **1 session in 19 days: standard treatment, no heroics, no new figures.**

**WP-E — locations + glossary, and the two understated statutory penalties.** Files: `src/app/locations/page.tsx`, `src/app/locations/[slug]/page.tsx`, `src/app/locations/[slug]/data.ts` (10 TD-10 lines), `src/app/glossary/page.tsx`, `src/app/glossary/[slug]/page.tsx`, `src/app/glossary/[slug]/data.ts` (`:518` s.62B; `:359,377,432` CIS300), `src/lib/schema.ts:71,200` (TD-01). Property source: `Property/web/src/app/locations/[slug]/page.tsx` and `Property/web/src/app/locations/page.tsx`. Spec: rollout §F.1, §I; DESIGN_SYSTEM §6a, §9; `house_positions.md` §3 and §4. Acceptance: 25 city routes ≥24 links each with all 10 `tradeMix` anchors resolving to a real `trade-types.ts` slug (a build-time guard over the 250 pairs, not a spot check); 50 glossary routes at or above their OWN measured floor (18 or 19, per page); `grep -rn 'priceRange' src` == 0; `grep -n '20% of the sums treated as paid' src/app/glossary/[slug]/data.ts` == 0; the CIS300 ladder in `data.ts` byte-matches `cis-penalty-calculator.ts`'s ladder; 0 `next/image` on the locations family and 0 Pexels outbound links; `python scripts/check_dependency_closure.py`. Depends on: WP-A, T-G10, T-G14, T-G15, T-G16, T-G17, T-G18. **TD-08 and TD-09 are the only live-false-statement fixes in the slice that are NOT owner-gated — they are statute, and `house_positions.md` already records the correct figures.**

Sequencing: WP-A first and alone (it is the ramp every other package reads). Then WP-B and WP-E in parallel (no shared files). Then WP-C (independent of B, but it wants the `data-cta` conventions WP-B settles). WP-D last, because it consumes WP-B's `CalculatorTabs` and shares `service-tiers.ts` with slice 1. **Cross-slice collision to flag to the manager: `src/config/service-tiers.ts` and `src/components/ui/layout-utils.ts` are touched by slice 1 as well as by this slice; `src/lib/schema.ts` is touched by whichever slice owns the homepage Organization block. Serialise those three files or the ports fight.** T1: builders never build; the manager builds, serially, and checks `BUILD_ID` mtime against the newest source file before trusting a green run (T2).

## 14. Risks and ambiguities, with my recommended resolution

1. **The gate ships onto a family with 7 sessions in 19 days.** Recommendation: ship it anyway — it is the standard, it is cheap, and the calculator family is the only surface on this site that has ever produced a calculator-sourced lead. But set the expectation that no after-reading will be available for months at this traffic, so the gate cannot be judged on Trade's own numbers.
2. **`calc_result` vs the gate (T-G3).** Recommendation: keep `calc_result`, move it BELOW the revealed result, and let the gate carry the primary ask. Deleting a surface that produced a lead 22 days ago, on a site with three leads in its entire history, to match a Property decision taken at 100× the traffic, is the wrong trade.
3. **`RelatedArticles` on calculator routes needs 12 `related[]` arrays that do not exist.** Recommendation: commission them with the 82 blog posts as the source pool; it is the cheapest available lift on a 0-unique-link family, and it is data, not design.
4. **The `/for/[slug]` 20-link floor leans on a personalisation component.** Recommendation: make the calculator link unconditional markup in the template and let `NextStepOffer` stay a bonus. A floor that depends on an experiment arm is not a floor.
5. **`/glossary/[slug]` × 50 and `/for/[slug]` × 45 both want FAQs the site does not have.** Recommendation: no FAQ on the glossary (the entry IS the answer) and keep the existing 45 trade FAQ arrays, converted to `FaqSection`. Do not commission 95 new FAQ sets.
6. **TD-01/TD-K1 cannot be fixed from inside this slice.** Recommendation: fix Trade's two forks in WP-E and hand TD-K1 to the orchestrator in the same report, explicitly, because a half-fix here is exactly the T23 shape that left a sibling site broken.
7. **The contrast instrument is unusable on this site until phase 1 lands (T25).** Recommendation: take every contrast decision in this slice from `BRAND_LAYER.md` §5.2/§5.3's hand-computed table, and hand-compute the two values that table does not carry: orange-100 on orange-600 (`for/[slug]:84`) and white on orange-600 as a tab label (§5). Never hand the owner `browser_check.mjs`'s raw contrast output.
