# Phase 2, packages P2-6 + P2-7 — contractors-ir35 design port

Server titles checked before starting: both `localhost:3621` (phase 1 build) and
`localhost:3611` (pre-port) served `<title>Specialist Contractor Accountants |
IR35 Advice UK</title>`. Neither package touched rendered UI, so no re-check
after the change.

## P2-6 — delete dead `ExitIntentModal.tsx`

**Import grep** (`import.*ExitIntentModal|from.*ExitIntentModal`, across `src/`): **zero hits.**

**JSX mount grep** (`<ExitIntentModal`, across `src/`): **zero hits.**

Also checked (brief didn't ask, did anyway per the false-positive history):
dynamic-import (`dynamic(...)` files grepped for `blog/Exit`): zero hits.
Name-only grep for completeness: 5 hits total — the component's own
`export function ExitIntentModal()` line, plus 4 comment mentions in 3 other
files (`MiniCapture.tsx`, `DeepScrollModal.tsx`, `SpecialistWidget.tsx` x2).
None is an import or a mount. Verdict: dead, confirmed independently of the
brief's own count.

**Deleted:** `contractors-ir35/web/src/components/blog/ExitIntentModal.tsx`.

**Comments corrected** (all three named in the brief):
- `src/components/forms/MiniCapture.tsx` (file-header consumer list) — dropped
  `ExitIntentModal` from the "no call sites change" list, noted it was retired
  in P2-6.
- `src/components/intent/DeepScrollModal.tsx` (line ~44) — removed the "shared
  per-session cap with ExitIntentModal" claim (nothing shares it now), kept the
  cap's own rationale.
- `src/components/support/SpecialistWidget.tsx` — two spots: the file-header
  "stand-down" block (was asserting `ExitIntentModal.tsx:74` as a live reader
  of `cfp_assistant_active`, a specific line reference into a file that no
  longer exists) and the inline comment on the `useEffect` that sets the key.
  Both now say the setter is orphaned since the retirement, left in place as
  harmless and cheap to re-wire if an exit-intent surface returns. Did not
  remove the `sessionStorage.setItem("cfp_assistant_active", "1")` call itself
  — that's a behaviour change outside this package's lease (comment-only fix).

Lease respected: only the one file plus the three comment blocks were touched.

## P2-7 — `primaryKeyword` frontmatter field (report only, nothing changed)

**The 13 values** (grepped `^primaryKeyword:` across `content/blog/*.md`):

| File | Value |
|---|---|
| challenge-ir35-determination-sds.md | "inside IR35 determination" |
| contractor-day-rate-to-take-home.md | "day rate take-home" |
| contractor-pension-schemes-sipp.md | "pension schemes for contractors" |
| director-salary-dividend-split-guide.md | "salary and dividend" |
| do-i-need-an-accountant-for-mtd.md | "do i need an accountant for making tax digital" |
| fee-payer-liability-ir35.md | "fee-payer liability" |
| how-to-choose-contractor-accountant.md | "contractor accountant" |
| how-to-use-cest-tool.md | "CEST tool" |
| mileage-claims-contractor-limited-company.md | "mileage claims" |
| msc-legislation-contractors.md | "MSC legislation" |
| off-payroll-working-rules-hub.md | "off-payroll working rules" |
| off-payroll-working-rules-private-sector.md | "off-payroll working rules" |
| travel-expenses-inside-ir35.md | "travel inside IR35" |

All 13 hold a genuine non-empty topical string, none is the empty-string trap
the brief warned about (a sibling site had this exact key present-but-blank).

**Consumer trace:**
1. `src/lib/blog.ts`, `parsePostFile()` (full file read, 142 lines) — casts
   `data` to `Partial<BlogFrontmatter>` and hand-picks fields onto the
   returned `BlogPost` object (lines 19-40: title, slug, date, updatedDate,
   author, authorSlug, category, metaTitle, metaDescription, altText, image,
   imageCredit, h1, summary, keyTakeaways, sourcesVerifiedAt, schema,
   canonical, faqs, contentHtml). `primaryKeyword` is not one of the picked
   keys, so it is dropped the moment a post is parsed. Never touched again
   downstream of this function.
2. `src/types/blog.ts` — `BlogFrontmatter` type (full file read) does not
   declare a `primaryKeyword` field at all.
3. `packages/web-shared/lib/frontmatter.ts` — `assertFrontmatter()` +
   `STANDARD_MANIFEST` (full file read, the validator referred to as "the
   schema builder"; there's no separate structured-data-from-keyword builder
   in this repo — the actual JSON-LD builder is `src/lib/schema.ts`, checked
   below). `STANDARD_MANIFEST` only requires `slug`, `title`, `date`,
   `category`, `metaDescription`. No mention of `primaryKeyword`.
4. `src/lib/schema.ts` (full file read, 313 lines, every `build*JsonLd`
   function) — `buildBlogPostingJsonLd()` reads `post.h1`, `.title`,
   `.metaDescription`, `.image`, `.category`, `.date`, `.updatedDate`,
   `.author`, `.reviewedBy`, `.reviewerCredentials`. No `primaryKeyword`
   anywhere in the file.
5. Repo-wide grep for the literal string `primaryKeyword` outside `content/`:
   zero hits in any `.ts`/`.tsx`/`.js` file under `contractors-ir35/` or
   `packages/web-shared/`. Only non-content hits are 2 Python scripts
   (`scripts/fix_meta_overflows.py`, `scripts/keyword_placement_audit.py`,
   used for editorial/audit tooling, not the runtime site) and 3 STATE/plan
   docs.

**Verdict: NOT consumed.** `primaryKeyword` never reaches metadata, the
renderer, JSON-LD schema, the sitemap, search, or any other runtime surface.
It is parsed by gray-matter, then silently dropped by `parsePostFile`'s
explicit field allowlist. Per the brief: this is a finding, not a deletion —
whether to wire it into the schema builder (e.g. as an `about`/`keywords`
JSON-LD field), backfill the other 49 posts, or remove it from all 13 is a
separate, later decision. Nothing changed in this package.
