// Re-pointed to shared component (DOM-identical, Phase D STD-04).
// The shared version is byte-for-byte identical to the previous local implementation.
export { ReadingProgress } from "@accounting-network/web-shared/content/ReadingProgress";
