// Re-pointed to shared component (DOM-identical, Phase D STD-04).
// Shared version uses focusRing from web-shared/components/ui/layout-utils which resolves
// the same CSS custom properties as the site-local import. Structure is identical.
export { TableOfContents } from "@accounting-network/web-shared/content/TableOfContents";
