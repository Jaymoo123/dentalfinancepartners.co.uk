# Wave 12 QA fix-round adjudications (conductor, 2026-08-21)

Binding for every fix round. Findings live in the seven QA reports in this
directory; where a report offers a drop-in, use it unless it conflicts with a
ruling below (rulings win). Writers apply ONLY items naming their surface plus
every batch-wide ruling that appears on their surface.

## Batch-wide rulings (apply wherever present on your surface)

1. **The boast class ("nobody else tells you this" / "the thing no other guide
   does" / "no other page prices this"), 22-27 instances batch-wide: DELETE or
   rewrite every instance.** Differentiation shows; it never announces itself.
   Zero self-referential boast lines per page, including titles and H1s.
2. **Rendered standfirst (`summary` frontmatter): rewrite to 40 words or
   fewer**, one clean answer sentence plus one substance sentence. No keyword
   catalogue, no 90-word single sentence, no "This page covers..." clause. If
   body paragraph 2 repeats the catalogue, delete that too (the "This page
   gives you / sets out" contents-paragraph tic dies batch-wide).
3. **QA-trail language out of reader copy**: no "we checked", "our arithmetic",
   "calculated from the sourced figures", no build-date stamps ("as at 21
   August 2026"). "(illustrative)" stays ONLY on genuinely illustrative
   figures, at most one table-note per table carrying the caveat once.
4. **FAQs: second person, batch-wide.** Any first-person FAQ block is
   rewritten. Byte-identical FAQ question strings across pages: the page with
   the LOWER traffic priority varies its string (pairs named in
   EDITORIAL_batch1 finding 8).
5. **Mandated devices keep the device, vary the words**: the calculator CTA
   ("Before you [verb]: triad" shape and the "in under a minute" promise),
   the three-quotes tip (drop the W9 verbatim qualifier "from a mix of local
   independents and bigger chains"; use the unused P5 variants), "Aim for 1.2%
   including VAT" (one page keeps it, others rephrase per their QA drop-in),
   the March/May exchange-completion example (vary the months per page).
6. **CGT block: ONE link out (rule 13).** Multi-link closes ("For X, read Y"
   chains, 5-link closes) trim to a single link, chosen per the report's
   drop-in. The 60-day point stays one line, never a walkthrough.
7. **Soft-authority density**: name a source only where a figure or quote
   needs it; delete decorative name-drops and date stamps beyond that. Zero
   bare case pincites ("(paragraph 36)") in prose; zero linked HMRC
   internal-manual quotes; case names in plain words where the case is the
   answer.
8. **reviewerCredentials: vary the sentence shape per page** (8 of 13 start
   "Checked"); keep reader voice, no internal vocabulary.
9. **Compound "X, and Y?" H2s: split or simplify** the instances your QA
   report names.
10. **EPC figure: £35 to £120 everywhere** (the sourced range).
11. **EPC deductibility: apply the new §5.B hedged position** (marketing-limb
    basis + one-clause hedge) wherever the deductible bucket includes the
    EPC. Never a bare assertion.
12. **Portal paradox: "in practice" house form.** State the s.1(4) test
    plainly, the mutual-exclusivity as how it works in practice, never as a
    statutory absolute.
13. **After-tax canonical set (FACTUAL_diy-pillar adjudication): TWO routes,
    labelled.** Route 1 no-agent: £3,860 cash / £3,165 at 18% / £2,934 at 24%.
    Route 2 online-agent (£999 + £80 check): £3,181 / £2,608 / £2,418.
    Break-even gap unchanged by tax (1.3% / 1.1%): that sentence is now a
    wave-wide keeper. Every surface states which route its figure prices.

## Per-surface (blockers first; the QA file name follows each)

- **A1** (FACTUAL_fees-calc): complete the CG15250 quote (restore "unless
  specifically provided for by TCGA92") or paraphrase outside quotation marks;
  apply its 4 advisories + batch rulings.
- **A2** (FACTUAL_fees-calc, EDITORIAL_batch2): add the £80 check to the
  pay-on-completion row (£2,329; £230 never-sell; £1,770 after tax; crossover
  £164,000); state the £999-£1,599 value-tiering condition; delete the
  research-note table cell and the build-date stamp.
- **A3** (FACTUAL_diy-pillar): fix the ID1 section B verifier list
  (conveyancer-class only, per gov.uk; the current list is ID3's); relabel the
  £80 as the identity/AML check; fix the total row to its own table's sum with
  the route labelled; CUT the 60-day walkthrough H2 to the one-line + one-link
  form (rule 13; the armed cgt1 pages own the mechanics); label its figures
  Route 2; portal-paradox "in practice".
- **A4** (FACTUAL_diy-pillar): label its figures Route 1; portal-paradox
  "in practice"; its 3 advisory drop-ins.
- **A5** (FACTUAL_diy-pillar, EDITORIAL_batch3): "roughly £3,300" -> £3,181
  labelled Route 2; strip the citation-theatre (15 name-drops + 9 date stamps
  down to figure-bearing attributions only); CGT close to one link; split the
  6 comma-spliced double-question H2s; batch rulings.
- **A6** (FACTUAL_letting-tiein-moving, EDITORIAL_batch1): make the MTD line
  conditional on the £50,000 threshold (its own worked landlord is below it);
  fix the ungrammatical hub sentence; de-stuff the six-anchor closing
  paragraph; batch rulings.
- **A7** (FACTUAL_fees-calc, EDITORIAL_batch2): add the "+ VAT" basis to all
  three Propelr borough ranges and correct the page's outer-London argument
  accordingly; rewrite the FAQ block to second person; label the floating
  at-a-glance box; trim to at most one three-quotes instance; batch rulings.
- **A8** (FACTUAL_letting-tiein-moving, EDITORIAL_batch3): remove the four
  bare pincites and the linked manual quote (plain words); fix the two Foxtons
  phrasing advisories (reconcile paras 37/20 in one sentence); delete the
  4-links-in-27-words aside; batch rulings.
- **A9** (FACTUAL_letting-tiein-moving, EDITORIAL_batch3): fix the
  first-registration sentence (voluntary-only discount; compulsory = full fee;
  use the drop-in); RESTRUCTURE: the three-bucket deductibility table moves
  from H2 #2 into the late CGT H2 (one block, rule 13); H2 #2 becomes the
  plain itemised cost content; apply the §5.B EPC hedge to the bucket; make
  the MTD/60-day lines conditional; add the England-focus scope caveat the
  calculator already carries; "Land Registration Fee Order 2024" out of prose
  (plain words); batch rulings. Structural change = verification read after.
- **A10** (FACTUAL_routes, EDITORIAL_batch1): its 3 factual advisories; boast
  purge incl the first-60-words instance; batch rulings.
- **A11** (FACTUAL_routes, EDITORIAL_batch2): its 2 factual advisories; boast
  purge (4 instances); batch rulings.
- **A12** (FACTUAL_routes, EDITORIAL_batch3): replace the permitted-area
  sentence with the drop-in (s.222(3) extension exists; the two tests differ);
  hedge the Consumer Code clause per §26.15 (one clause); "For X, read Y" x4
  trimmed to one link; batch rulings.
- **A13** (FACTUAL_routes, EDITORIAL_batch2): its 5 factual advisories; EPC
  range to £35-£120; word-count trim only if a batch ruling already shortens
  it (do not chase the 2,600 ceiling otherwise); batch rulings.
- **Pillar** (FACTUAL_diy-pillar, EDITORIAL_batch1): swap the terminal
  CTASection props so the calculator is primary and the consultation
  secondary; "£3,160" -> £3,081 labelled Route 2; second three-quotes
  instance deleted; boast purge incl the H1-area instance; the "What should
  you do next?" close gains one sentence of content beyond the CTA; batch
  rulings.
- **Calculator** (FACTUAL_fees-calc, EDITORIAL_batch3): fix the worked-example
  display arithmetic (golden-pinned £4,161; show the rounding honestly, e.g.
  1.42% of £293,000 = £4,160.60); attribute the £550 removals default (HOA);
  apply the §5.B EPC hedge in the note strings; its editorial advisories.
  The 73-word above-tool copy STAYS (conductor adopts QA's recommendation:
  the spec band is the defect; spec row will be annotated at wave close).

## Verification every writer runs before replying
Em/en-dash count 0; register re-measured and still in band; links resolve;
FAQ count still 10-14 (blog); metaTitle <=60 / metaDescription <=155; any
figure you changed re-derived and consistent with the canonical route set.
Reply with what changed, one compact list.
