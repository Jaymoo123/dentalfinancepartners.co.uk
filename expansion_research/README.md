# expansion_research/ — R0 research preconditions (2026-07-11)

Generated by `r0_collect.py` (re-runnable; read-only against Supabase and live sites, $0 spent).

| File | What it is |
|---|---|
| `r0_collect.py` | Collector script for all three deliverables. Free DataForSEO check + Supabase SELECT exports + live sitemap fetch. No Supabase writes, no paid calls. |
| `dataforseo_check.txt` | Result of the free `appendix/user_data` credential/balance check. **2026-07-11: HTTP 200, creds valid, but balance is -$0.03 — top-up required before any paid call.** |
| `corpora/<site>_queries.csv` | 28d query corpus per site: `query, impressions, clicks, source` (source=gsc\|bing). Exported from Supabase `gsc_query_data` (fresh to 2026-07-06) + `bing_query_data` (fresh to 2026-07-08), aggregated per query over the window. Cross-site piggyback seed pool + own-estate overlap baseline. |
| `own_estate_exclusion.json` | All 8 live domains (canonical from each `*/niche.config.json`, plus www/bare variants) + full live sitemap URL list per site (all fetched successfully, no local fallback needed). |

Notes:
- contractors-ir35 has 0 Bing rows (site not yet ingested/verified in BWT data pipeline); construction-cis has only 8. GSC coverage exists for all 8 sites.
